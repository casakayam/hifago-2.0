---
id: hifago-journal-2026-09
titre: "Journal hifago — septembre 2026"
theme: journal
statut: vivant
maj: 2026-09-01
resume: >
  Historique chronologique complet, une entrée par feature/session, de tout ce qui a été livré
  dans hifago/ en septembre 2026. À ouvrir seulement pour comprendre pourquoi une décision passée
  a été prise, ou retrouver un piège déjà rencontré ailleurs qu'en §11 de hifago/CLAUDE.md.
mots_cles: [journal, historique, seo, référencement, sitemap, robots, json-ld, hreflang]
repond_a:
  - "Qu'est-ce qui a été fait et pourquoi, en septembre 2026, feature par feature ?"
---

# Journal hifago — septembre 2026

> Historique complet, jamais élagué. Une entrée par feature/session, dans l'ordre chronologique.
> Ne pas résumer ni réécrire une entrée existante — seulement en ajouter de nouvelles (append),
> jamais écraser. Nouveau mois = nouveau fichier (`2026-10.md` le 1er octobre).

## 2026-09-01 — deux des cinq règles SEO étaient fausses, dont une violée par une bibliothèque

**Demande de Jérôme** : « pour le web il faut une techno qui fonctionne bien pour le référencement
IA et Google », puis « fais-en des règles et specs pour que ce soit utilisable plus tard aussi ».

**La réponse à la question posée est : la techno est déjà la bonne, et il ne faut pas y toucher.**
Next.js App Router rend le contenu côté serveur — le HTML servi contient déjà noms, descriptions et
prix, ce qu'exigent les crawlers IA qui, contrairement à Googlebot, n'exécutent quasiment pas de
JavaScript. Le seul `useEffect` qui charge des données est la disponibilité PMS, qui n'est pas
indexable. Tout le problème était dans la couche de métadonnées absente au-dessus.

### Ce que l'audit a trouvé, et qui n'était pas ce qu'on croyait

`CLAUDE.md` §5 tranchait cinq règles depuis le cadrage. Deux n'étaient pas tenues, mais pas les
deux qu'on aurait devinées.

⚠️ **§5.2 était violée par une bibliothèque, en silence.** `next-intl` active `alternateLinks` **par
défaut** (`routing/config.js` : `alternateLinks ?? !0`) et `i18n/routing.ts` ne le surchargeait pas.
Le middleware posait donc sur **chaque** réponse un en-tête HTTP `Link` portant les hreflang
`es`/`en`/`x-default` — que personne n'avait écrits, que rien ne contrôlait, **construits depuis
`x-forwarded-host`** (`getAlternateLinksHeaderValue.js`), et posés **aussi sur les fiches `noindex`
servies en repli JSONB**, en contradiction directe avec §5.3. Corrigé par `alternateLinks: false` :
une seule source, les métadonnées.

⚠️ **Mon premier diagnostic était faux sur §5.4.** J'avais écrit que « `x-default` n'existe nulle
part » et classé la règle non tenue. C'est faux : §5.4 parle du **canonical** vers la langue source,
et il était implémenté depuis le début (`products/[slug]/page.tsx`). J'avais requalifié la règle en
une exigence différente et inventée. Relevé par une revue adversariale, corrigé avant d'écrire une
ligne de code.

**§5.5 n'avait jamais existé en code** : aucun `app/sitemap.ts`. S'ajoutaient : aucun
`metadataBase` (donc tous les `canonical` déjà écrits étaient **relatifs, donc inertes**), aucun
`robots.ts`, aucun JSON-LD, aucun OpenGraph, et **aucune métadonnée sur la page d'accueil** alors
que les deux libellés dormaient déjà inutilisés dans `messages/*.json`.

### Le piège qui aurait livré un sitemap vide sans que rien n'échoue

⚠️ **Les metadata routes de Next sont PRÉRENDUES AU BUILD.** Sans directive, `app/sitemap.ts` aurait
été figé au moment du build — l'inverse exact du « sitemap dynamique » de §5.5. Et `revalidate` ne
suffit pas : le premier rendu se fait quand même au build.

⚠️ **Ce qui rendait l'erreur indétectable** : le job `build` de la CI n'a **aucun Supabase**
(`needs: [db]`, mais runner distinct, aucun `supabase start`, avec `NEXT_PUBLIC_SUPABASE_URL:
http://127.0.0.1:54321`), et `postgrest-js` **avale** l'erreur réseau en `{ data: null }` au lieu de
la lever. On aurait livré un sitemap vide, build vert, tests verts. Corrigé par
`export const dynamic = "force-dynamic"` — **prouvé par la sortie du build** : `/robots.txt` sort en
`○ (Static)` et `/sitemap.xml` en `ƒ (Dynamic)`.

⚠️ **Aucun test unitaire ne protège de ce piège** et j'avais prétendu le contraire dans une première
version du plan : un mock rend toujours des données, donc il valide la logique et jamais la
connexion. Le seul garde-fou réel est un smoke test post-déploiement, inscrit dans la spec.

### `Disallow` n'est pas `noindex`, et un groupe nommé annule le groupe générique

⚠️ Une page en `Disallow` n'est **jamais chargée**, donc son `noindex` n'est jamais lu : elle peut
finir indexée sans description. `Disallow` économise du budget de crawl, `noindex` empêche
l'indexation — jamais les deux sur la même page.

⚠️ **Aucun groupe `User-Agent:` nommé par bot dans robots.txt.** Un crawler n'obéit qu'au groupe le
plus spécifique qui le nomme et **ignore alors le groupe `*`** : un groupe `GPTBot / Allow: /` sans
répétition des `Disallow` aurait autorisé GPTBot précisément là où le groupe générique l'interdit.
La décision « tous les crawlers IA sont autorisés » (Jérôme, ce jour) est donc documentée **en
commentaire dans la source**, pas en règles dans le fichier servi.

### Le même piège d'échappement s'est refermé CINQ fois

Le JSON-LD doit remplacer chaque `<` par la **séquence d'échappement JSON** — barre oblique
inversée, `u`, `003c` — parce que le contenu vient de **partenaires** et finit dans un `<script>`.
⚠️ **La doc officielle Next montre cette ligne sous deux formes dont une est inerte** : l'une
remplace `<` par le caractère `<` lui-même, ce qui ne fait strictement rien. Les deux sont
visuellement identiques une fois rendues, et le piège s'est refermé **cinq** fois dans la même
session : dans le plan, dans la spec, dans un commentaire de code, dans ce journal — et, le plus
grave, **dans le garde-fou censé l'interdire**, qui cherchait `<`, un motif que tout fichier `.tsx`
contient, donc un contrôle qui ne pouvait jamais échouer. C'est exactement le §11 point 20.

**Leçon** : quand une règle porte sur deux écritures visuellement identiques, la vérifier par
**comportement** et jamais par relecture. Le test unitaire fait passer une description contenant
`</script>` et vérifie qu'elle ressort neutralisée.

### Les garde-fous sont vérifiés par MUTATION, pas observés verts

`scripts/check-seo.sh` (branché sur le job `lint`) porte deux contrôles. Chacun a été testé en
cassant volontairement le code : ⚠️ **la première version laissait passer la mutation « retirer
l'échappement »** — elle acceptait tout fichier qui délègue à `serializeJsonLd`, sans jamais
regarder si `serializeJsonLd` échappait encore. Durcie, puis re-vérifiée : les deux mutations sont
désormais détectées.

### Ce qu'on n'émet pas, et pourquoi

⚠️ **Aucune table d'avis n'existe** dans ce schéma (recherche exhaustive). Aucun `aggregateRating`,
`review` ni `ratingValue` n'est émis : inventer une note déclenche une action manuelle Google,
longue à lever. ⚠️ **L'adresse n'est pas décomposable** : `establishments.address` est une chaîne
unique, et `address-autocomplete.ts` ne demande que `formattedAddress` + `location` — aucun
composant (`addressLocality`, `postalCode`) n'est persisté. Seul `addressCountry: "CO"` est garanti.
⚠️ **`lat`/`lon` existent et sont déjà accordés à `anon`** (aucune migration), mais sont souvent
vides : le nœud `geo` est omis, et testé comme tel. ⚠️ **Un `evento` est un `Event`, jamais un
`Product` avec offre** : `price_cop` y est nul par contrainte, et `price_label` est un texte libre
non parsable.

### Le canonical manquait là où ça comptait le plus

⚠️ `proxy.ts` pose le cookie d'attribution depuis `?ref=` sur **n'importe quelle** page, et
`r/[code]/route.ts` redirige vers `/<locale>?ref=<code>`. **Chaque code promo distribué fabrique
donc une URL indexable distincte de l'accueil** — et l'accueil n'avait aucun canonical. Le canonical
est désormais auto-référent sur toute route routée.

### Vérifié

`npm ci` puis, sur les trois workspaces : **565 tests verts** (admin 138, web 161, domaine 266),
typecheck et lint propres, `docs:check` à jour, `check-seo.sh` et `check-design-system.sh` verts.
Build de production réussi. **Serveur réel interrogé** : `robots.txt` interdit tout, `sitemap.xml`
rend 39 URL avec `x-default` et `lastmod`, le `<head>` porte un canonical **absolu**, l'en-tête
`Link` de next-intl ne contient plus de hreflang, et le JSON-LD `Product`+`Offer` (80 000 COP) et
`BreadcrumbList` est présent et parsable. **3 tests e2e** (`e2e/seo.spec.ts`) verts — premier test
du dépôt à interroger le HTTP brut et le `<head>`.

### Aléas rencontrés, à ne pas redécouvrir

⚠️ **`npm install` a cassé `node_modules`.** Il a échoué sur un bug d'arborist
(`ERR_INVALID_ARG_TYPE` dans `rollbackMoveBackRetiredUnchanged`), et son rollback a supprimé la
plupart des liens de `node_modules/.bin` : `eslint` et `vitest` sont devenus introuvables (code
127), alors que `npx` fonctionnait encore juste avant. Réparé par `npm ci`. **Si le lockfile seul
doit changer, utiliser `npm install --package-lock-only`** — c'est ce qui a finalement servi.

⚠️ **`npx prettier --write` sur un fichier existant reformate tout le fichier** : aucune config
prettier n'existe dans ce dépôt, donc ses défauts (80 colonnes) diffèrent du style du projet (100).
Un diff de 112/59 sur une modification de 40 lignes. Annulé par `git checkout` et patchs rejoués à
la main. **Ne pas lancer prettier sur ce dépôt.**

⚠️ **`check-timezone.sh` échoue, et ce n'est PAS ce lot** : `tests/notification-real/send_8_real_emails.mjs`
(fichier **non commité**, écrit le 2026-08-31) contient `new Date().toISOString()…`. La CI cassera
au premier commit de ce fichier. Signalé, non corrigé — c'est le travail en cours d'une autre
session.

⚠️ **La spec 25 n'a jamais été ajoutée au tableau de `docs/specs/README.md`.** Constaté en y
ajoutant la 26. Non corrigé : décrire une spec que je n'ai pas lue intégralement serait pire que le
trou.

### Non fait, assumé

**Ce lot ne produira aucun effet mesurable avant la bascule de domaine**, et cela a été dit à Jérôme
avant qu'il valide le périmètre. `robots.txt` bloque tout (décision du jour), la Deployment
Protection Vercel renvoie un 302 à tout visiteur anonyme, et `hifago.co` sert encore **le legacy en
production**. Aucune validation par Search Console ou par un test de résultats enrichis n'est
possible aujourd'hui. La raison de livrer quand même : ce projet a déjà prouvé qu'une règle non
implémentée reste lettre morte — §5.5 en est l'exemple.

**Restent ouverts** : la bascule de domaine (301 depuis `/guatape` et `/reservar`, retrait du SSO,
Search Console) ; la sortie du rendu dynamique (`cookies()` dans `createClient`) — le client public
sans cookies posé ici en est la brique ; `opengraph-image` et favicon, faute d'asset de marque ;
`asLocalizedField` reste un cast sans validation (un JSONB scalaire fait renvoyer `"T"` comme titre
par `resolveLocalizedField`) ; `apps/web` n'a aucun `not-found.tsx` ; `updated_at` n'a aucun trigger,
donc `lastModified` ne bouge pas quand seules les photos changent.

**Le plafond n'est pas technique** : quelques dizaines de fiches aux descriptions courtes, sans
contenu éditorial ni avis, sur une seule ville. Ce lot lève des blocages ; il ne crée aucune demande.

**Rien commité.**

## 2026-09-01 (suite) — le terrain des composants, et un playground qui a d'abord servi à trouver des bugs

**Demande de Jérôme** : « mettre en place tout ce qu'il faut pour commencer à créer les composants
atomiques avec un playground », puis, en cours de cadrage, quatre exigences ajoutées une à une —
« penser au SEO, aux trads, à la lisibilité », « le responsive aussi », et « un socle pour lancer
plusieurs agents en même temps ».

### Storybook, choisi contre ma recommandation, et qui marche

J'avais recommandé une route interne dans `apps/web` (zéro dépendance, vrai contexte). Jérôme a
tranché pour **Storybook**. C'est la bonne nouvelle du lot : `@storybook/nextjs-vite` 10.5.10
déclare `next: ^16` et `react: ^19`, et tout a fonctionné du premier coup. Cette décision **clôt un
point ouvert** : `apps/test-ux/README.md` disait depuis le 2026-08-25 « Storybook ou preview
interne, décision séparée » — jamais instruite, jamais arbitrée. Le README est corrigé.

⚠️ **L'installation a fait entrer 3 avis `high` sans correctif** (`image-size` → DoS par boucle
infinie sur des parsers ICNS/JXL/HEIF, via `vite-plugin-storybook-nextjs`). `No fix available` :
ni `npm audit fix` ni un `overrides` n'y peuvent rien. Le job `lint` de la CI serait passé au rouge
sur tout le monorepo. Je me suis arrêté là plutôt que de contourner — décision de Jérôme :
**la CI audite désormais `npm audit --omit=dev --audit-level=high`**, c'est-à-dire ce qui est
réellement déployé. Ce qu'on perd est écrit dans le YAML : plus AUCUN contrôle automatique sur les
dépendances de développement, et le motif de revenir en arrière (un correctif d'`image-size`).

### Ce que le playground a trouvé avant même d'être fini

⚠️ **Les polices Geist ne sont pas appliquées, et ne l'ont jamais été.**
`app/[locale]/layout.tsx` définit `--font-geist-sans` / `--font-geist-mono`, alors que HeroUI et
Tailwind v4 consomment `--font-sans` / `--font-mono`. Les noms ne correspondent pas : rien ne relie
les deux, et la vitrine tourne sur la pile système. Vérifié à l'exécution — `--font-sans` vaut
`-apple-system, BlinkMacSystemFont, …`. **Signalé, non corrigé** (hors périmètre : ce serait un
changement visuel de production). Le playground reproduit fidèlement ce comportement plutôt que de
le maquiller, sinon il mentirait sur ce que voit un visiteur.

⚠️ **`hifago/DESIGN.md` et « le pipeline de design séparé » n'ont jamais existé** : quatre
références dans `apps/test-ux/`, et `git log` ne montre aucun commit de ce fichier. Cohérent avec le
fait que le thème `vitrine` ne définit **aucun** token (le thème `admin` en définit ~37). La story
`Playground/Tokens` lit les valeurs calculées à l'exécution et montre, pour la première fois, ce sur
quoi la vitrine tourne réellement.

### Les incertitudes, levées par mesure et pas par confiance

Le plan imposait quatre vérifications avant d'écrire quoi que ce soit. Toutes faites en réel, au
navigateur piloté :
- **Tailwind scanne-t-il sous Vite ?** C'était le piège n°1 : le `@source` de `packages/ui` ne
  couvre que `packages/ui/src`, et sous Vite la racine de détection change — les composants
  s'afficheraient **sans style, en silence**. Parade : le `@source "../**/*.{ts,tsx}"` déclaré dans
  `apps/web/app/globals.css` — remonté là plutôt que laissé dans un `.storybook/preview.css`, pour
  que la production ferme le même piège et pas seulement le playground ; ce fichier a donc été
  supprimé. Preuve : une carte en `h-80`, classe qu'aucun autre fichier n'utilise, mesure **320 px**.
  *(Formulation corrigée le 2026-09-01 en fin de journée — elle citait encore `preview.css`, jamais
  commité. Relevé par l'agent B de la vague 1.)*
- **Les composants réels rendent-ils ?** C'est le défaut que j'avais laissé passer dans la première
  version du plan : sans stories sur du code existant, on validait un tuyau vide. Corrigé —
  `CatalogBrowser` et `ProductPhotos` ont leurs stories. Mesuré : libellés traduits (`Buscar` en es,
  `Search` en en), `href` localisé `/es/products/…`, images `next/image` réellement chargées
  (`naturalWidth > 0`), état vide présent, **aucun débordement horizontal en 390 px**, zéro erreur
  console.

### L'i18n éclatée, pour une raison que j'avais d'abord mal formulée

Les messages passent d'un `es.json` unique à **un fichier par namespace et par locale**, agrégés par
`messages/index.ts`. ⚠️ **Ma première justification était fausse** : j'avais écrit « conflit git
garanti », alors que git merge souvent sans heurt des clés éloignées. La vraie raison est pire :
d'après `AGENTS-PARALLELES.md`, les agents travaillent **dans le même répertoire de travail** — il
n'y a donc **aucun merge git**, ils s'écrasent en direct, sans que rien ne le signale.

Imports **statiques** à dessein : `import.meta.glob` ne marche que sous Vite, une lecture de disque
ne marche ni côté client ni en test. Seuls des imports statiques fonctionnent dans les trois
environnements (Next serveur, Vitest, Vite). Le fichier d'agrégation ne change qu'à l'ajout d'un
*namespace* (rare), jamais d'une clé.

**`messages/parity.test.ts`** attrape la faute i18n la plus silencieuse (une clé d'un seul côté :
next-intl affiche alors le nom brut de la clé au client) **et** le namespace créé mais jamais branché
dans l'agrégateur. ⚠️ **Vérifié par MUTATION**, pas observé vert : retirer `searchLabel` de l'anglais
le fait échouer, et créer un `Chrome.json` non déclaré aussi — le test nomme le fautif.
⚠️ **Ce qu'il ne fait PAS** : vérifier qu'une clé est utilisée, ni qu'aucune chaîne n'est écrite en
dur. Cette règle-là reste tenue par la relecture ; ne pas croire qu'elle est couverte.

### Trois pièges rencontrés en écrivant

1. ⚠️ **Le lint a attrapé un vrai défaut dans ma story `Tokens`** : `setState` synchrone dans un
   effet. La correction donne un meilleur composant — initialiseur paresseux pour la lecture, et
   l'effet ne sert plus qu'à **s'abonner** aux changements de `data-theme` par `MutationObserver`.
   La bascule de thème est désormais réactive sans rechargement.
2. ⚠️ **Storybook conserve les accents dans les identifiants de story** : `Playground/Sémantique`
   donne `playground-sémantique--…`, URL fragile. Parade : `id` explicite en ASCII dans le `meta`,
   le titre affiché gardant son accent. Convention notée dans le README des composants.
3. ⚠️ **L'iframe de prévisualisation contient deux `<h1>` cachés** (« No Preview » et un vide) : une
   vérification automatique qui compte les titres doit filtrer sur la visibilité, sinon elle croit à
   trois `h1` là où la story n'en a qu'un.

### Le découpage pour travailler à plusieurs

Principe retenu : **supprimer les points de collision par construction** plutôt que compter sur un
protocole. D'où **aucun barrel** dans `components/` et **aucun registre de stories** (découverte par
glob) : ajouter un composant ne modifie aucun fichier partagé. `AGENTS-PARALLELES.md` reçoit une
section « Agent qui crée des composants » — périmètre exclusif, fichiers interdits, ports à ne pas
prendre — et le découpage en vagues : vague 1 séquentielle sur les atomes partagés (le vrai risque
n'est pas le conflit de fichier mais deux agents créant le même atome sous deux noms), puis vague 2
à quatre agents, un par verticale, frontières calées sur les namespaces i18n existants.

### Vérifié

**567 tests verts** (admin 138, web 163 dont les 2 nouveaux, domaine 266), typecheck, lint (avec
`eslint-plugin-storybook`), `npm run build --workspace=apps/web`, `build-storybook`,
`npm audit --omit=dev`, `check-seo`, `check-design-system`, `docs:check`.

⚠️ **`check-timezone.sh` échoue toujours** sur `tests/notification-real/send_8_real_emails.mjs` —
fichier **non commité** du 2026-08-31, déjà signalé au lot précédent. Ce n'est pas ce lot ; la CI ne
le verra pas tant qu'il n'est pas commité, mais elle cassera au premier commit de ce fichier.

### Non fait, assumé

Aucun composant du design system n'a été créé — c'était le périmètre (« le terrain seulement »).
Le playground contient donc des stories sur des composants **existants**, les primitives HeroUI, les
tokens et un gabarit sémantique. La **vague 1** (`PageShell`, `Title`, `Price`, `TypeBadge`,
`BackLink`, `Image`) reste à faire, et doit rester séquentielle.

⚠️ **Storybook est structurellement aveugle au responsive de composition** : il rend des composants
isolés, alors qu'un débordement vient presque toujours du parent. Les e2e Playwright à 390×844 sur
les vraies pages restent ce qui tranche — ne pas les abandonner sous prétexte qu'on a un playground.

**Rien commité.**

## 2026-09-07 — Le rangement : CLAUDE.md ne porte plus d'état, les pièges deviennent des règles chargées à la demande, hifago/ devient le projet

Contexte : Jérôme, « le repo ne va pas, il y a de la doc et des instructions de partout ». Audit par
trois explorateurs, plan v1, puis plan v2 après un panel adversarial de cinq critiques (37 findings,
2 bloquants : le curseur §12 reconduisait le dispositif qui avait déjà régressé en deux jours le
18/08 ; « §12 supprimé sans perte » était faux — le trou (b) n'existait nulle part ailleurs). Plan :
`~/.claude/plans/le-repo-ne-va-lexical-deer.md`. Ce jour : chantiers 1, 2 et 3 du plan v2. Restent
le 4 (hook à l'édition + `scripts/check-instructions.mjs` + `statut`/`revise:` dans
`docs_index.js`), le 5 (doc d'aplomb : 6 statuts faux, spec 25, README, prompts/) et le 6
(relecture intégrale des quatre cahiers).

**Décisions de Jérôme** : `hifago/` s'ouvre désormais comme racine de travail (45 sessions sur 46
depuis le 15/08 ouvraient le dépôt legacy parent — les skills `hifago-*` n'y étaient pas chargés,
Claude Code ne remonte les skills projet que jusqu'à la racine git) ; Claude Code est le seul agent
(Codex retiré du contrat d'`AGENTS.md`) ; conventions ECC reprises (taxonomie rule / skill / agent /
hook, seuils, « pointer, pas répéter »), rien d'ECC installé.

**Chantier 1 — hifago/ devient le projet.** 8 skills `hifago-*` déplacés de `/.claude/skills/`
(dépôt parent) vers `.claude/skills/` ; `hifago-init` archivé (`archive/skills/`, scaffold du
2026-08-12, décrivait des choix abandonnés le 14/08) ; `supabase`/`supabase-postgres-best-practices`
réinstallés ici (`npx skills add supabase/agent-skills`) ; 27 renvois `hifago/CLAUDE.md` →
`CLAUDE.md` dans les skills ; 4 mémoires copiées vers le dossier projet de hifago ; `.gitignore`
complété ; skills `next`/`backlog` réécrits pour `docs/backlog.md`. Dépôt parent : `AGENTS.md`
(titre, point 0, §6 retiré), `scripts/setup-agent-skills.ps1` et `.github/workflows/hifago-ci.yml`
(doublon mort) supprimés — rien d'autre.

**Chantier 2 — CLAUDE.md sans état.** `docs/backlog.md` créé (thème `journal`, jamais chargé,
cité par `CLAUDE.md` §12 et par la table de routage) — chaque point vérifié comme encore ouvert dans
le journal et le `git log` avant d'y entrer (`RESEND_API_KEY`, fermé le 31/08, en est exclu) ; §12
(569 l.) supprimé, remplacé par 3 lignes de pointeurs ; règles Vercel extraites vers §7.4 ; 12
renvois « CLAUDE.md §12 » réécrits vers le journal (specs 05/07/18/19/21/22, une migration,
`db-setup.sh`, `login/page.tsx`, 2 e2e) ; `AGENTS-PARALLELES.md` point 1 réécrit, et sa section
« Découpage en vagues » marquée comme plan d'origine (l'exécution réelle est allée jusqu'à la
vague 8). **Écart au plan** : pas converti en skill — 27 fichiers vivants (10 tests de concurrence,
specs, e2e, README) le citent par son nom exact.

**Chantier 3 — règles par déclencheur, invariants toujours chargés.** `CLAUDE.md` 1 046 → moins de
200 lignes (cible de la doc officielle) ; 5 règles `.claude/rules/` avec `paths:` — `supabase.md`,
`tests.md`, `apps.md`, `seo.md`, `ui.md` — **le plan en prévoyait 4** : le SEO a un déclencheur
distinct (`apps/web/**` seulement) et le fusionner dans `apps.md` dépassait les 100 lignes ; §5 et
§6.5 y ont migré ; §3 et §4 restent entiers dans `CLAUDE.md` (une règle `paths:` ne se charge pas
quand on CRÉE un fichier) ; §11 conservé en index numéroté (88 renvois vivants dans 83 fichiers,
dont 12 migrations SQL appliquées) ; §8.4 = piège 18 ; argumentaire calendrier de §2.2 déplacé dans
`docs/04` (révision du 2026-08-29) ; `hifago-migration`, `-ui`, `-review`, `-test`, `-seed` citent
au lieu de recopier ; `apps/web/components/README.md` dédupliqué (4 blocs → renvois).

### Traçabilité de l'ancien §12 (texte intégral : `git show f598a2d:CLAUDE.md`, l. 479-1046)

| Bloc de §12 (lignes) | Destination |
|---|---|
| 2026-09-01 2e lot (486-522) | `2026-09.md` l. 181 ; polices Geist → backlog ; `@source` → `ui.md` |
| 2026-08-28 tâche 3 (524-558) | `2026-08.md` l. 7509 (+ 7777 cache) ; `createTtlCache`, `addDaysIso`, `pms_category_not_quoted`, sonde `start==end` → backlog ; `end_date` inclusif, fixture, date de la réponse → `supabase.md`, `tests.md` |
| 2026-08-29 cinq lots (560-615) | `2026-08.md` l. 7895-8252 ; RangeCalendar → `docs/04` + `ui.md` ; deux règles opposées → `supabase.md` ; `setPendingOrderId` → `apps.md` |
| ⚠️ Trous (a)/(b) (617-626) | (a) `2026-08.md` l. 8176 et suiv. ; (b) **nulle part ailleurs** → `docs/backlog.md` § Arbitrage |
| 2026-08-27 (628-695) | `2026-08.md` l. 5912-6767 ; NON FAIT (1)(2)(3)(5) → backlog ; Vercel → §7.4 ; grants `establishments`, Edge/Vault, Deno, `pg_get_functiondef` → `supabase.md` |
| 2026-08-24 suite 3, spec 23 (697-744) | `2026-08.md` l. 5222 (paragraphe gras, pas d'en-tête `##`) |
| 2026-08-24 suite 2, agenda (746-753) | `2026-08.md` l. 5209 |
| 2026-08-24 suite, retest MP (755-767) | `2026-08.md` l. 5188 ; support MP → backlog |
| 2026-08-24 walk-in (769-783) | `2026-08.md` l. 5137 |
| 2026-08-23 relais IP (785-802) | `2026-08.md` l. 5007 ; IP à déclarer → backlog |
| 2026-08-23 Vercel préprod (804-845) | `2026-08.md` l. 4854 ; Git↔Vercel (`cosmogab`) → backlog ; `framework=nextjs`, redeploy, Shared env → §7.4 |
| 2026-08-21 seed cloud (847-863) | `2026-08.md` l. 4773 ; rotation `service_role` → backlog ; piège 17 → `/hifago-seed` ; 18 → §8.4 |
| 2026-08-21 dispo live (865-880) | `2026-08.md` l. 4605 ; Route Handler jamais depuis un Server Component → `apps.md` ; modifier CSS sur `<td>` → `tests.md` |
| 2026-08-20/21 suite 9 (882-902) | `2026-08.md` l. 4462 (suite 8) et 4551 (suite 9), paragraphes gras |
| 2026-08-19 suite 2, LobbyPMS T1 (904-928) | `2026-08.md` l. 3102 ; `productTypeGating` → `apps.md` |
| 2026-08-19 Mi cuenta (930-947) | `2026-08.md` l. 3056 |
| 2026-08-18 suite 6, spec 19 T1 (949-990) | `2026-08.md` l. 2906 ; `waitForURL`, FK du nettoyage e2e → `tests.md` ; « Reintentar pago » → `apps.md` ; webhook secret, Tranche 2, page de retour → backlog |
| 2026-08-18 suite 7 (991-998) | `2026-08.md` l. 2966 |
| 2026-08-19 spec 20 (1000-1009) | `2026-08.md` l. 3013 |
| « Corrigé le 2026-08-24 » + liste en attente (1011-1042) | correctif : `2026-08.md` l. 5137 ; toute la liste « en attente » → backlog § Dette QA/UI |

Vérification par lecture de chaque bloc et recherche de son contenu dans le journal (pas seulement
de ses mots-clés) ; les deux entrées sans en-tête `##` sont des paragraphes gras aux lignes
indiquées. Tout ce qui était une action ouverte a une ligne dans `docs/backlog.md`.

### Traçabilité de l'ancien §11 (20 points, `git show f598a2d:CLAUDE.md` l. 242-478)

1 → `supabase.md` · 2, 3, 4, 5, 7, 8 → `tests.md` · 6, 9, 15, 16 → `apps.md` · 10 → `tests.md`
(environnement) · 11 → `apps.md` (formulaires — le plan le mettait dans `tests.md`, mais
`noValidate` se pose dans le `.tsx`) · 12, 13, 14 (avec sa leçon générale) → `supabase.md` · 17 →
`/hifago-seed` (le portait déjà) · 18 → `CLAUDE.md` §8.4 · 19 → journal (récit) + backlog (support
Mercado Pago) · 20 → `CLAUDE.md` §11 (doctrine) + `tests.md` (fuseau). Les récits de diagnostic
(cause non élucidée des points 7, 15, 16, 17, 19) ne sont pas recopiés : ils sont dans le journal
aux dates données dans chaque point d'origine. L'index §11 de `CLAUDE.md` garde les 20 numéros.

**Vérifié** : `CLAUDE.md` 198 lignes (1 046 au matin) ; règles 45-84 lignes chacune ; backlog 52 ;
les 18 sous-points cités par le code vivant (`§2.1/3/6/7`, `§3.5`, `§4.1/4`, `§5.1-5`, `§6.5`,
`§8.2-4`, `§11.16`, `§11 point 20`) existent tous — `§5.1-5.5` avaient d'abord disparu dans un
stub, rattrapés par le contrôle ; `npm run docs:check` vert (39 documents) ; `npm run typecheck`
vert (web + admin) ; `check-design-system.sh`, `check-seo.sh`, `check-timezone.sh` verts. Non
vérifié ici, à faire dans une session ouverte depuis `hifago/` : `/context` (Memory files = ce seul
`CLAUDE.md`, skills `hifago-*`/`next`/`backlog` présents) et le hook `InstructionsLoaded` sur un
fichier de chaque glob (`.sql`, `.test.tsx`, `.spec.ts`, `apps/web/**`, `packages/ui/**`) — c'est
le test décisif du chantier 3, il n'a pas pu être fait depuis cette session (ouverte à la racine).
Rien n'est commité : à relire et committer par Jérôme.

## 2026-09-02 → 2026-09-04 — Vagues 2 à 8 de la vitrine (rattrapage rétroactif, 6 commits)

Entrée reconstituée le 2026-09-07 pendant le rangement (chantier 5) : ces 6 commits n'avaient reçu
aucune entrée de journal au moment où ils ont été faits — exactement le mode d'échec que le nouveau
hook Stop (`scripts/hooks/check-journal-updated.mjs`) est censé attraper désormais. Reconstituée à
partir des messages de commit, qui étaient déjà détaillés.

**217c1b6 (02/09) — 14 atomes au total.** Button/IconButton, Card, LinkButton, Field, Textarea,
Select, Checkbox écrits par des agents à périmètres disjoints. Ferme le piège §11.16 pour l'app :
chaque fichier qui importait `@hifago/ui` directement risquait de casser `next build` ; ils sont
désormais le seul point d'entrée HeroUI, tous `"use client"`. HeroUI n'a pas d'axe couleur sur son
`Button` (variant à 7 valeurs mélangeant forme et couleur) — deux axes séparés obtenus en reposant
les custom properties `--button-bg*` que HeroUI consomme, sans réécrire le composant.

**e2e5437 (02/09) — Storybook rangé par ce qu'on cherche.** Cinq groupes (Actions/Saisie/Affichage/
Structure/Playground) au lieu de quatorze entrées à plat sous « Atoms ». Le `title` d'une story ne
suit pas le dossier — délibéré, deux besoins différents (composabilité vs recherche). Aucun
composant touché, seulement les `title`.

**35a293f (02/09) — deux défauts trouvés par la revue croisée, tous deux corrigés.** (1) La raison
d'être n°1 de la surcouche (« un composant qui importe `@hifago/ui` porte `"use client"` ») n'était
tenue par rien — invisible au typecheck/lint, seulement à `next build` (§11 point 20). Rejoint
`check-design-system.sh`, vérifié par mutation. (2) Le calcul de contraste WCAG était écrit deux
fois, divergent (seuils 0.03928 vs un autre) — remonté au thème, un seul endroit.

**ed2df08 (02/09) — un défaut d'accessibilité réel fermé.** Le message d'erreur de 3 champs sur 4
(Field/Textarea/Select) était sous le seuil WCAG (`--danger` mesuré 3.56:1, sous les 4.5:1 requis) —
`Checkbox` avait la bonne couleur posée à la main, les trois autres déléguaient à HeroUI. Décision
remontée dans le thème.

**9612130 (04/09) — vagues 6 et 7 (toasts, calendrier, barre de recherche), plus la revue.** ⚠️ La
parade au remontage de la grille de calendrier n'en était pas une : `DayButton` était défini À
L'INTÉRIEUR de `Calendar`, mémoïsé sur des valeurs qui changent à chaque rendu de l'appelant —
react-day-picker démonte/remonte ses 42 cases dès que le TYPE du composant change. `BoutonJour`
hissé au niveau du MODULE (son type ne peut plus changer), la donnée par jour transmise par contexte.

**f598a2d (04/09) — Vague 8 : DateRangeField, PeopleField, SearchPanel.** Un seul agent
délibérément (les deux filtres partagent le même déclencheur à popover). Disposition tranchée par
Jérôme : filtres sous la barre, pas en segments dans la pilule. `DateRangeField` enveloppe le
`Calendar` de la vague 6 en `mode="range"` — pas une seconde grille, qui aurait réintroduit des
défauts déjà mesurés et corrigés.

**Non vérifié depuis (rattrapage seul, pas une revue) : build/tests/lint de l'état final de ces 6
commits.** À faire avant de s'appuyer dessus pour la suite si un doute apparaît.

## 2026-09-07 (suite) — Chantier 6 : relecture intégrale des quatre cahiers des charges

Dernier chantier du plan de rangement (`~/.claude/plans/le-repo-ne-va-lexical-deer.md`), fait le
même jour que les chantiers 1-5 mais dans une session à part, ouverte depuis `hifago/`. Méthode
imposée par Jérôme : **un seul fil, séquentiel** — pas d'agents parallèles, un cahier après
l'autre, section par section, contre les 26 specs et le code réel (migrations, `apps/`). 2 861
lignes relues ; 205 lignes ajoutées, **aucune supprimée**.

**Ce qui a été produit** : l'en-tête `## Écarts connus` existe maintenant dans les **quatre**
cahiers (il n'était posé que dans 01 et 02, pour les specs 15 et 19) ; **8 specs** portent un champ
`revise:` contre 2 au matin. Rien n'a été réécrit dans les sections elles-mêmes — décision
délibérée : une section d'un cahier est une unité de validation avec Jérôme (« ✅ validé le … »),
la réécrire silencieusement invaliderait ce statut. L'en-tête en tient lieu, comme pour 15 et 19.

**Les deux écarts les plus lourds étaient prévus par le plan, et ils l'étaient à raison.**

*Spec 24 (retrait de l'étage hôtel, T3 du 2026-08-27) — elle touche bien les quatre cahiers, pas
seulement le 00.* Le §2 du modèle de données avait été réécrit le jour même ; tout le reste avait
été laissé. Le « **gap critique** — aucun mécanisme de prix/disponibilité pour un établissement à
chambres sans PMS », présenté comme *le point le plus structurant de tout l'audit* et cité comme
tel par 00 (§1 + Synthèse), 01 (§4) et 02 (§4 + §7), est **fermé** depuis que la chambre est un
produit `lodging` ordinaire avec son `product_availability` et son `product_date_rates`. Trois
autres lignes tombaient avec lui : le slug d'établissement « ❌ toujours ouvert » (livré, migration
`20260827200000`), les horaires check-in/check-out « absents pour le mode `rooms` » (livrés, même
migration), et l'affirmation de 01 §5 selon laquelle l'identifiant d'un type de chambre PMS-backed
« **EST** directement » celui de la catégorie Lobby (faux : `products.id` d'un côté,
`products.lobby_category_id` de l'autre).

*Spec 18 (créneaux horaires réservables) — l'écart qu'elle avait elle-même déclaré.* Sa section
« Out » disait noir sur blanc : « Mise à jour des 3 cahiers des charges — référencés, pas
réécrits », et son §1 constatait que les cahiers « ne promettent que le mécanisme legacy binaire
matin/après-midi ». C'était exact et c'était resté vrai : cinq passages (01 §3a/§3d/§3e, 02 §3d,
03 §2/§3g) décrivent encore deux demi-journées, dont une phrase devenue absurde — « une date n'est
fermée que si ses **deux** créneaux sont pleins ». `product_slot_rules` produit autant de créneaux
que la règle en définit, et `product_calendar.closed_slot`, la colonne que ces phrases décrivaient,
a été droppée par la spec 17 T0 (`20260817160000`).

**Trois écarts que le plan n'avait pas anticipés, dont un renversement complet.**

⚠️ **02 §3d affirme encore que le prestataire ne voit « **jamais** le contact du client (téléphone,
email) : ce n'est pas son canal de relation avec le client final ».** C'est l'inverse du système
réel depuis le 2026-08-19 : `order_lines` porte `holder_phone`/`holder_email` (migration
`20260819180000_order_lines_holder_contact_operator.sql`, « Le prestataire a désormais besoin de
contacter son client »), et `apps/admin/app/partner/(app)/reservations/` les affiche en liste comme
en détail. La spec 24 §10.C s'appuie même dessus pour justifier l'envoi des coordonnées dans la
note du booking Lobby. Ce n'est pas une nuance de vocabulaire : c'est une règle de confidentialité
énoncée comme un principe, exactement contredite par le code. **Effet de bord trouvé au passage** :
deux specs portent encore l'ancienne règle — 17 (§ invariants, « PII minimale côté socio ») et 20
(l.73) — donc l'écart survit dans des documents que le contrôle `revise:` ne regarde pas. Reporté
en backlog plutôt que corrigé ici : ce sont des specs `implemente`, les toucher demande de dire
laquelle des deux dates fait foi, et c'est déjà tranché — mais dans le journal, pas dans leur
texte.

**L'acceptation de contrat par rôle a été abandonnée (2026-08-19 puis 2026-08-20), et 03 ne le sait
pas.** Le cahier 02 §3a raconte l'abandon en détail ; le cahier 03 en dépend encore à trois
endroits — §3d (« l'admin doit voir cet état accepté/en attente »), §3g (« le versement est bloqué
si son identité n'a pas encore accepté le contrat »), §3f (la justification du périmètre du rappel
Habeas Data). Le cas d'école de ce chantier : une décision correctement documentée là où elle a
été prise, jamais propagée là où elle était consommée.

**Deux points ouverts avaient été tranchés sans que le cahier l'apprenne.** 02 §3b laissait « à
trancher au chiffrage » l'ajout d'un établissement supplémentaire (self-service ou via l'admin) —
la spec 06 l'a tranché le 2026-08-15 : self-service, par proposition modérée. Corollaire dans la
même section 02 §3e : « un prestataire **ne crée jamais** un hébergement » est faux depuis. Et 02
§3h porte encore un « Point à challenger avec Jérôme » sur le QR ré-attribuable, alors que la
recommandation a été tranchée le 2026-08-13 et livrée : `apps/web/app/[locale]/r/[code]/route.ts`
redirige, et la spec 26 §5.3 raisonne dessus.

**Le reste, plus petit, mais faux quand même** : le type `tour` n'existe plus (retiré par la spec 17
T0) alors que 02 le cite trois fois ; le canal des notifications proactives n'est plus « à définir »
(Resend, spec 23, avec son journal `notification_emails` — ce qui rend à moitié fausse la ligne « ❌
absent » de 00 §6) ; la « majoration week-end » listée en cible future par 01 §7/A11 est livrée
depuis la spec 12 ; 01 §3a interdit toute tarification par nombre de personnes sur un hébergement,
alors que la spec 12 y a réutilisé `price_tiers` tel quel — nuance conservée dans l'en-tête, parce
que ce que 01 décrit (« seuil + pourcentage de remise ») n'a effectivement jamais été construit,
pour aucun type de produit ; et 03 §3c dit « lecture seule pour l'admin » d'un hébergement
PMS-backed, alors que la frontière posée par la spec 24 est plus fine — Lobby fait foi sur la
**disponibilité** seule, hifago sur nom, description, photos, capacité et prix.

**Vérifié.** `npm run docs:check` vert (39 documents, `ai-index.json` et `INDEX.md` régénérés) ;
`npm run check:instructions` vert ; `git diff --stat` sur les quatre cahiers = **205 insertions, 0
suppression**. **Test négatif du garde-fou `revise:`**, celui qui compte : les quatre cahiers remis
à leur version commitée (sauvegarde hors dépôt, restaurés ensuite), `docs:check` lève **25
violations** nommant chaque couple spec↔cahier et exigeant l'en-tête « Écarts connus ». Le contrôle
posé au chantier 4.5 n'est donc pas décoratif — c'est lui qui empêchera la prochaine spec de
repartir sans son cahier. Rien n'est commité.

## 2026-09-07 (suite) — Le parcours client a changé sur ses quatre axes, et le cahier ne le savait pas

Session ouverte sur une demande de Jérôme : « maintenant qu'il y a la plupart des composants pour
faire le front de web, on va commencer le dev dessus […] d'abord il faut penser à l'archi du site,
pages ouvertes à tout le monde et celles quand pas log ». Elle s'est terminée ailleurs que prévu, et
c'est le fait marquant.

### Le diagnostic, avant toute discussion

L'écart n'était pas « il manque des composants ». Les **26 composants** des vagues 1 à 8 (chacun
avec son test et sa story) sont écrits, et **aucun n'est branché dans une route** : compté à la
main, `SiteHeader`, `SiteFooter`, `SiteMenu`, `SiteToaster`, `SearchBar`, `SearchPanel`,
`LanguageSwitcher`, `PageShell`, `Card`, `Title`, `BackLink`, `TypeBadge`, `PhotoStrip` = **zéro
usage** hors stories et tests. Les seuls imports de `@/components` dans `app/` sont trois `JsonLd`.
Le layout ne monte ni header ni footer — le site n'a **aucun landmark de navigation**.

Pire, la coquille avait déjà **décidé de l'architecture sans qu'elle soit écrite** : `SiteFooter`
pointe vers `/legal`, `/privacy`, `/contact`, `/help`, `/terms` — cinq liens morts ; `SiteHeader`
envoie le panier sur `/checkout` alors qu'il n'y a pas de route `/cart` ; et `SearchPanel` documente
lui-même « il n'existe pas encore de route de recherche pour porter les dates et le nombre de
personnes ».

### Le retournement

À la quatrième question de cadrage, Jérôme a coché **les quatre axes** de « qu'est-ce qui a changé
depuis le cahier client ? » — arrivée et découverte, ce qu'on vend, parcours de réservation, compte
et après-vente. Autrement dit **le §2 entier ne faisait plus foi**.

Conséquence méthodologique immédiate, et c'est elle qui a réorganisé la session : **on ne pouvait
pas écrire la spec d'architecture**. La carte des routes n'est pas une décision indépendante, elle
*tombe* du parcours. L'ordre est donc devenu : capturer le parcours → réécrire le cahier → écrire la
spec. Un panel de conception à quatre angles avait été lancé en parallèle au début de la session ; il
a été abandonné sans être lu, ayant été construit sur l'ancien parcours.

### La méthode, choisie par Jérôme

Interview **étape par étape** (« pas à pas, pas tout d'un coup » — il a repris la main deux fois
quand j'avançais trop vite, dont une fois pour dire « avant de passer à la fiche produit on n'a pas
fini la home »). Cahier **réécrit pour de vrai**, pas un en-tête « Écarts connus » : « une section
est une unité de validation », donc elle se revalide. Puis **une spec d'archi, puis une spec par
écran, écrites une à la fois** — argument retenu : les quatre specs du projet écrites « en gros »
(17, 19, 21, 24) sont toutes encore `partiel`, quand les specs serrées sont toutes `implemente`.

### Ce que l'interview a produit

**L'accueil EST l'écran de résultats de recherche** — les critères vivent dans son URL, pas sur une
route `/buscar`. Ça a une conséquence heureuse : le canonical auto-référent de l'accueil, posé pour
`?ref=`, couvre `?q=` gratuitement. Et une conséquence à trancher dans la spec : une page qui lit
ses critères dans l'URL devient dynamique, ce qui mord sur la décision « lectures publiques
anonymes, pages cacheables » prise plus tôt dans la même conversation.

**Une seule fonction de requête sert trois écrans** : `buscar({})` = accueil, `buscar(critères)` =
résultats, `buscar({tipo})` = listing. C'est la réponse directe au « il y aura pas mal de requêtes à
gérer » de Jérôme — il n'y en a qu'une, avec ses index.

**La page des activités est un index de tags**, pas une liste de produits (les quatre autres types
listent directement). Structure choisie aussi parce qu'elle reste entièrement crawlable là où une
grande liste en scroll infini ne montre à Google que sa première fournée.

**Cinq décisions renversent ou ferment du déjà-validé**, toutes écrites comme telles dans le cahier
avec la date qu'elles renversent :
1. le **placement des suggestions** — ouvert depuis le 2026-08-11 — est fermé : après *chaque*
   ajout au panier, par retour à l'accueil réordonnée selon le panier ;
2. la **modification d'une réservation** depuis le compte sort du premier périmètre (renverse le
   2026-08-13) ; `modify_order_line` reste utilisée côté socio/admin ;
3. **plus de WhatsApp sur la confirmation** — tenable seulement parce que `SiteFooter` en porte un
   sur toutes les pages ;
4. **`partners.phone` devient public** pour permettre de contacter l'établissement. ⚠️ Porté en
   version étroite dans le cahier : le téléphone d'un partenaire **possédant un établissement
   actif**, jamais la table entière — `partners` est la table d'identité de tout le monde, référents
   particuliers compris ;
5. le **rayon de 20 km** n'avait pas été reconduit dans le périmètre de recherche. Signalé plutôt
   que supprimé en silence, puis tranché : **différé, pas abandonné**, et la fonction de recherche
   doit pouvoir l'accueillir sans être réécrite.

**Le panier change de nature** : il survit maintenant à la fermeture de l'onglet. Ce qui était un
cas rare — une ligne devenue indisponible entre l'ajout et le paiement — devient fréquent. Elle est
signalée en place, jamais retirée automatiquement (invariant §3d).

**Deux périmètres neufs sont apparus, nommés comme tels** : rattacher une commande passée en invité
à un compte créé après coup (RPC à écrire), et le champ de contact d'un établissement.

### Décisions transverses actées pour le front

URL **en espagnol partout**, segments non traduits · **aucune requête Supabase dans un `page.tsx`**,
tout passe par `lib/catalog/` · lectures publiques par le **client anonyme sans cookies** ·
**identité visuelle neutre d'abord** (donc aucune couleur en dur, ce qui devient un contrôle CI, pas
une consigne) · **quatre zones, quatre coquilles**, la seule garde d'auth à l'entrée du compte —
l'invité doit pouvoir réserver de bout en bout · on repart **de zéro sur les vues** en **extrayant
d'abord** la logique de réservation (`LodgingReservationForm` et ses 572 lignes de disponibilité
LobbyPMS, vérifiées en réel le 2026-08-27) plutôt qu'en la réécrivant.

### Vérifié

`npm run docs:index` puis `npm run docs:check` verts — 39 documents, manifeste et index à jour.
`git diff --stat` sur le cahier : **173 insertions, 129 suppressions**, §2 découpé en six
sous-sections (2a accueil et découverte · 2b parcours principal en 9 étapes · 2c compte client ·
2d condition d'annulation · 2e parcours secondaires · 2f points encore à trancher). Statut de la
section passé à **« en relecture »** avec renvoi git vers la version validée du 2026-08-11.

### Non fait, assumé

- **§2 n'est pas revalidé** — il est en relecture, c'est Jérôme qui referme.
- **§3e (règles de panier) n'a pas été touché** alors qu'il décrit des plafonds pensés pour un
  panier de session, devenu un panier de plusieurs jours. Autre unité de validation.
- **La spec 27 n'est pas écrite.** Elle vient après la relecture du cahier.
- Le carnet de décisions complet de la session vit dans
  `~/.claude/plans/maintenant-qu-il-y-a-parsed-robin.md` (carte des routes, quatre zones, cinq
  contrôles CI à créer). Il n'est pas une source de vérité du dépôt : la spec 27 le remplacera.

## 2026-09-07 (suite) — L'audit adversarial du §2 : 25 trouvailles sur 40 étaient fausses, et les 14 vrais trous n'étaient pas des erreurs

Suite immédiate de l'entrée précédente. Jérôme demande : « est-ce qu'il y a besoin de challenger ?
ou de le faire par spec par exemple ou coûteux pour rien ? ». Réponse retenue, et c'est elle qui a
donné sa forme à l'audit : **challenger les décisions ne sert à rien** (ce sont les siennes), et
**challenger par spec est déjà couvert** (`/hifago-review`, `docs:check`, `avant-la-spec.md`). Le
seul trou réel est celui que **rien ne vérifie** — cahier↔cahier, et §2↔§3 dans le même document —
et il ne se comble qu'une fois, sur la source.

### Le dispositif

46 agents : cinq chercheurs (les trois autres cahiers · les 26 specs · le reste du cahier 01 ·
le code réel · la fidélité du texte au carnet d'interview), puis **un vérificateur par trouvaille,
dont la consigne était de la RÉFUTER**, pas de la confirmer — « par défaut, une trouvaille que tu ne
peux pas prouver est fausse ». Enfin un critique de complétude.

⚠️ **Premier lancement tué par la limite de session** (38 agents sur 46 en échec, reset à 17 h). Le
`resumeFromRunId` a tenu ses promesses : les cinq chercheurs ont rejoué depuis le cache, seuls les
vérificateurs sont repartis. Second passage : **46/46, zéro échec**.

### Le résultat qui compte : 25 trouvailles sur 40 sont tombées

**62 % de faux positifs**, éliminés par la passe adversariale. Sans elle, on aurait « corrigé »
vingt-cinq problèmes inexistants dans quatre cahiers. Les 15 survivantes se replient sur **5
problèmes distincts** — la duplication entre cibles étant précisément le signal :

1. **§3e dit toujours que le panier meurt à la fermeture de l'onglet** — trouvé par les **cinq**
   chercheurs. Et `apps/web/lib/cart/CartContext.tsx` cite nommément §3e comme son autorité.
   §3e passe à « à rouvrir » dans le sommaire, l'écart est consigné, **la section n'est pas
   réécrite** : c'est une unité de validation de Jérôme.
2. **§8 rouvrait le placement des suggestions** que §2b.5 venait de fermer.
3. **§4, §8, cahier 03 §3g et cahier 00** portaient encore la modification partielle comme un geste
   client.
4. **§2c affirmait que le front d'auth d'`apps/web` reste à construire** — faux : `/login`,
   `/signup`, `/verify-email` existent depuis la feature 32 ; ce qui manque, c'est Google et le mot
   de passe oublié.
5. **Cahier 00** affirme qu'aucune coordonnée `lat`/`lon` n'existe — démenti par ses **propres
   tableaux d'audit**, qui les portent en « ✅ livré ».

### Cinq faits vérifiés à la main, hors audit, dont une correction due à Jérôme

En parallèle, vérification directe des points les plus lourds :

- ⚠️ **La justification du retrait du WhatsApp était fausse, et elle avait servi à décider.** J'avais
  écrit « `SiteFooter` porte déjà un contact sur toutes les pages » — il n'est monté **nulle part**,
  et l'architecture de coquilles le placera dans la zone vitrine, **pas dans le tunnel** où vit
  l'écran de confirmation. Signalé à Jérôme, qui a **maintenu le retrait** ; le cahier porte
  désormais la vraie justification (l'email Resend de la spec 23) **et dit explicitement que la
  première était fausse**.
- **`orders_select` exclut l'invité de toute lecture** (« il n'a justement aucune session pour
  prouver la propriété de sa commande ») — donc l'écran de résultat adressable, conçu d'abord pour
  lui, exige un jeton ou une lecture `service_role`.
- **`robots.ts` ne bloque que `/{locale}/r/`, `/auth/` et `/api/`** — l'adresse de confirmation,
  qui porte nom, téléphone et email, doit y être ajoutée.
- **`partners.phone` n'est pas le téléphone que le partenaire remplit** : celui-là vit dans
  `partner_accounts.phone`, et la migration `20260819100000` dit pourquoi. La décision « contact
  public » est maintenue, mais la colonne redevient à trancher.
- **« Portée au backlog » était faux** pour la modification partielle. Ligne ajoutée.

### Les 14 manques : le vrai apport de l'audit

Ce ne sont pas des erreurs, ce sont **des règles trop vagues pour devenir une assertion de test** —
exactement ce que `avant-la-spec.md` demande de trancher avant d'écrire. Six bloquaient l'écriture
de la spec 27. Tous ont été soumis à Jérôme et tranchés dans la foulée :

- **Réordonnancement des sections** : les types **absents** du panier passent devant, les présents
  tombent à la fin. Choisi contre « suivre le dernier ajout » parce que l'ordre ne saute pas.
- **Filtre « personas » = capacité déclarée**, jamais les places restantes. ⚠️ **Conséquence la plus
  structurante de la session** : la recherche ne consulte aucune disponibilité, donc elle
  **n'interroge jamais LobbyPMS** et fonctionne sans dates. `buscar()` reste une requête SQL pure.
- **Filtre « dates » = chevauchement**, jamais inclusion : un camp du 10 au 15 sort pour une
  recherche du 12 au 13, il peut déborder avant et après. Un produit sans date requise reste
  visible.
- **Le prix ne bouge pas** — et de toute façon `create_order` calcule tout côté serveur, le
  navigateur n'envoyant aucun montant : un panier ancien ne peut pas faire payer un ancien prix.
- ⚠️ **Sixième renversement : le compteur de places restantes est retiré.** « Le client voit en
  temps réel les places restantes quand elles se raréfient » venait du portail actuel et de la
  version validée du 2026-08-11. Aucun seuil, aucun affichage. L'anti-survente reste entière.
- **La fiche vitrine n'était pas un manque, c'était un défaut de lecture.** `external_booking_url`
  existe depuis le 2026-08-13 avec le commentaire « générique, plus jamais figé WhatsApp seul ».
  Une vitrine, ce n'est **jamais** `sellable = false` (qui la rend invisible) : c'est un produit
  publié portant cette URL. Le front branche aujourd'hui sur `isEvento` — c'est ça qu'il faut
  corriger, pas ajouter une colonne.
- **Écran de résultat** : code secret dans l'adresse, **sans limite de temps**, envoyé aussi par
  email. Tradeoff assumé : qui détient le lien voit les coordonnées du client.
- **Rattachement d'une commande invitée : par adresse email** — sûr parce que l'email est vérifié à
  l'inscription. RPC à écrire.
- **Où vivent les critères de recherche** : dans l'URL des pages **qui affichent des résultats**
  seulement ; en mémoire du navigateur ailleurs. ⚠️ **Ceci résout la tension signalée le matin** :
  les fiches gardent une adresse propre et **restent cacheables**, seules les listes sont dynamiques.
- **Prix d'une carte d'établissement groupée** : une **colonne « prix minimum »** recalculée à
  chaque ajout/édition/suppression d'un produit, pour afficher « desde X ». ⚠️ Dénormalisation —
  à maintenir **dans la même transaction**, sinon elle dérive en silence.
- **Page `actividades`** : seuls les tags portant au moins une offre publiée (sinon page vide
  indexée). Plafond de 8 valable aussi sous recherche, section à 0 masquée, aucun ordre particulier.
- **Connexion en cours de checkout** : les informations du compte font foi et remplacent la saisie.

### Deux chantiers backend nés de cette session, à chiffrer

La **RPC de rattachement par email**, et la **colonne de prix minimum** avec sa maintenance
transactionnelle. Ni l'une ni l'autre n'existait avant aujourd'hui.

### Vérifié

`npm run docs:index` puis `npm run docs:check` verts après chaque lot. Cinq fichiers touchés :
les cahiers 00, 01 et 03, le backlog, l'index. §3e et §4 changent de statut dans le sommaire ;
aucune section validée n'a été réécrite — les écarts vivent dans les en-têtes prévus pour ça.

### Non fait, assumé

- **§2 toujours en relecture** : Jérôme n'a pas encore refermé la section.
- **Le commentaire de `CartContext.tsx` cite une règle en cours de réouverture.** Il n'est pas
  encore faux (§3e reste la version validée) ; il le deviendra à la seconde où §2b.6 sera revalidé,
  et doit être corrigé dans le même geste.
- **Six points restent ouverts au §2f**, dont la recherche géographique différée et la colonne
  exacte du contact d'établissement.

## 2026-09-07 (suite) — Spec 27 : le terrain du front, et une décision qui n'est apparue qu'en l'écrivant

Troisième lot de la journée, enchaîné après le commit de l'audit. La spec 27
(`docs/specs/27-architecture-vitrine-et-routage.md`, 428 lignes) traduit en plan technique le §2 du
cahier client réécrit le matin. Elle ne construit **aucun écran** — c'est son périmètre : la carte
des routes en espagnol, les quatre zones et leurs coquilles, la garde d'accès, la couche
`lib/catalog/`, la stratégie de rendu, le plan d'extraction de la logique de réservation, et les
cinq contrôles CI.

### Ce que l'écriture a fait apparaître, et qu'aucune interview n'avait vu

**Le regroupement des établissements doit se faire en SQL, pas en TypeScript.** Il tombe de la
combinaison de deux décisions prises séparément dans la journée — « huit offres par section » et
« un établissement apparaît comme une seule carte dès deux couchages ». Le catalogue actuel
(`app/[locale]/page.tsx`) groupe **après** la requête ; plafonner à huit *produits* puis grouper
donne moins de huit *cartes*. Le plafond porte sur ce que le client voit, donc le regroupement doit
le précéder — d'où la colonne `es_establecimiento` dans la fonction `search_catalog`. Personne ne
l'avait vu pendant l'interview : c'est l'écriture du contrat qui l'a révélé.

### Deux choix techniques tranchés dans la spec, avec leur raison

- **`security invoker`, jamais `security definer`.** `search_catalog` ne lit que du public
  (`products_select_public` exige `sellable`, `establishments_select_public` exige
  `status = 'active'`) : en invoker, ces policies s'appliquent d'elles-mêmes. Un `security definer`
  contournerait RLS **sans aucun besoin** — exactement ce que `CLAUDE.md` §3.5 interdit de présenter
  comme un filet de sécurité.
- **Pas de `next-intl pathnames`.** Les segments sont en espagnol dans les deux locales. Conséquence
  SEO écrite noir sur blanc : l'URL anglaise ne portera pas de mots-clés anglais. Réversible, au
  prix d'une table de correspondance et de 301.

Delta de base volontairement mince : deux extensions du tronc Postgres (`unaccent`, `pg_trgm`), une
fonction de recherche, des index trigrammes. **Aucune table, aucune écriture, aucune RPC
`security definer`** — cette spec ne touche à rien de capacitaire, donc rien ne relève de la
frontière RPC-only.

`products.external_booking_url` passe de « mort » à « à activer » : posé le 2026-08-13 avec le
commentaire « générique, plus jamais figé WhatsApp seul », jamais consommé, alors que le front
branche sur `isEvento`. C'est lui qui fait une fiche vitrine — **jamais** `sellable = false`, qui la
rendrait invisible.

### Point ouvert nouveau, pour Jérôme

`products_price_cop_required_unless_evento` n'exempte que les eventos : un **transport en vitrine**
devra porter un prix même s'il n'est pas vendu en ligne, ou la contrainte doit être relâchée. Sans
arbitrage, la spec garde la contrainte et le produit porte un prix indicatif.

### Aléa

Le champ `revise:` a d'abord été posé sur `docs/01-cahier-des-charges-client.md §2` — refusé par
`docs:check`, qui n'accepte qu'un chemin de cahier sans section. Il a été **retiré** plutôt que
corrigé : une spec qui *raffine* un cahier ne le *révise* pas, et le §2 ne dit rien que la spec 27
contredise. `revise:` est réservé aux contradictions.

### Vérifié

`npm run docs:index` puis `npm run docs:check` verts — **40 documents** (39 la veille), ligne
ajoutée au sommaire de `docs/specs/README.md`.

### Non fait, assumé

La spec est en **`brouillon`** : c'est Jérôme qui la fait passer en relecture. Aucun code écrit,
aucune migration créée — la spec précède l'implémentation, c'est tout l'objet du découpage retenu
le matin (« une spec d'archi, puis une spec par écran, écrites une à la fois »).

**Suite immédiate — le point ouvert de la spec 27 a été tranché le jour même.** Jérôme a choisi de
**relâcher la contrainte de prix** : `products_price_cop_required_unless_evento` devient
`…_unless_vitrine`, `check (type = 'evento' or external_booking_url is not null or price_cop is not
null)`. Un produit portant une URL de contact peut donc afficher `price_label` (« Consultar »,
« Desde $150.000 ») au lieu d'un montant chiffré — exactement ce que font déjà les eventos, avec une
colonne qui existait et n'était pas réservée à un type. Écarté : garder la contrainte et saisir un
prix indicatif, parce qu'annoncer un montant ferme sur une offre dont le tarif se négocie est un
écart qui se découvre au moment de payer. La nouvelle contrainte est **strictement plus permissive**
que l'ancienne : aucune donnée existante ne devient invalide. La spec 27 gagne une seconde migration
au delta (`<ts>_precio_opcional_en_vitrina.sql`) et n'a plus aucun point ouvert propre — les six
qui restent sont ceux, hérités, du §2f du cahier client.

## 2026-09-07 (fin de journée) — Les deux documents validés, et une convention de statut qui n'en était pas une

Jérôme valide **le §2 du cahier client** et **la spec 27**. Les deux passent en ✅ validé au
2026-09-07 : le §2 dans sa ligne de statut et dans la table du sommaire, la spec 27 dans ses treize
sections.

**Un contrôle a refusé la validation de la spec, et il avait raison.** Poser
`statut: validé par Jérôme le 2026-09-07` dans le frontmatter fait échouer `docs:check` :
« statut hors énumération (brouillon | partiel | implemente | supprimee) ». En creusant, la cause
n'est pas une faute de frappe mais **une ambiguïté du `README.md` des specs**, qui écrivait que le
statut `brouillon → en relecture → validé par Jérôme le AAAA-MM-JJ` est « porté à la fois par le
frontmatter et par une table interne ». C'est faux pour le frontmatter, et le contrôle le sait :

- le **frontmatter `statut`** décrit l'état d'**implémentation** — une spec validée mais pas encore
  construite reste `brouillon` ;
- l'état de **validation** vit uniquement dans la table « Sommaire et statut », section par section.

Le `README.md` des specs a été corrigé pour distinguer les deux axes, avec la consigne d'ajouter une
ligne sous le titre quand une spec est validée sans être construite — sinon un lecteur qui voit
`statut: brouillon` conclut qu'elle n'est pas arbitrée. C'est exactement la même famille de défaut
que ceux traqués toute la journée : une convention écrite qu'un contrôle contredit, et c'est le
contrôle qui fait foi.

**Ce que ça débloque.** Le §2 étant validé, `apps/web/lib/cart/CartContext.tsx` cite désormais une
règle (§3e du cahier client) **officiellement contredite** : le commentaire est passé de « pas encore
faux » à « faux ». Il devra être corrigé dans le geste qui rend le panier persistant. Et le §3e
lui-même, marqué « à rouvrir », attend son arbitrage avec la question des plafonds.

### Vérifié

`npm run docs:index`, `npm run docs:check` et `npm run check:instructions` verts — 40 documents.

### Non fait, assumé

Aucun code écrit. La prochaine étape est la spec 28 (l'accueil), qui pourra enfin s'appuyer sur deux
documents validés plutôt que sur un carnet de conversation.

## 2026-09-07 (suite) — Spec 28 : le premier écran, et deux endroits où la rédaction a dû trancher

Écrite dans la foulée de la validation du §2 et de la spec 27.
`docs/specs/28-vitrine-accueil-et-resultats.md`, 356 lignes : l'accueil de la vitrine, qui est aussi
l'écran de résultats de recherche — cinq sections, le bloc de recherche enfin branché, deux
variantes de carte, le contrat d'URL. Premier écran du chantier front.

**Découpage en deux tranches.** T1 = tout l'écran. T2 = les **suggestions** de la barre de
recherche. Justification : `SearchBar` accepte une liste vide (état prévu, `emptyLabel`) et `Entrée`
soumet toujours le texte tapé — c'est son contrat, écrit dans son propre code. La recherche est donc
complète sans suggestions ; les livrer dans le même lot le doublait pour un confort.

**Deux décisions signalées plutôt que glissées.**

*Le prix « desde » d'un établissement groupé : un agrégat SQL, pas une colonne dénormalisée — ce qui
écarte la forme proposée par Jérôme le jour même.* Il avait décrit « une variable qui, quand tu
ajoutes ou édites ou supprimes une chambre, met un prix mini ». Trois raisons de préférer l'agrégat,
écrites dans la spec : une dénormalisation non maintenue **dérive en silence** (aucune erreur, aucun
test rouge, un prix faux affiché pendant des mois) ; les écritures de produit passent par **trois
chemins** (RPC admin, modération d'une proposition socio, import LobbyPMS) et un seul oubli suffit ;
et à l'échelle du catalogue l'agrégat est indexable et coûte zéro. La spec porte la **condition de
bascule** : si la requête dépasse un budget mesuré (à fixer en spec 29, qui la fera tourner sur des
listes plus longues), on dénormalise — et la colonne devient alors un cache, écrit dans la **même
transaction**, avec le test qui le vérifie. **À confirmer par Jérôme**, puisque c'est sa forme qui
est écartée.

*Le `<h1>` est masqué visuellement, pas absent.* La maquette n'a rien au-dessus du bloc de
recherche, mais une page sans `h1` est une faute d'accessibilité comme de référencement. ⚠️ La
distinction qui rend ça légitime : masquer **visuellement** (classe de lecteur d'écran, texte présent
dans le DOM à toutes les largeurs) n'est pas le `hidden md:block` que la règle SEO interdit, lequel
sort le contenu de l'index **mobile**.

**Un point ouvert laissé à Jérôme.** Il a demandé un carrousel sur les cartes, puis une carte « plus
rectangulaire » en ligne pour les activités. Or le visuel de `Card layout="row"` fait **64 px** —
dimensionné pour la ligne produit d'une fiche établissement, trop petit pour un carrousel. Trois
issues écrites dans la spec ; recommandation : agrandir le visuel et garder le carrousel, seule
issue qui ne crée pas deux cartes divergentes. Décision visuelle, à voir dans Storybook.

**Audit lancé** sur la spec, même dispositif que pour le §2 : trois chercheurs (fidélité aux sources
amont · respect des règles du projet · faisabilité contre le code et la base réels), chaque
trouvaille passée devant un réfutateur, puis un critique de complétude qui se demande si un agent
peut coder cette spec sans revenir poser une question. Résultat non encore intégré à l'heure de
cette entrée.

### Vérifié

`npm run docs:index` puis `npm run docs:check` verts — **41 documents**, ligne ajoutée au sommaire
de `docs/specs/README.md`.

### Non fait, assumé

Rien n'est commité : l'audit tourne encore et peut modifier la spec. Aucun code écrit — la spec 27
(le terrain) n'est pas implémentée non plus, et elle doit l'être avant la 28.

## 2026-09-07 (suite) — L'audit de la spec 28, et la chaîne de décisions qu'il a déclenchée

Même dispositif que pour le §2 : trois chercheurs (fidélité aux sources amont · respect des règles
du projet · faisabilité contre le code et la base réels), chaque trouvaille passée devant un
réfutateur, puis un critique de complétude. **28 agents, zéro échec. 14 trouvailles confirmées sur
24 — 10 réfutées.** Verdict : **la spec 28 n'était pas codable en l'état.** Elle a été réécrite
intégralement (356 → 455 lignes).

### Les trois trouvailles qu'aucune relecture n'aurait donné

**Le réordonnancement des sections selon le panier était littéralement inécrivable.** Deux raisons
cumulées : `CartLine` **ne porte pas le type de l'offre** — rien dans le panier ne dit si une ligne
est une activité ou un transport — et le panier est un état **client** que `page.tsx`, Server
Component, ne peut pas lire. C'était la seule chose que la spec ajoutait au parcours après un ajout
au panier.

**`SearchPanel` ne pouvait pas être rendu par la page.** Toutes ses props sont des fonctions
(`onSubmit`, `onCriteriaChange`, `onSuggestionSelect`) : les passer depuis un Server Component lève
une erreur de sérialisation à l'exécution de la route. La spec ne créait **aucun** composant client
hôte — le développeur aurait dû inventer l'architecture de l'écran, ce que la spec existe pour
fixer. Ajouté : `BuscadorInicio`, qui porte aussi `aujourdIso` calculé dans le fuseau
`America/Bogota` (§11.20).

**Deux décisions du même jour se contredisaient.** La spec 27 a relâché la contrainte de prix le
matin (une offre en vitrine peut n'avoir aucun `price_cop`) ; la spec 28 calculait le prix « desde »
d'un hôtel par `min(price_cop)` l'après-midi. Sur des colonnes nulles, l'agrégat rend une valeur
fausse ou nulle. Chaque décision était bonne seule ; ensemble elles cassaient. Corrigé par un
`filter (where … price_cop is not null)` et la règle « aucun prix chiffré → aucun prix affiché,
jamais desde 0 ».

### Le reste, non moins réel

- **Le carrousel n'avait aucune source de données** : `search_catalog` ne rendait aucun média, et le
  texte alternatif n'a **aucune colonne** en base (`product_media` = `id, product_id, storage_path,
  sort, created_at`). Trois colonnes de retour ajoutées ; alt calculé.
- **`PhotoStrip` existe depuis le 4 septembre et a été bâtie pour ce cas exact** — la spec faisait
  réécrire un carrousel dans la carte. Rupture de la règle « un composant qui duplique quelque chose
  d'existant ». Corrigé.
- **Supprimer l'ancien accueil emportait son `generateMetadata` et son `JsonLd` WebSite**, que la
  spec décrivait comme « déjà en place ». Le canonical auto-référent de l'accueil — celui qui
  rassemble les variantes `?ref=` et désormais `?q=` — serait parti avec. À reprendre avant
  suppression.
- **Deux `<main>` imbriqués** : la spec 27 en posait un dans `(vitrine)/layout.tsx`, `PageShell` en
  pose un second et son en-tête le dit (« l'unique `<main>` d'une page »). **La spec 27 a été
  corrigée du même geste** : le layout ne pose plus que l'en-tête et le pied de page, et un
  onzième invariant a été ajouté.
- `leerCriterios` prenait `URLSearchParams`, ce que Next ne passe pas · le `q` cherchait « le
  libellé du type », qui n'existe dans aucune table · aucune table `tipo` → segment d'URL n'était
  donnée · aucune clé de traduction n'était nommée · l'ordre à l'intérieur d'une section n'était
  fixé nulle part, rendant le résultat non reproductible entre deux chargements.

### La chaîne de décisions déclenchée par une seule trouvaille

Le point le plus intéressant de la journée n'est pas une correction mais un enchaînement. Jérôme a
lu « le réordonnancement est infaisable » et a répondu **« on n'a pas designé la liste pour le
panier, il faudra le faire »**. De là, en trois échanges :

1. le panier persistant faisait **perdre l'attribution** d'un invité (cookie sans durée de vie,
   panier qui survit) → il fallait garder l'identifiant du référent quelque part ;
2. « dans l'utilisateur » — sauf qu'un invité **n'a pas d'utilisateur** → décision : lui en donner
   un, par **session anonyme Supabase**, ouverte **au premier geste engageant** (ajout au panier ou
   arrivée par `?ref=`), jamais à la simple visite ;
3. l'invité ayant désormais une identité, **le panier peut vivre en base** → décision prise, ce qui
   règle d'un coup la persistance, le fait que le serveur puisse enfin lire le panier (donc le
   réordonnancement redevient faisable), et le suivi entre appareils.

**Deux gains collatéraux**, dont un que j'avais d'abord surestimé et corrigé auprès de Jérôme : la
commande d'un invité devient lisible par son auteur (`orders_select` cesse de l'exclure) — mais
**seulement sur le même navigateur**, un lien ouvert depuis l'email sur un autre appareil réclamant
toujours le code secret ; et surtout, le **rattachement par email disparaît au profit d'une
conversion d'identité** : Supabase lie un email à l'utilisateur anonyme **sans changer son
identifiant**, donc les commandes suivent d'elles-mêmes, sans RPC de rapprochement et sans le risque
du « qui crée un compte avec cet email récupère ces commandes ».

⚠️ **Le coût, mesuré et écrit** : **79 fichiers de migration** accordent des droits à
`authenticated`, **67 policies** existent, et **`is_anonymous` n'apparaît nulle part** dans le
projet. Aujourd'hui `authenticated` signifie « personne réelle ayant vérifié son email » ; demain il
signifiera aussi « visiteur de passage ». Chaque droit et chaque policy doit être relu **avant** la
bascule. Le trigger `handle_new_auth_user` crée par ailleurs une ligne `partner_accounts` à chaque
nouvel utilisateur — à revoir.

### Trois arbitrages, tranchés le même jour

- **Texte alternatif** : `<nom de l'offre>, foto <i> de <n>`. Une colonne rédigée par le partenaire
  serait meilleure, mais elle exige migration, écrans de saisie, et surtout quelqu'un pour l'écrire
  sur chaque photo — elle resterait vide et il faudrait ce repli de toute façon.
- **Ordre dans une section** : `created_at desc`. Sans `order by`, la base rend ce qu'elle veut et
  le même visiteur qui recharge voit d'autres cartes.
- **`personas`** : une intention (« ça marche pour n personnes »), la colonne juste de chaque type —
  `capacity` pour un logement, `max_qty` pour tout le reste. Jamais `capacity` sur un non-logement,
  où elle désigne le cupo d'une date et non la taille d'un groupe.

### Conséquence sur le découpage

Le **réordonnancement sort de la Tranche 1** et devient la Tranche 3, derrière deux specs dont il
dépend désormais : **identité anonyme**, puis **panier en base**. L'accueil elle-même — sections,
recherche, cartes — n'en dépend pas et reste livrable. C'est plus honnête que la version du matin,
qui faisait dépendre la Tranche 1 d'un mécanisme dont on sait maintenant qu'il ne pouvait pas
fonctionner.

### Vérifié

`npm run docs:index` puis `npm run docs:check` verts — 41 documents. Le §3e du cahier a été réécrit
(plafonds globaux relevés 12/36/40, persistance, créneaux réels, promesse d'indépendance des nuits
périmée) et repasse en « à revalider ».

### Non fait, assumé

Aucun code écrit. Trois specs restent à écrire, dans un ordre imposé : identité anonyme → panier en
base → modèle de la ligne de panier. La carte d'activité en ligne garde un point ouvert visuel (son
visuel de 64 px est trop petit pour un carrousel).

**§3e validé par Jérôme le 2026-09-07**, dans la foulée de sa réécriture. Le cahier client n'a donc
plus aucune section en attente : §2 et §3e sont validés le même jour, §3a à §3d et §3f à §7 restent
sur leurs validations d'août. Deux conséquences deviennent effectives à cet instant :
`apps/web/lib/cart/CartContext.tsx` cite en commentaire une règle (« perdu si l'onglet est fermé »)
désormais **officiellement contredite** — le commentaire est passé de « pas encore faux » à
« faux », et devra être corrigé dans le geste qui rendra le panier persistant ; et les plafonds
codés en dur dans `create_order` (`v_lodging_lines > 4`, `v_lodging_units > 12`,
`v_prestation_lines > 20`) sont désormais en écart avec le cahier, qui dit 12/36/40 — une migration
les attend.

**Règle posée dans la foulée — les composants manquants se nomment dans la spec, jamais en codant.**
Demande de Jérôme : « si il manque des items dans le design system il faudra les créer pendant ou à
part de la spec ». Écrite dans `apps/web/components/README.md`, parce que c'est là qu'on la lira.
Deux cas seulement : **dans le même lot** quand le composant ne sert que cet écran et ne demande
aucune décision visuelle ; **dans un lot à part** quand il servira plusieurs écrans, qu'il **modifie
un composant existant**, ou qu'il demande un arbitrage. Jamais un troisième cas : un composant n'est
jamais écrit à l'intérieur d'un `page.tsx`, même provisoirement — la règle du barrel RSC l'interdit
de fait, et rien ne le rattraperait ensuite.

Le mode d'échec que ça évite vient d'être vécu : `PhotoStrip` **existait** et la spec 28 faisait
réécrire un carrousel à sa place. Un composant découvert manquant *pendant* le codage finit
improvisé dans le fichier de la route, sans test ni story, invisible au prochain écran — et on se
retrouve avec deux composants qui font la même chose.

Vérification faite dans la foulée sur l'accueil : **`EstadoVacio` n'existe nulle part**, alors que la
spec exigeait un « état vide explicite » sans nommer de composant. Les stories `Vide` du dépôt ne
sont que des champs de filtre non remplis, pas un état de page. La spec 28 porte désormais une
section « Ce que le design system n'a pas encore » : `SeccionOfertas`, `TarjetaOferta` et
`EstadoVacio` naissent **dans le lot** ; le visuel plus grand de `Card layout="row"` part en **lot à
part**, puisqu'il modifie un atome que d'autres écrans utilisent déjà.

## 2026-09-07 (fin) — La base est verte, et trois des cinq contrôles seraient nés avec des faux positifs

Avant d'écrire la moindre ligne de code, deux vérifications que rien n'obligeait à faire et qui ont
toutes deux rapporté.

### La base est verte — l'inconnue du 4 septembre est fermée

L'entrée du 2026-09-02→04 se terminait par : « **Non vérifié depuis (rattrapage seul, pas une
revue) : build/tests/lint de l'état final de ces 6 commits.** À faire avant de s'appuyer dessus pour
la suite si un doute apparaît. » Le doute apparaissait — on allait coder par-dessus. Lancé :

```
typecheck   ✓  les deux apps
lint        ✓  les deux apps
tests       ✓  693 (427 apps/web + 266 packages/domain), 74 fichiers
build       ✓  18 routes, aucune erreur
```

On part donc d'un état sain, et on saura distinguer ce qu'on casse de ce qui l'était déjà. Le build
confirme au passage le constat de la spec 27 : **tout est dynamique** (`ƒ`) sauf `robots.txt`.

### Les contrôles CI mesurés à la main, et ce que ça a évité

Les cinq contrôles de la spec 27 ont été exécutés **à la main** sur l'état actuel, pour mesurer
l'écart avant de les écrire. Résultat brut : quatre contrôles sur cinq semblaient échouer. Résultat
réel après vérification : **un seul échoue**.

- ⚠️ **`@hifago/ui` dans un `page.tsx` — deux fichiers signalés, zéro violation.** Les deux
  occurrences sont des **commentaires** qui citent la règle pour expliquer pourquoi le fichier ne
  l'importe pas. Le build vert était le bon signal : si c'étaient de vrais imports, il aurait cassé.
- ⚠️ **`next/link` — deux occurrences, zéro violation.** Ce sont les commentaires de `LinkButton` et
  `BackLink` qui expliquent quoi ne pas faire.
- ⚠️ **Huit couleurs en dur, zéro violation.** Ce sont les **drapeaux SVG** de `LanguageSwitcher`
  (colombien, britannique). Un drapeau n'a pas de jeton sémantique : sa couleur *est* sa définition.
  Un contrôle qui les signale est un contrôle qu'on désactive au bout d'une semaine.
- ✔️ **Aucune requête Supabase dans un `page.tsx` — cinq violations réelles**, et ce sont exactement
  les cinq écrans que les specs 28 et suivantes remplacent. L'écart se referme avec la migration.

Deux règles d'écriture en découlent, consignées dans la spec 27 : **ne matcher que la ligne
`import`, jamais le texte libre**, et **exempter les couleurs qui ne sont pas des couleurs de
thème** (attributs `fill`/`stroke` d'un SVG). Sans elles, trois contrôles sur cinq naissaient
bruyants — et un contrôle qui crie à tort ne protège plus rien.

C'est une illustration directe de §11.20 : une règle vérifiée mécaniquement ne vaut que si le
mécanisme est juste. Mesurer avant d'écrire coûte dix minutes et évite d'écrire un garde-fou faux.

### Ordre de construction proposé pour la spec 27

**Lot A** — `lib/catalog/` et la migration `search_catalog`, **d'un coup avec les colonnes de la
spec 28** (photos, `precio_desde`, `price_label`, total de section) plutôt qu'en deux migrations :
la 28 est écrite et auditée, on sait ce qu'elle demande. Purement additif, rien ne casse.
**Lot B** — les quatre coquilles, le passage des routes à l'espagnol, l'**extraction** de la logique
de réservation, puis la suppression des anciens écrans. Lot structurel, à suivre de près par la
spec 28 sinon le site n'a plus de pages.
**Lot C** — les cinq contrôles CI, en dernier, quand le code les satisfait ; les poser avant, c'est
les poser rouges.

### Non fait, assumé

Toujours aucune ligne de code. La spec 28 reste en brouillon — l'audit l'a corrigée, Jérôme ne l'a
pas encore relue.

**Spec 28 validée par Jérôme le 2026-09-07**, après sa réécriture consécutive à l'audit. Les deux
specs du chantier front sont donc validées le même jour, et le cahier client n'a plus aucune section
en attente. Rien ne bloque plus l'écriture du code — la première ligne du chantier sera le **Lot A**
de la spec 27 (`lib/catalog/` et la migration `search_catalog`), construit d'un coup avec les
colonnes que la spec 28 exige, plutôt qu'en deux migrations successives.

## 2026-09-07 (soir) — Première ligne de code du chantier front : `search_catalog`

Lot A de la spec 27, moitié SQL. `supabase/migrations/20260907200000_search_catalog.sql`, 287
lignes dont une bonne moitié de commentaires — les décisions de la journée doivent survivre au
fichier, pas seulement aux specs.

**Rectification d'un chiffre annoncé plus haut** : la base fait **831 tests** (138 `apps/admin` +
427 `apps/web` + 266 `packages/domain`), pas 693. L'entrée précédente n'avait lu que la fin de
sortie et manqué la suite d'`apps/admin`. Aucune décision n'en dépendait.

### Ce que la fonction fait, et ce qu'elle ne fait pas

`security invoker`, jamais `definer` : elle ne lit que du public, donc les policies `_select_public`
s'appliquent d'elles-mêmes. `search_path = ''` posé quand même, tout est qualifié. Aucune écriture,
aucune table capacitaire — rien ici ne relève de la frontière RPC-only. `grant execute` à `anon` et
`authenticated` : un visiteur non connecté doit pouvoir chercher, c'est l'objet même de la vitrine.

Elle **ne lit aucun compteur de disponibilité**, et c'est ce qui la garde en SQL pur : `p_personas`
compare des capacités déclarées, `p_desde`/`p_hasta` ne consultent que `product_calendar`. La
recherche n'appelle donc jamais LobbyPMS.

### Quatre choses trouvées en écrivant, qu'aucune spec n'avait vues

**`product_calendar` est CREUX.** L'absence de ligne ne veut pas dire « fermé » mais « le défaut du
produit » — `products.calendar_default_open`, `true` par défaut. D'où deux branches : quand le
défaut est ouvert, l'offre ne disparaît que si **toutes** les dates de la plage sont explicitement
fermées (un `count(*) < (p_hasta - p_desde + 1)`, sans `generate_series`) ; quand il est fermé, il
faut une ouverture explicite.

**`products.sort` existe et est dormante** — `int` nullable, créée le 13 août avec la table, jamais
renseignée (absente du seed), jamais lue par une ligne d'application. La décision `created_at desc`
tient donc, et le jour où l'ordre manuel sera voulu, la colonne est déjà là.

**Pas d'index trigramme, et c'est délibéré.** La spec 27 le prévoyait. `unaccent()` est déclarée
STABLE, pas IMMUTABLE : Postgres refuse de l'indexer dans une expression sans une fonction enveloppe
marquée IMMUTABLE à la main — un mensonge au planificateur. À l'échelle du catalogue le parcours
séquentiel est gratuit ; l'index viendra avec sa mesure, et il exigera une colonne générée.

**Une incohérence que j'avais introduite, trouvée par le test.** `personas=1` rendait 17 offres
là où le catalogue en compte 19 : j'avais écrit `coalesce(capacity, 0) >= n` pour un logement, donc
une capacité **absente** valait zéro et faisait disparaître l'offre — alors que pour les autres
types une borne absente signifiait « pas de limite, donc visible ». Deux logements sont concernés,
dont **la chambre PMS de Casa Kayam**, que Jérôme a justement décidé de toujours faire apparaître.
Corrigé en `capacity is null or capacity >= n` : une borne absente ne filtre pas, dans les deux
branches.

### Un écart à la spec, signalé plutôt que glissé

Les photos d'une carte d'établissement groupée : la spec dit « celles du premier couchage », repris
du catalogue actuel. La fonction prend **celles de l'établissement d'abord** (`establishment_media`,
spec 04), le premier couchage en repli. Raison : le catalogue actuel prend celles du couchage parce
qu'il listait des produits et n'avait pas d'autre source ; une carte qui représente un LIEU doit
montrer le lieu quand il a ses propres photos. Le repli garde le comportement actuel, donc rien ne
régresse. **À valider par Jérôme.**

### Vérifié, et comment

- **Le regroupement d'établissement n'est pas couvert par le seed** : aucun établissement n'a deux
  couchages vendables (Casa Kayam n'en a qu'un en local). Prouvé dans une **transaction annulée** —
  second couchage inséré, Casa Kayam devient bien **une seule carte**, `precio_desde` prend le
  minimum (60 000), et `total_seccion` compte **6 cartes et non 7 produits**, ce qui était tout
  l'enjeu du regroupement-avant-plafonnement. `rollback`, zéro ligne restante.
- **`unaccent` dans les deux sens** : « guatape » et « Guatapé » rendent les mêmes 7 offres ;
  « KAYAK » en majuscules trouve son offre.
- **Recherche par nom d'établissement** : « casa kayam » remonte bien ses offres.
- **Plafond et total** : 8 cartes affichées sur 9 activités, `total_seccion = 9` — le « Ver más »
  aura un chiffre juste.
- **Droits** : `has_function_privilege` confirme `anon` **et** `authenticated`.
- Lint propre, **831 tests verts**, build vert sur les deux apps, types régénérés (la RPC apparaît
  typée avec ses neuf paramètres nommés).

### Un piège d'outillage à ne pas redécouvrir

⚠️ **Éditer un fichier de migration APRÈS `supabase migration up` laisse le registre en désaccord
avec le disque.** `supabase_migrations.schema_migrations` mémorise les instructions **au moment de
l'application** : après correction, la base avait mémorisé 13 442 caractères pour un fichier qui en
faisait 15 546. La fonction en base était bonne (rejouée à la main), mais le registre mentait — et
un `db reset` ultérieur aurait appliqué autre chose que ce qui avait été testé.
Réparé **sans effacer la base** : la migration ne contient que de l'idempotent (`create or replace`,
`comment on`, `grant`), donc retirer la ligne du registre et relancer `migration up` la ré-enregistre
avec le bon contenu. ⚠️ `npm run db:setup` n'a **pas** été lancé : son propre en-tête prévient que
`db reset` efface une base **partagée par toutes les sessions ouvertes sur le dépôt** — ça demande
l'accord de Jérôme, pas une initiative.

### Non fait, assumé

La moitié TypeScript du Lot A — `lib/catalog/` et ses fonctions `buscarSecciones`, `buscarTipo`,
`listarTagsConOferta`, `getProductoPorSlug`, `getEstablecimientoPorSlug`, `listarRutasIndexables` —
reste à écrire. Aucun test automatisé de `search_catalog` : les vérifications ci-dessus ont été
faites à la main. Un test pgTAP ou un test d'intégration devra les figer, sans quoi le
regroupement-avant-plafonnement n'est protégé par rien.

## 2026-09-07 (soir, suite) — La couche `lib/catalog/`, et le lint qui m'attrape

Moitié TypeScript du Lot A. Quatre fichiers, 30 tests : `tipos.ts` (types partagés, zéro
dépendance), `segmentos.ts` (la table type ↔ segment d'URL, seule source), `criterios.ts` (lecture
et écriture des paramètres d'URL) et `buscar.ts` (`buscarSecciones`, `buscarTipo`).

**Périmètre volontairement réduit.** La spec 27 liste aussi `getProductoPorSlug`,
`getEstablecimientoPorSlug`, `listarTagsConOferta` et `listarRutasIndexables`. Aucune n'a de
consommateur avant les specs 29 et 30 : les écrire maintenant serait construire à l'aveugle ce que
leurs écrans redéfiniront. Elles viendront avec eux.

**Le lint m'a attrapé, et il avait raison.** `criterios.ts` validait une date en faisant un
aller-retour `new Date(...).toISOString().slice(0, 10)` — motif interdit sans exception par
`no-restricted-syntax` (règle de fuseau du 2026-08-28). Mon commentaire prétendait même que
« ce n'est qu'une validation de forme, pas une date métier ». C'est précisément l'argument que la
règle refuse : un motif toléré « juste ici » se propage. Réécrit en comparant les composantes une à
une (`Date.UTC` puis `getUTCFullYear/Month/Date`), qui ne construit aucune date civile. C'est la
deuxième fois de la journée qu'un garde-fou mécanique rapporte — après les faux positifs mesurés
avant d'écrire les contrôles.

**Un écart à la spec 28, corrigé dans la spec plutôt que subi.** La spec faisait rendre le texte
alternatif des photos par `lib/catalog/`. Un `alt` est du texte d'INTERFACE : l'y mettre ferait
entrer next-intl dans la couche de données et la rendrait intestable sans contexte i18n. Il est
construit par le composant qui affiche la photo, qui reçoit déjà `nombre` et connaît le rang. La
spec a été mise en accord.

⚠️ **`server-only` n'existe pas dans ce dépôt** — ni utilisé, ni installé, ni déclaré (vérifié).
La spec 27 le prévoyait en tête de la couche. Ajouter une dépendance ne se fait pas au détour d'un
lot : l'import est retiré et le manque est **écrit dans le fichier**. Ce qui tient la frontière en
attendant : le module n'est importé que par des Server Components, et `check-data-layer.sh`
interdira à un `page.tsx` de parler à Supabase. Ce qui n'est **pas** couvert : un composant client
qui importerait la couche embarquerait la clé anonyme et le graphe Supabase dans le bundle, sans
erreur. **Décision d'une ligne pour Jérôme** : ajouter le paquet officiel (sans code) ou non.

**Ce que les tests figent, et qui aurait cassé en silence.** Le plafond par section est appliqué par
la BASE, mais la limite globale doit couvrir toutes les sections plafonnées : sans elle, la dernière
section serait tronquée sans aucune erreur — la page rendrait simplement moins de cartes. Un test
vérifie `p_limite = porSeccion × 5`. Sont figés aussi : une carte d'établissement sans aucun prix
chiffré n'affiche **pas** « desde 0 » (conséquence directe du relâchement de la contrainte de prix
le matin même), l'ordre des sections, le retrait des sections vides, l'échec franc sur erreur, et
l'abandon silencieux d'une ligne au type inconnu.

### Vérifié

Typecheck, lint propre, **861 tests** (138 admin + 457 web + 266 domain, soit +30), build vert sur
les deux apps.

### Non fait, assumé

Aucun test automatisé ne couvre encore `search_catalog` **en base** : les 30 tests de `buscar.ts`
mockent le client, donc ils vérifient la mise en forme, jamais le SQL. Le regroupement
d'établissement, le `unaccent` et le prédicat `personas` restent tenus par des vérifications
manuelles. C'est la dette la plus visible de ce lot.

## 2026-09-07 (soir, fin) — Le test pgTAP de `search_catalog`, et la suite qui était déjà rouge

**16 assertions pgTAP** sur `search_catalog` (`supabase/tests/database/search_catalog.test.sql`),
toutes en rôle `anon` — le rôle réel d'un visiteur, ce qui vérifie du même geste que les policies
`_select_public` laissent passer ce qu'il faut. Ça ferme la dette nommée dans l'entrée précédente :
les 30 tests Vitest de `buscar.ts` mockent le client et ne prouvent rien du SQL.

Ce qui est désormais figé : un produit non vendable ou d'un établissement archivé n'apparaît jamais ·
un établissement à **deux** couchages vendables sort comme UNE carte et ses couchages ne sortent pas
en plus · un établissement à **un** couchage n'est pas groupé · le « desde » prend le minimum des
couchages **vendables** (un couchage non vendable à 10 COP ne le fausse pas) · `total_seccion`
compte des cartes et non des produits · `unaccent` dans les deux sens et la casse ignorée ·
chercher un nom d'établissement remonte ses offres · `personas` avec la colonne juste de chaque
type, et une borne absente qui ne filtre pas · le calendrier creux (fermé sur toute la plage →
absent ; aucune ligne → présent par défaut).

⚠️ **Chaque assertion est scopée sur ses propres fixtures**, par identifiants fixes, jamais sur un
comptage global — précisément pour ne pas rejoindre les fichiers déjà fragiles.

### La correction que ce lot impose à une entrée précédente

⚠️ **J'ai écrit ce matin « la base est entièrement verte ». C'était incomplet** : j'avais lancé
typecheck, lint, Vitest et build — **jamais `npm run test:db`**. Lancée maintenant, la suite pgTAP
est **ROUGE**, et l'était avant tout ce qui a été fait aujourd'hui.

**Quatre fichiers, sept assertions** : `admin_audit_log` (3), `catalog_rls` (2),
`product_availability_rpc` (1), `set_product_availability_socio` (1). Vérifié empiriquement et pas
déduit : **la suite a été relancée sans mon fichier, et exactement les mêmes quatre échouent**. Le
message le plus parlant est `have: 99, want: 0` sur un comptage d'`audit_log` — c'est le mode
d'échec déjà décrit au backlog (« 6 fichiers pgTAP sensibles au volume de données locales
accumulées, `audit_log` non scopé »).

**Ce que ça veut dire, et c'est le point** : la suite pgTAP ne donne **aucun signal** sur cette
machine. Un test de base qui échoue toujours ne protège plus rien — même famille de défaut que les
contrôles CI qui seraient nés avec des faux positifs. Porté au backlog.

**La réparation est connue** : `npm run db:setup` remet la base à plat. ⚠️ Son en-tête prévient
qu'il **efface une base partagée par toutes les sessions ouvertes sur le dépôt** — ça demande
l'accord de Jérôme, pas une initiative. C'est maintenant la deuxième raison concrète de le lancer.

### Vérifié

`search_catalog.test.sql ... ok` — 16 assertions. Les 45 autres fichiers pgTAP inchangés.

## 2026-09-07 (nuit) — Base locale remise à plat : la suite pgTAP repasse au vert, et le seed révèle un trou

Jérôme : « tu peux faire ce que tu veux sur celle en local ». `npm run db:setup` lancé — les trois
étapes que son en-tête impose et qui ne sont écrites qu'à cet endroit : `db reset --no-seed`, puis
les comptes auth par l'API Admin (un `INSERT` direct dans `auth.users` est refusé sur Supabase
Cloud), puis `seed.sql`.

### Trois choses prouvées d'un coup

1. **La migration `search_catalog` s'applique depuis zéro.** `db reset` rejoue toutes les migrations
   depuis le fichier : elle n'a pas seulement marché parce qu'on l'avait appliquée à la main.
2. **La suite pgTAP repasse au vert : 49 fichiers, 846 tests, PASS.** Les 7 assertions rouges
   venaient donc **uniquement** des données locales accumulées, comme l'analyse le supposait — et
   comme la relance sans mon fichier l'avait déjà indiqué. Le point ouvert le matin même est
   refermé et **retiré du backlog** (règle : une ligne retirée = un point refermé, vérifié).
3. **Tout le reste tient sur une base propre** : 0 erreur TypeScript, 0 erreur de lint, 861 tests
   Vitest, les deux apps buildées.

### Ce que la base propre a révélé, et qui était masqué par le bruit

Le seed réel est **beaucoup plus petit** que ce que la base accumulée laissait croire : **2 activités
et 1 logement** au catalogue public, contre 19 offres avant remise à plat. Tout le reste était du
résidu d'e2e.

⚠️ **Et Casa Kayam n'a qu'un seul couchage vendable.** Donc `supabase/seed.sql` **ne couvre pas la
règle de regroupement d'établissement**, qui est pourtant une règle centrale de l'accueil. La
conséquence n'est pas théorique : un e2e Playwright de l'accueil ne verrait **jamais** de carte
groupée, et passerait au vert en ne testant rien de ce mécanisme.

La logique, elle, reste protégée — `search_catalog.test.sql` la couvre avec ses **propres**
fixtures, précisément parce qu'il ne compte jamais le catalogue global. C'est ce choix de scoping
qui fait qu'il est vert sur base sale comme sur base propre.

Point porté au backlog, et **au périmètre du lot de la spec 28** : ajouter un second couchage
vendable à un établissement existant du seed, sans quoi l'e2e de l'accueil sera un test qui rassure
sans vérifier.

### Vérifié

`npx supabase test db` → **49 fichiers, 846 tests, PASS**. `npm run typecheck` 0 erreur,
`npm run lint` 0 erreur, `npm run test` 861 tests, `npm run build` deux apps compilées.

## 2026-09-07 (nuit) — Lot B1 : la vitrine a enfin une coquille, et les e2e sont des bombes à retardement

Le Lot B de la spec 27 tel qu'écrit casse le site en son milieu : renommer les routes et supprimer
les écrans laisse un site sans pages jusqu'à ce que la spec 28 arrive. Découpé en trois, et **B1
livré ici** — parce qu'un groupe de routes `(…)` **ne change aucune URL** : on peut donc créer les
quatre zones et y déplacer les écrans **sans rien renommer**, et tout continue de marcher.

**Livré** : les quatre coquilles (`(vitrine)`, `(tunnel)`, `(cuenta)`, `(auth)`), la 404 localisée
(`apps/web` n'en avait AUCUNE), le namespace `NotFound` dans les deux locales, et le retrait des
cinq liens morts du footer.

**Vérifié par le build, pas déduit** : la table des routes est **identique** avant/après —
`/[locale]`, `/[locale]/account/orders`, `/[locale]/checkout`, `/[locale]/establishments/[slug]`,
`/[locale]/login`, `/[locale]/products/[slug]`, `/[locale]/r/[code]`, `/[locale]/signup`,
`/[locale]/verify-email`.

### Trois choses que la vérification en navigateur a corrigées, dont deux de mes propres conclusions

**1. J'ai d'abord testé un build périmé.** Le serveur tournait sur un `next build` antérieur au
déplacement du fichier de 404. Ma première conclusion — « la 404 ne rend pas » — portait sur du code
qui n'était pas celui que je venais d'écrire.

**2. J'ai failli rapporter un faux positif.** Après rebuild, `curl` trouvait bien « Esta página no
existe » dans le HTML… mais c'était **la table des traductions** que next-intl embarque dans la
page, pas le contenu rendu. Il a fallu chercher l'élément React (`["$","h1",…]`) pour distinguer la
donnée du rendu.

**3. La coquille est bien rendue CÔTÉ SERVEUR, et ça compte pour le SEO.** `CoquillaVitrine` porte
`"use client"` — j'ai craint que l'en-tête et le pied de page n'existent qu'après hydratation, ce
qui aurait rendu invisibles à Googlebot les liens `/en/…` du sélecteur de langue, dont `SiteHeader`
documente lui-même qu'ils sont « ce qui fait découvrir la version anglaise par le maillage
interne ». Mesuré sur l'accueil servie : `<header>` ×1, `<footer>` ×1, `<nav>` ×2, `href="/en"` ×2.
Un composant client est bien rendu au serveur ; la crainte était infondée.

### Une décision d'architecture prise en écrivant, et elle décide de la vitesse du site

⚠️ **L'état de connexion est résolu CÔTÉ CLIENT, pas dans le layout serveur.** Si
`(vitrine)/layout.tsx` lisait la session pour alimenter `SiteHeader`, il appellerait `cookies()` —
ce qui rend **toute la zone dynamique**, y compris les fiches produit et établissement que la
spec 27 veut cacheables. `CoquillaVitrine` résout donc l'état après hydratation, avec un abonnement
`onAuthStateChange` (sans lui, se connecter dans un autre onglet laisserait l'en-tête mentir).
Prix assumé : au premier rendu l'en-tête affiche « Iniciar sesión » puis bascule. C'est le bon prix —
la valeur d'une page de catalogue ne dépend pas de qui la regarde.
`SiteHeader` n'est **pas** modifié : il garde sa prop `isAuthenticated`, on l'alimente autrement.

### Ce que la doc de Next 16 dit, et qu'il fallait lire

Une URL **entièrement inconnue** (`/es/nimportequoi`) ne rend PAS la 404 localisée : elle est
traitée à la racine, avant d'entrer dans `[locale]`. La doc embarquée le documente exactement, et
nomme notre cas : *« `global-not-found.js` est utile quand […] votre layout racine est défini avec
un segment dynamique de premier niveau (ex. `app/[country]/layout.tsx`) »* — c'est littéralement
`apps/web`, dont le layout racine **est** `app/[locale]/layout.tsx`. `not-found.tsx` couvre les
`notFound()` d'un segment (slug inconnu, locale inconnue), ce qui est le cas fréquent ;
`global-not-found.js` (expérimental, drapeau à activer) couvrirait le reste. Non fait, signalé.

### Les tests du footer, réécrits plutôt que supprimés

Retirer les cinq liens a cassé quatre tests qui les exigeaient. Ils sont **réécrits, pas
supprimés** : l'un vérifie qu'aucun lien mort n'est servi, un autre verrouille la **convention** que
le futur lot des pages institutionnelles devra respecter (cinq clés, liens localisés). Et le test de
cible tactile a changé de nature : il mesurait `min-h-11` sur les liens retirés ; le seul lien encore
servi est le bouton WhatsApp, qui ne porte **aucune** classe de hauteur — sa cible vient de la
variante `button--lg` du design system. Vérifier une classe Tailwind aurait été vérifier une
implémentation que ce composant n'a pas.

### ⚠️ Les e2e sont des bombes à retardement, et aujourd'hui est le jour où deux ont sauté

Trois specs e2e échouent. **Prouvé indépendant de B1** : la suite relancée après `git stash` de tout
mon travail échoue exactement pareil.

La cause est structurelle. `supabase/seed.sql` pose des disponibilités sur des dates **en dur** —
`2026-09-05`, `-07`, `-10`, `-12` — et **huit specs** codent les mêmes dates en dur
(`const DATE = "2026-09-05"`). Nous sommes le 7 : la date du 05 est passée, celle du 07 est
aujourd'hui et le seed la pose déjà pleine (`capacity 1, booked 1`). Les trois specs qui échouent
sont exactement celles qui visent le 05 et le 07. **Les cinq autres visent le 10 au 15 : elles
mourront d'elles-mêmes d'ici une semaine.**

Personne ne l'avait vu parce que **les e2e n'avaient pas été lancés** — ni dans le rattrapage du
2026-09-04, ni dans ma vérification « base verte » de ce matin, qui ne couvrait que typecheck, lint,
Vitest et build. Deuxième fois de la journée que cette vérification se révèle incomplète.

Le plus frappant : **le remède existe déjà et il est documenté pour ce cas exact.**
`packages/e2e-support/src/date.ts` expose `isoDate(daysFromNow)`, avec ce commentaire — « une date
relative à aujourd'hui **sans dépendre de données seedées figées** ». Cinq specs importent déjà ce
module. La migration a été commencée et jamais finie, parce que c'est le **seed** qu'il faut rendre
relatif, pas seulement les specs. Porté au backlog.

### Vérifié

Typecheck 0 erreur, lint 0 erreur, **862 tests** Vitest, build vert, table des routes inchangée,
coquille présente dans le HTML servi. E2E : 11 passent, 3 échouent pour la raison ci-dessus,
2 non exécutées.

## 2026-09-07 (nuit) — Les bombes à retardement désamorcées, et deux fragilités mises au jour

Demande de Jérôme : régler ça avant B2, puisque sans e2e fiables le renommage des routes ne serait
pas vérifiable.

### Ce qui est corrigé

`supabase/seed.sql` posait ses disponibilités sur **huit dates en dur** et **douze specs** codaient
les mêmes littéraux. Tout est désormais relatif au **mois suivant**, jour du mois conservé — une
spec qui visait le 12 vise toujours un 12, ce qui garde les deux fichiers lisibles.

**Pourquoi le mois suivant et pas « aujourd'hui + n »**, qui aurait été le réflexe : le calendrier
d'une fiche produit n'affiche **qu'un mois**, celui de la première disponibilité (`defaultMonth`
dans `ReservationForm.tsx`). Un décalage relatif traverse la frontière de mois une fois sur trois,
et la date visée n'est alors **pas dans le DOM** — le sélecteur `[data-date]` échoue sans rien dire
d'utile. Le mois suivant est toujours entier et toujours futur. C'est la leçon que
`nextMonthIsoDate()` portait déjà dans `packages/e2e-support`, née d'un échec de
`partner-agenda.spec.ts` le 2026-08-27 ; elle n'avait simplement jamais été appliquée au seed.

Un helper nommé porte la convention : **`seedDate(day)`**, qui refuse un jour au-delà de 28 (février
existe). Les douze specs l'utilisent. Une treizième — `admin-evento-vitrine` — utilise
`nextMonthIsoDate` : sa date est **saisie** par un admin dans un formulaire, pas seedée, et le
helper juste n'est pas le même.

**Résultat sur la vitrine : de 3 échecs / 11 passent / 2 non exécutées à 1 échec / 15 passent.**

### L'échec restant n'a rien à voir avec les dates

`reserve-lodging-pms-availability` utilisait **déjà** `isoDate(n)`. Diagnostic : le test clique une
seconde date pour former une plage qui **enjambe une nuit pleine** et attend un avertissement
(`range-unavailable-warning`) ; or le calendrier **désactive** désormais ce bouton — il empêche la
sélection au lieu de l'avertir. Le test décrit un comportement que le produit n'a plus. Arbitrage
produit, hors de ce lot.

### Deux fragilités mises au jour, dont une que j'ai déclenchée

⚠️ **Les 36 specs e2e d'`apps/admin` échouent, et la cause est unique** : `échec de la vérification
TOTP` pour `admin@hifago.test`. `seed.sql` insère un secret TOTP **en clair** dans
`auth.mfa_factors` ; **GoTrue v2.195.0 chiffre désormais ces secrets**, donc la vérification échoue.
Preuve que c'est bien ça : les deux autres facteurs de la base, créés par le **vrai** parcours
d'enrôlement, sont intacts et vérifiés.

C'est **exactement la leçon que le seed avait déjà apprise pour les comptes** — son propre en-tête
explique qu'un `INSERT` direct dans `auth.users` est refusé et qu'il faut passer par l'API Admin
(`seed_auth_users.mjs`). La même chose vaut maintenant pour le facteur MFA.

⚠️ **Et c'est moi qui l'ai déclenché** : la CLI Supabase **n'est épinglée nulle part**. `npx
supabase`, lancé aujourd'hui pour la première fois depuis longtemps, a installé la 2.117.0, qui a
tiré des images Docker plus récentes. Une commande de lecture a monté toute la stack locale sans
prévenir. Les deux points sont au backlog : réparer l'enrôlement MFA du seed, et épingler la CLI.

### Vérifié

Typecheck 0 erreur, lint 0 erreur, seed relu en base (`2026-10-05` à `-20`, mois suivant, jours
conservés). E2E vitrine : **15 passent, 1 échoue** pour la raison ci-dessus. E2E admin : bloqués par
le MFA, indépendamment de ce lot.

---

## 2026-09-07 (nuit) — Lot B2 : les routes passent à l'espagnol, et la CI était rouge depuis cinq commits

**Ce qui était demandé** : « note pour plus tard ; en attendant avance sur le reste » — le MFA du
seed et l'épinglage de la CLI restent au backlog, on enchaîne sur B2, le renommage des routes de la
vitrine en espagnol (spec 27 §0).

### Le renommage lui-même

Sept dossiers déplacés par `git mv`, donc l'historique de chaque fichier reste attaché :

| Avant | Après |
|---|---|
| `(auth)/login` | `(auth)/entrar` |
| `(auth)/signup` | `(auth)/registro` |
| `(auth)/verify-email` | `(auth)/verificar-email` |
| `(cuenta)/account/orders` | `(cuenta)/cuenta/reservas` |
| `(tunnel)/checkout` | `(tunnel)/pago` |
| `(vitrine)/products/[slug]` | `(vitrine)/productos/[slug]` |
| `(vitrine)/establishments/[slug]` | `(vitrine)/establecimientos/[slug]` |

Puis 66 fichiers d'`apps/web` réécrits, plus trois lignes hors de l'app (`webProductUrl` de
`packages/e2e-support`, un fixture de `buildAuthCallbackRedirect.test.ts`, un commentaire de
`bogotaDates.ts`). Le remplacement n'a **pas** été un `sed` sur le mot : il n'a touché un segment
qu'en **position d'URL** — juste après un délimiteur de chaîne, après une interpolation `${…}`, ou
après un préfixe de locale. Sans cette précaution, `@/lib/products/…` et `./support/login` (le
helper e2e) partaient avec, et le second n'aurait cassé qu'à l'exécution des e2e.

Ce que le filtre positionnel ne pouvait pas voir, et qu'il a fallu reprendre à la main : la **cible
d'un `?next=`** (`/entrar?next=/checkout` — le second chemin n'est pas en début de chaîne), le
**chemin relatif d'un import** entre deux dossiers renommés (`../../products/[slug]/ProductPhotos`,
seule vraie casse), et un chemin **percent-encodé** dans une assertion (`%2Fes%2Fcheckout`) — celui-
là est passé jusqu'au Vitest, seul témoin possible.

Table des routes du build, après : `/[locale]`, `/cuenta/reservas`, `/entrar`,
`/establecimientos/[slug]`, `/pago`, `/productos/[slug]`, `/registro`, `/verificar-email`, plus
`/[locale]/r/[code]` et les cinq routes d'API, intacts. Aucune redirection posée : `apps/web` n'a
jamais été servi en production.

### Ce qui n'a PAS été renommé, et pourquoi

- **`apps/admin` garde ses routes en anglais.** Elles ne sont pas localisées et ne sont pas des URL
  publiques ; les mêmes mots y sont donc restés partout, y compris dans les commentaires d'`apps/web`
  qui citent un fichier admin.
- **Les composants de vue, les `data-testid` et les namespaces i18n gardent leurs noms**
  (`CheckoutForm`, `go-to-checkout-link`, `CheckoutPage`). Ce ne sont pas des URL, et le lot B3
  réécrit ces écrans : les renommer maintenant serait du bruit sur des fichiers voués à disparaître.
- **L'icône de panier du header pointe sur `/pago`, pas sur `/carrito`.** La spec 27 sépare bien les
  deux, mais `/carrito` n'existe pas encore : la faire pointer là aujourd'hui donnerait un 404
  depuis chaque page du site. L'écran visé porte aujourd'hui **à la fois** la liste du panier et le
  formulaire de paiement. `SiteHeader` dit pourquoi, en une constante déjà prévue pour ça.

### ⚠️ La vraie trouvaille : `hifago-ci` est rouge depuis cinq commits, et je ne l'avais jamais regardé

`gh run list` : **échec sur les cinq derniers pushes**, depuis `feat(web): lib/catalog`. Deux causes
enchaînées, toutes deux invisibles en local parce que **`npm run lint` ne lance aucun des scripts de
la CI** :

1. **`docs:check`** échouait depuis `lib/catalog` — et la cause est un **bug de fuseau dans le
   générateur d'index lui-même**. `scripts/docs_index.js` datait un fichier modifié par
   `new Date().toISOString().slice(0, 10)`, donc **en UTC** : tout `docs:index` lancé après 19 h à
   Guatapé écrivait la date du **lendemain**, quand la CI relit la date du **commit**. L'index était
   déclaré périmé dès le push, indéfiniment. C'est le §11.20 mot pour mot — sauf que
   `check-timezone.sh` ne balayait pas `scripts/`.
2. **`check-timezone.sh`** a rejoint le rouge au lot B1 : son exemption pour
   `LodgingReservationForm.timezone.test.tsx` est listée **par chemin**, et le fichier était passé
   sous le groupe de routes `(vitrine)`. Le chemin a d'ailleurs bougé **deux fois dans la même
   journée** — `(vitrine)` hier soir, `productos` aujourd'hui.

Les trois corrections, chacune vérifiée **par mutation** et non observée verte :

- `docs_index.js` calcule sa date à `America/Bogota` (même formule `Intl.DateTimeFormat` +
  `formatToParts` que `packages/domain/src/time/`, recopiée parce que ce script est du JS nu lancé
  hors des workspaces) ;
- `check-timezone.sh` balaie désormais **`scripts/`** — prouvé par un fichier témoin déposé puis
  retiré : rouge avec, vert sans. `docs_index.js` y est exempté nommément, avec sa raison ;
- l'exemption de chemin porte un avertissement : elle est solidaire de l'arborescence des routes,
  à revérifier dans tout commit qui déplace un écran.

`docs/backlog.md` repassait aussi au-dessus de sa limite de 60 lignes : quatre bricoles fusionnées
en une ligne dense, ce que le fichier fait déjà pour la dette QA.

### ⚠️ Et une seconde : mon propre test pgTAP était rouge, pour une raison plus profonde que je ne le croyais

`search_catalog.test.sql` : **6 assertions sur 16** en échec, sans qu'une ligne de SQL ait bougé.
L'en-tête du fichier affirme que chaque assertion est scopée sur ses propres fixtures, donc immunisée
contre les données accumulées en local. **C'est faux, et d'un cran** : `search_catalog()` porte un
`p_limite` par **défaut de 24** et un `order by tipo` — et `activity` trie en premier. Le jour où la
base locale a dépassé 24 activités laissées par les e2e, la fenêtre s'est refermée **avant** les
couchages, et le `where id = …` de l'assertion filtrait un ensemble qui ne les contenait déjà plus.

Scoper l'assertion ne suffit pas quand la pagination s'applique **avant** elle. Chaque appel du
fichier passe désormais un `p_limite` explicite. L'application, elle, n'était pas concernée :
`buscarSecciones` calcule déjà `porSeccion × 5`.

### Vérifié

Typecheck 0 erreur · lint 0 erreur · **862 tests Vitest** (138 admin + 458 web + 266 domain) ·
`next build` d'`apps/web` vert, table des routes relue · les **5 contrôles de la CI** au vert
(`check-design-system`, `check-seo`, `check-timezone`, `docs:check`, `check:instructions`).

**pgTAP : 846/846 sur base reconstruite** (`npm run db:setup`). Avant la reconstruction, 5 fichiers
échouaient encore : tous de la famille « données locales accumulées » déjà au backlog
(`audit_log` non scopé), aucun lié à ce lot ni au changement de seed de cette nuit — c'est ce que la
reconstruction prouve.

**E2E vitrine : 15 passent, 1 échoue** — `reserve-lodging-pms-availability`, exactement le même
échec qu'avant ce lot (le calendrier désactive là où le test attend un avertissement). **Aucune
régression du renommage.** E2E admin toujours bloqués par le MFA du seed, donc les specs admin qui
consomment `webProductUrl` n'ont pas pu être vérifiées en réel.

⚠️ **Une exécution sur sept du `npm run test` monorepo a montré 1 échec** dont je n'ai pas capturé le
nom ; six exécutions ultérieures sont vertes (dont trois de la seule suite `apps/web`). Signalé tel
quel plutôt que passé sous silence : je ne sais pas quel test c'était.

### ⚠️ Correction du même soir : la CI est restée rouge, et ma correction de fuseau ne suffisait pas

Le push de ce lot a échoué sur `docs:check` — **encore**. Le bug de fuseau était réel et corrigé,
mais il n'était pas la cause principale ; il la masquait.

`actions/checkout` clone **en superficiel** (`--depth 1` par défaut), et dans un dépôt superficiel
`git log -1 -- <chemin>` rend le **commit de tête pour n'importe quel fichier** — il n'a pas
d'historique à consulter. `gitMaj` datait donc les 41 documents du jour du push, quand l'index
commité porte leurs vraies dates. `docs:check` ne pouvait pas passer, **quoi qu'on corrige**.

Reproduit avant de conclure, plutôt que déduit : `git clone --depth 1` du dépôt local dans un
dossier temporaire, `node scripts/docs_index.js --check` → même échec, aux mêmes deux lignes ;
puis `--build` dans ce clone → **24 lignes de différence**, toutes des dates ramenées au jour du
push.

Et le vrai chiffre est pire que celui que j'ai écrit plus haut : ce n'était pas cinq runs rouges
mais **dix-sept d'affilée**, depuis `chore(repo): le rangement complet` du 2026-09-07 — le commit
qui a touché beaucoup de documents d'un coup et fait diverger leurs dates de celle de la tête. Le
dernier run vert date du **2026-09-04**.

Deux corrections, la seconde étant celle qui compte :

- `fetch-depth: 0` sur le `checkout` du job `lint` — la CI voit enfin l'historique ;
- **`docs_index.js` refuse de tourner sur un clone superficiel** (`git rev-parse
  --is-shallow-repository`) et sort en code 2 avec le geste à faire. Vérifié par mutation : le même
  script, dans le clone `--depth 1`, refuse ; dans le dépôt complet, passe. Sans cette garde, le
  script continuerait de produire un index faux **en silence** — et c'est ce silence qui a coûté
  dix-sept runs, pas le calcul de date lui-même.

---

## 2026-09-07 (nuit) — Lot C : les cinq contrôles de la vitrine, et deux d'entre eux ont trouvé quelque chose

Spec 27 §8. Cinq règles écrites dans la spec, cinq contrôles qui les rendent bloquantes. Chacun
**vérifié par mutation** — on casse la règle, on regarde le contrôle échouer, on répare — jamais
observé vert et déclaré bon.

| Contrôle | Fichier | État |
|---|---|---|
| couleurs en dur (hex, `oklch(`, `rgb(`, palette Tailwind) | `scripts/check-tokens.sh` | créé |
| requête Supabase dans un fichier de route | `scripts/check-data-layer.sh` | créé |
| navigation interne hors `@/i18n/navigation` | `scripts/check-i18n-links.sh` | créé |
| zones `(tunnel)`/`(cuenta)`/`(auth)` noindex | `scripts/check-seo.sh` | étendu |
| barrel `@hifago/ui` dans un fichier de route | `scripts/check-design-system.sh` | étendu |

### `scripts/lib/sans-commentaires.pl` — le fichier sans lequel trois contrôles naissaient faux

La spec l'avait prévu ; le chiffre exact le rend concret. Une recherche naïve de `@hifago/ui` dans
les fichiers de route remonte **quatre** fichiers — `(vitrine)/page.tsx`, `(vitrine)/layout.tsx`,
`(vitrine)/establecimientos/[slug]/page.tsx`, `not-found.tsx` — dont **aucun ne l'importe** : tous
les quatre citent la règle en commentaire pour expliquer qu'ils la respectent. Un contrôle qui
punit les fichiers les plus soigneux est un contrôle qu'on désactive.

Les filtres écrits jusqu'ici dans ce dépôt (`grep -v ':[0-9]*: *//'`, `sed 's://.*::'`) ratent les
lignes de **continuation** d'un commentaire de bloc ou d'un commentaire JSX `{/* … */}` — elles
n'ont aucun préfixe. D'où un petit filtre partagé qui retire `/* … */` et `// …` **en conservant
les numéros de ligne** (il remplace par du vide sans toucher aux retours à la ligne), avec un
`(?<!:)` pour ne pas tronquer les URL au premier `//`.

### Le contrôle de navigation a trouvé deux vrais défauts

La spec pariait que « les quatre autres contrôles passent déjà ». Faux : `LoginForm.tsx` et
`SignupForm.tsx` prenaient `useRouter` dans **`next/navigation`** au lieu d'`@/i18n/navigation`.
Avec `localePrefix: "always"`, `router.push("/verificar-email?…")` produit un chemin qui n'existe
pas ; le proxy le rattrape par une redirection qui **redevine la langue depuis un cookie** au lieu
de garder celle de la page lue. Rien ne casse visiblement — c'est exactement pour ça que la règle
avait besoin d'un contrôle. Corrigé dans le même lot, et `login.spec.ts`/`signup.spec.ts` le
prouvent de bout en bout.

Le contrôle ne vise que les quatre exports qui fabriquent un chemin (`useRouter`, `redirect`,
`permanentRedirect`, `usePathname`) : `notFound()` reste légitime, il n'a pas d'équivalent localisé
et ne construit aucune URL. Vérifié par mutation dans les deux sens.

### Les zones noindex ne se contrôlent pas page par page — mesuré, pas déduit

La formulation de la spec (« une route sans `robots: { index: false }` ») aurait crié à tort sur
`(cuenta)/cuenta/reservas/page.tsx`, qui n'en déclare pas. Vérifié **en réel sur le serveur de
dev**, dans les deux sens :

- en retirant le `robots` de `(tunnel)/pago/page.tsx`, `/es/pago` sert **toujours**
  `<meta name="robots" content="noindex, follow">` — hérité de la coquille de zone ;
- en lui faisant déclarer `robots: { index: true }`, elle sert `index` et **annule sa zone**.

Le contrôle a donc deux moitiés : la coquille de chaque zone impose le noindex, et aucun fichier de
la zone ne le redéclare indexable. C'est la seconde moitié qui protège réellement.

### Deux autres écarts avec la prévision

**`check-design-system.sh` ne couvrait pas l'invariant 1.** Il ne balayait que
`apps/web/components/`, jamais `apps/web/app/`. La règle y est d'ailleurs plus stricte : un
composant peut importer le barrel s'il porte `"use client"`, une route ne le peut **jamais** —
la « réponse » `"use client"` sur une route coûterait le rendu serveur et les métadonnées.

**Six écrans hérités, pas cinq** : `(cuenta)/layout.tsx` est né depuis la mesure. Il est exempté du
seul `createClient` — la garde d'accès du §5 doit résoudre la session — mais **jamais** du
`.from(`, et une mutation le vérifie. La liste des six est de la dette VISIBLE : elle doit rétrécir
à chaque lot, et une ligne ajoutée y serait une régression. Le contrôle est bloquant dès
aujourd'hui sur tout écran neuf, au lieu d'attendre la réécriture des six — c'est-à-dire de ne
jamais être activé.

### Vérifié

Les **huit** contrôles au vert (`design-system`, `timezone`, `seo`, `tokens`, `data-layer`,
`i18n-links`, `docs:check`, `check:instructions`) · typecheck 0 · lint 0 · **862 tests Vitest** ·
e2e vitrine **15/16**, l'échec restant étant toujours `reserve-lodging-pms-availability`.

Onze mutations au total, chacune remise en place : hex, classe de palette, hex en commentaire (ne
doit PAS crier), `.from(` en route, `createClient` en route, `.from(` dans la garde exemptée,
`next/link`, `<a href="/…">`, `redirect` de `next/navigation`, `notFound` (ne doit PAS crier),
coquille sans robots, page qui annule sa zone, barrel en route, barrel en commentaire (ne doit PAS
crier).

---

## 2026-09-08 — Lot D : l'accueil existe, et c'est le premier écran du chantier front

Tranche 1 de la spec 28. Le catalogue à plat disparaît ; l'accueil devient l'écran de résultats,
les critères vivent dans son URL, et plus une seule requête ne part d'un fichier de route.

### Trois choses que la reconnaissance a changées avant la première ligne de code

Cinq agents ont cartographié la surface existante en parallèle avant que j'écrive quoi que ce soit.
Trois trouvailles ont modifié le plan :

- **Aucune migration n'était nécessaire.** Le §6 de la spec annonçait trois colonnes à ajouter à
  `search_catalog` — le lot A les avait déjà écrites, la spec ayant été rédigée avant lui.
- **`lib/catalog/criterios.ts` et `segmentos.ts` existaient déjà**, avec exactement le contrat du
  §7, plus un `hayCriterios` non prévu.
- **Un SIXIÈME fichier e2e** dépendait du testid de l'ancienne carte :
  `apps/admin/e2e/partner-qr-tool.spec.ts`, que la liste des « fichiers touchés » de la spec ne
  nommait pas. Sans la reconnaissance, il serait tombé à l'exécution — dans l'autre app.

### Ce qui a été construit

Sept agents en parallèle, contrats de props figés d'avance : `TarjetaOferta` (molecule),
`SeccionOfertas` (organism), `EstadoVacio` (molecule), `BuscadorInicio` (hôte client colocalisé),
les deux fichiers de messages, le second couchage du seed, et les e2e. J'ai gardé `page.tsx` —
le point d'intégration — et le typecheck a validé les sept contrats d'un coup.

**`CatalogBrowser` est supprimé.** 186 lignes de `page.tsx` qui faisaient trois requêtes
séquentielles et soixante lignes de regroupement, plus 125 lignes qui filtraient en mémoire côté
client. Le regroupement vit maintenant dans `search_catalog`, le filtrage dans SQL.

**La preuve que la règle tient** : `scripts/check-data-layer.sh` ne porte plus d'exemption pour
`(vitrine)/page.tsx`. La liste des écrans hérités est passée de cinq à quatre — c'est le premier
rétrécissement, et c'est exactement ce que ce contrôle a été écrit pour rendre mesurable.

**Le seed couvre enfin le regroupement.** Un second couchage vendable sur Casa Kayam, sans
`lobby_category_id` (un second produit PMS ferait dépendre l'accueil de LobbyPMS) et à un prix
INFÉRIEUR au premier — c'est ce qui rend vérifiable que `precio_desde` vaut bien le minimum.
Prouvé sur base reconstruite : `search_catalog` rend une carte d'établissement pour Casa Kayam avec
`precio_desde = 65000` face à un couchage à 100 000.

### Deux défauts que j'ai corrigés après les agents

**Le pluriel du filtre « personas » ne pouvait pas venir de la page.** « 1 persona » / « 3 personas »
s'accorde sur un nombre qui est un état CLIENT ; la page ne le connaît pas. La clé existait dans les
messages et personne ne la consommait : `PeopleField` affichait le nombre nu sur son déclencheur.
C'est `BuscadorInicio` qui la traduit désormais.

**La resynchronisation du panneau sur l'URL manquait**, alors que le §9 de la spec l'exige en toutes
lettres. React garde l'état d'un composant monté et l'initialiseur de `useState` ne rejoue pas :
sans elle, le bouton « précédent » du navigateur affichait des champs qui ne décrivaient plus les
résultats. Écrite avec le pattern « ajuster l'état pendant le rendu », et surtout : **la comparaison
porte sur la signature de l'URL, jamais sur l'objet de props** — celui-ci est reconstruit à chaque
rendu, le comparer effacerait la saisie en cours à chaque frappe. Les deux régressions opposées ont
leur test, et la première est vérifiée par mutation (retirer le bloc fait rougir ce test-là et lui
seul).

### Deux points ouverts, portés au backlog plutôt que tranchés

- **La carte groupée perd son décompte.** Le cahier §2b.4 montre « Casa Kayam · 6 alojamientos »,
  mais le type `TarjetaOferta` fixé au §0 de la spec 28 ne porte aucun nombre. Combler l'écart coûte
  une colonne au `returns` de `search_catalog` et un champ au type — pas un chantier, mais la
  modification d'un contrat validé. Pas à trancher en codant.
- **Le carrousel de la carte d'activité** tient dans un visuel de 64 px : les flèches et les points
  d'Embla occupent la vignette entière. `Card.tsx` n'a pas été touché — c'est un lot à part, la
  convention l'exige. La story `Affichage/TarjetaOferta` → `Lista` existe pour regarder le défaut
  avant de choisir entre les trois issues.

### Vérifié

Typecheck 0 · lint 0 · **895 tests Vitest** (138 admin + 491 web + 266 domain) · les six contrôles
de la vitrine verts · `next build` vert · **pgTAP 846/846 sur base reconstruite** ·
**e2e vitrine 21 passent sur 22** — l'échec restant est toujours
`reserve-lodging-pms-availability`, inchangé depuis trois lots.

L'e2e de l'accueil (`home.spec.ts`, six tests) couvre l'ordre des sections, le clic vers une fiche,
la recherche qui pousse ses critères dans l'URL, l'état vide et le retour depuis cet état, un
paramètre invalide qui rend 200 plutôt qu'une erreur, et la carte groupée qui mène à la page
d'établissement. Il compte aussi les `<h1>` : un seul, c'est l'invariant.

### Lot D, suite — la Tranche 2 : les suggestions de la barre

Le §2 de la spec tenait en une phrase et ne disait pas ce qu'une suggestion CONTIENT. Plutôt que
d'inventer, je l'ai repris des deux seules sources qui l'avaient déjà tranché : les stories de
`SearchBar` du 2026-09-02 — des offres, des établissements, des raccourcis de catégorie, chacun avec
une ligne secondaire « nature · lieu » — et le §2a du cahier (« on cherche par tag, par nom, par
type d'offre et par établissement »).

**Le premier état ne coûte rien.** Avant la deuxième frappe, la barre propose les types réellement
présents à l'écran avec leur nombre d'offres : la page les a déjà comptés pour rendre ses sections.
Aucune requête, et un type absent des résultats n'apparaît pas — un raccourci ne mène jamais à une
page vide.

**Il fallait un pont, et un seul est possible.** Le champ est client, `lib/catalog/` est
`server-only` : `GET /api/catalogo/sugerencias` appelle la même RPC que l'accueil, avec le même
client anonyme et les mêmes policies. Il n'expose rien de plus que la page. Sa réponse d'échec ne
porte PAS de clé `sugerencias` — une panne qui aurait la forme d'un succès s'afficherait comme
« aucun résultat ».

**L'agent a extrait plutôt que dupliqué, et son argument est le bon** : `clave` et `href` ne sont pas
« du code qui se ressemble », c'est la même décision — comment ce dépôt désigne une offre. Deux
copies dériveraient au premier changement de segment d'URL, la carte irait au bon endroit et la
suggestion au mauvais, et **aucun test des deux modules ne deviendrait rouge**. D'où
`identidadDeFila` dans `buscar.ts`, importée par `sugerencias.ts`.

**Deux garde-fous distincts sur la frappe**, qu'on confond souvent : l'anti-rebond de 250 ms évite
une requête par caractère ; l'`AbortController` évite qu'une réponse lente à « kay » arrive APRÈS
celle de « kayak » et réécrive la liste avec un résultat périmé — un défaut qui ne se voit qu'en
réseau lent.

**Le lint m'a repris, et il avait raison.** Ma première version vidait les suggestions de façon
synchrone dans l'effet ; `react-hooks/set-state-in-effect` l'a refusé. Le vidage ne servait à rien :
l'affichage est DÉRIVÉ — sous le seuil, la liste rendue est celle des raccourcis. Une liste périmée
peut rester en mémoire, elle n'atteint jamais l'écran. C'est le « you might not need an effect » de
React, appliqué à la lettre.

**Un raccourci de type n'a délibérément pas de `href`** : il ne quitte pas la page, il change ses
critères — donc il passe par `onSuggestionSelect`, et `escribirCriterios` reste le seul écrivain de
l'URL. Les suggestions d'offre, elles, portent un vrai `href`, avec ⚠️ le préfixe de langue posé À
LA MAIN : `SearchBar` rend l'option en `<a>` natif, pas avec le `Link` localisé. Conséquence
assumée : les raccourcis de type ne se combinent jamais avec une recherche texte, puisqu'ils
disparaissent dès la deuxième frappe.

**Vérifié** : typecheck 0 · lint 0 · **914 tests Vitest** · les six contrôles verts · `next build`
vert (`/api/catalogo/sugerencias` dans la table des routes) · **e2e vitrine 22 passent sur 23**,
l'échec restant étant toujours `reserve-lodging-pms-availability`. L'e2e des suggestions prouve la
chaîne complète en navigateur : les raccourcis avant la frappe, la liste venue du catalogue après,
la ligne secondaire composée côté client, et le clic qui atterrit sur `/es/productos/…` avec son
préfixe de langue.

### Lot D, fin — le §9 « États de l'écran », tenu pour de bon

Deux promesses de la spec que le code ne tenait pas encore.

**La navigation n'était pas signalée**, et pire, elle risquait de vider l'écran : une recherche
re-rend la page côté serveur, et sans transition React remplace tout par un vide — le panneau
compris, alors que le §9 exige qu'il reste utilisable. `useTransition` garde l'écran précédent monté
et donne, en prime, le seul signal fiable qu'une recherche est en cours. ⚠️ La région d'état est
rendue **en permanence, vide au repos** : un `role="status"` monté au moment où il a quelque chose à
dire n'est jamais annoncé par un lecteur d'écran. C'est la faute classique du motif, invisible à
l'œil comme au typecheck — d'où un test qui ne vérifie pas le texte mais l'EXISTENCE de la région.

**Une base injoignable rendait la page d'erreur nue de Next.** L'échec était franc (c'est la
décision de la spec 27 §9 : « un catalogue vide rendu comme un catalogue normal est pire qu'une
erreur ») mais illisible. `(vitrine)/error.tsx` le rend lisible. Trois choses apprises en l'écrivant :
`"use client"` y est imposé par Next et non choisi ; elle ne rend PAS la coquille contrairement à
`not-found.tsx`, parce qu'un `error.tsx` de groupe est déjà rendu à l'intérieur du layout de sa zone
— la rendre donnerait deux en-têtes ; et le message brut de l'erreur ne doit jamais atteindre
l'écran, il arrive d'une couche qui parle à Supabase et peut porter un nom de table. Il part au
journal avec son `digest`. Un test l'affirme explicitement plutôt que de faire confiance.

Les trois autres zones n'ont toujours pas les leurs : seule la vitrine porte aujourd'hui un écran
qui interroge le catalogue, et créer trois fichiers sans écran pour les justifier serait de
l'anticipation — ce que le README interdit.

**Vérifié** : typecheck 0 · lint 0 · **918 tests Vitest** · six contrôles verts · build vert ·
e2e vitrine 22/23, échec restant inchangé.

## 2026-09-08 — Une règle de cadrage : quelle forme, quel effort, avant chaque tâche

Jérôme a posé la bonne question : « ultracode et xhigh, est-ce que ça vaut vraiment le coup sachant
que ça consomme beaucoup ? » La réponse honnête est **non, pas en permanence** — et elle se mesure.

**Les chiffres de la session** : 170 agents en sept workflows. Les deux gros audits ont tué **60 %
de leurs propres trouvailles** à la vérification (15 confirmées sur 40, puis 14 sur 24) : le
mécanisme adversarial fonctionne, mais une bonne part du budget sert à fabriquer du bruit puis à le
détruire. La construction de l'accueil à sept agents écrivait du code dont TOUS les contrats étaient
déjà figés — les agents exécutaient, ils ne décidaient rien — et **les deux vrais défauts du lot ont
été trouvés après coup, pas par eux**. À l'inverse, la reconnaissance à cinq agents, le plus petit
budget de la journée, a trouvé trois choses qui ont changé le plan avant la première ligne de code.

D'où `.claude/rules/orchestration.md` : une table de décision (tâche → forme → effort) et l'
obligation d'annoncer en UNE ligne, avant de commencer, la forme retenue et pourquoi. La règle
courte : **orchestrer quand je ne sais pas encore ce qu'il y a dans le code, ou quand il faut être
sûr avant de valider ; jamais quand je sais déjà quoi écrire.**

Deux limites écrites noir sur blanc dans la règle plutôt que découvertes plus tard : je ne peux pas
changer l'effort de la session en cours (c'est `/effort`, côté Jérôme) — je peux seulement le
recommander et le fixer pour les sous-agents ; et ultracode ne s'allume pas depuis ici non plus.

⚠️ **Et cette règle n'a AUCUNE vérification mécanique**, ce qui est exactement ce que le §11.20
condamne. Aucun script ne peut constater qu'une phrase de cadrage a été écrite. Le seul contrôle est
humain : l'annonce est visible en tête de réponse, donc son absence se remarque. C'est écrit dans la
règle elle-même — si elle commence à manquer, c'est le signal qu'elle est morte, pas qu'elle est
respectée.

## 2026-09-08 — La revue adversariale du lot D : 16 trouvailles sur 29 étaient fausses, 5 étaient vraies

Quatre chercheurs (correction, règles du dépôt, conformité aux specs, valeur des tests), chaque
trouvaille passée devant un réfuteur **payé pour la détruire** et instruit de réfuter en cas de
doute. 29 remontées, **16 tuées**, 5 confirmées — et confirmées en les REPRODUISANT, pas en les
déduisant : contre le Postgres local pour les deux défauts SQL, par mutation exécutée pour les trois
trous de couverture.

### Le vrai défaut : les suggestions ne proposaient pas ce qu'on leur demandait

`search_catalog` finit par `order by c.tipo, c.rango_seccion` — un ordre pensé pour l'ACCUEIL, qui
range ses cinq sections. Or `products.type` est du texte, donc l'ordre est alphabétique :
`activity < camp < evento < lodging < transport`. Demander six lignes à cette requête, c'est
demander **« les six premières activités »**, jamais « les six meilleures correspondances ».

Reproduit en réel : taper « Casa Kayam » proposait bien l'hôtel ; en lui ajoutant cinq activités, le
même appel rendait six activités et **la carte de l'hôtel avait disparu**. Le prédicat texte couvre
le nom de l'établissement, donc son nom fait correspondre tous ses produits — et sa propre carte, de
type `lodging`, est servie en dernier. Latent avec le catalogue actuel, certain dès qu'un partenaire
vend une poignée d'activités. J'avais hérité cet ordre de l'accueil sans le réexaminer, et le
commentaire que j'avais écrit à côté (« les MEILLEURES correspondances, tous types confondus ») était
un souhait, pas une description.

### Trois trous de couverture, dont un dans mon propre travail de la veille

- **Retirer `useTransition` en entier** — l'import, l'état, la transition, donc tout le signal de
  navigation — laissait **515 tests sur 515 au vert**, `eslint` à zéro, aucun `check-*.sh` en défaut.
  La moitié du commit `222d8fe`, écrit quelques heures plus tôt pour tenir le §9, pouvait disparaître
  sans qu'un seul garde-fou bouge. Le test qui existait ne couvrait que la moitié accessibilité (« la
  région est montée au repos ») ; la moitié « elle dit quelque chose » n'était vérifiée nulle part.
- **Retirer le `if (!reponse.ok) throw`** du pont de suggestions laissait la suite verte, alors que
  le rendu partait en `TypeError: Cannot read properties of undefined` — donc sur l'écran d'erreur.
- **Les trois tests du lot montaient un catalogue i18n écrit à la main.** Renommer `{indice}` en
  `{index}` dans les messages laissait tout vert pendant que chaque photo aurait porté
  `alt="HomePage.fotoAlt"` en production : `t()` n'est pas typé sur le catalogue (aucune augmentation
  `IntlMessages` dans ce dépôt), et `parity.test.ts` ne compare que des CHEMINS de clés, jamais leurs
  variables. Ils utilisent désormais `loadMessages("es")` — la convention déjà majoritaire du dépôt.
  ⚠️ Et la justification qui les excusait (« le lot i18n est écrit en parallèle ») était **fausse** :
  `git log -S"fotoAlt"` montre que les messages et le test sont nés dans le MÊME commit.

Les quatre correctifs sont chacun vérifiés par mutation : la mutation rougit exactement le test
ajouté, et lui seul.

### Le cinquième : documenté, daté, pas corrigé

`?tag=<slug inconnu>` vide l'accueil, alors que le §0 promet « tag inconnu → ignoré ». Vrai pour
`tipo`, validé à la lecture ; faux pour `tag`, qui part tel quel en SQL — et `BuscadorInicio` le
reporte à chaque recherche, donc la page ne se déverrouille plus. Non corrigé ici pour deux raisons
dites franchement : **rien ne fabrique de lien `?tag=` aujourd'hui** (les pages de tags sont la
spec 29), et le correctif tient en trois lignes de SQL dans une fonction de 300 qu'il faudrait
republier en entier. La spec 29 doit de toute façon redéfinir `search_catalog` et donner des tags au
seed — qui n'en a **aucun**, si bien que le volet « chercher par libellé de tag » du cahier §2a n'est
couvert par rien non plus. Porté au backlog comme tâche de ce lot-là, pas comme intention.

### Ce que la revue dit du dispositif lui-même

**55 % de faux positifs.** C'est le prix, et il est visible : les seize réfutations sont
argumentées, plusieurs corrigent la gravité annoncée à la baisse. Deux réfuteurs ont eux-mêmes
corrigé le trouveur (« l'accueil n'est PAS touchée », « ces deux chaînes SONT tenues par
`home.spec.ts` ») — c'est exactement ce qu'on attend d'un contradicteur.

⚠️ Un agent réfuteur a laissé un fichier de brouillon dans l'arbre (`ZZrefute.test.tsx`), attrapé par
le hook de fin de session. Autoriser un agent à muter un fichier pour tester une hypothèse suppose
qu'il restaure — la vérification `git status` doit rester du côté de l'orchestrateur.

**Vérifié** : typecheck 0 · lint 0 · **923 tests Vitest** · six contrôles verts · build vert ·
e2e vitrine 22/23, échec restant inchangé.

### Fin de session — l'ordre de la suite, validé

Jérôme a validé le 2026-09-08 : **spec 29 (listings + index de tags) PUIS spec 30 (fiches produit et
établissement, précédée du lot B3 — l'extraction de la logique de réservation vers `lib/reservas/`)**.

La raison qui a emporté la décision est vérifiable : **les cinq « Ver más » de l'accueil mènent
aujourd'hui à un 404** (`/es/actividades`, `/alojamientos`, `/transportes`, `/camps`, `/eventos` —
mesuré au serveur de dev). C'est le seul trou visible du parcours qu'on vient de livrer. La fiche
produit, elle, fonctionne : elle est simplement restée l'ancien écran, hors du design system et avec
ses huit requêtes Supabase dans le fichier de route (exemptée nommément par
`scripts/check-data-layer.sh`, une des quatre exemptions restantes).

Ce que la spec 29 devra ramasser au passage, en plus de ses six routes : **le seed n'a AUCUN tag**
(`catalog_tags` est vide), donc l'index de tags n'aurait rien à montrer et le volet « chercher par
libellé de tag » du cahier §2a n'est couvert par rien ; et le `?tag=<slug inconnu>` trouvé par la
revue, reporté ici précisément parce que ce lot redéfinit `search_catalog` de toute façon.

Session close ici : le contexte est devenu trop lourd, la suite reprend dans une conversation neuve
à partir de ce journal, du backlog et des specs 27/28.

## 2026-09-08 (suite) — Spec 29 : un entretien écran par écran, et des tags qui deviennent des catégories

Session neuve, reprise depuis le journal, le backlog et les specs 27/28. Objet : écrire la spec des
six routes où mènent les cinq « Ver más » de l'accueil — toutes en 404.

### Ce que la reconnaissance a corrigé avant la première question

Lectures ciblées, sans agents : je savais quels fichiers ouvrir. Deux constats ont changé le
cadrage.

- **`hrefSeccion` n'est pas « écrite et jamais appelée »** : `page.tsx:144` l'utilise pour les cinq
  liens. C'est `buscarTipo` qui est écrite, testée, et appelée par personne.
- **L'écran admin des étiquettes existe déjà** (`apps/admin/app/admin/tags/`, six fichiers, un e2e)
  et n'écrit que `label: { es }` + `slug`. Ni description, ni image, ni traduction anglaise — fait
  qui allait devenir décisif deux questions plus loin.

### Sept tours de questions, une décision renversée en cours de route

L'index de tags d'abord, parce que c'était le moins décidé des six écrans. Treize arbitrages, dont
trois méritent d'être retenus :

- **Une activité sans tag n'était visible nulle part.** Le cahier §2a fait de `/actividades` un
  index de sous-catégories : une activité non taguée n'y figure donc dans aucune tuile, et le seed
  n'a *aucun* tag — les trois activités actuelles sont dans ce cas. Jérôme a choisi une tuile
  « Otras actividades » conditionnelle, à `/es/actividades/otras` : un **slug réservé** par
  contrainte SQL, parce qu'une catégorie nommée « Otras » masquerait la page en silence.
- **Jérôme a renversé sa propre réponse deux tours plus tard**, et c'est la décision structurante du
  lot : *« les tags qui sont en fait des catégories, on doit pouvoir ajouter une image et le nom et
  son texte que l'on display »*. Il avait choisi « libellé seul » quelques minutes avant. Ça ne
  change pas l'affichage, ça change le **modèle de données** — `catalog_tags` gagne `description` et
  `image_path` — et ça fait sortir le lot du front : l'écran admin doit pouvoir les remplir, sinon
  une catégorie créée en production naît vide.
- **Une recherche lancée depuis un listing repart à l'accueil.** Le site garde **un seul écran de
  résultats**, celui posé par la spec 28. L'alternative (rester dans le listing) affichait « aucun
  résultat » pour « kayak » tapé depuis les hébergements, alors que le site en vend trois.

### Ce que la rédaction a tranché seule, et pourquoi

**Extraire `catalog_candidates`.** Trois écrans doivent désormais filtrer le catalogue avec
exactement les mêmes règles : les sections de l'accueil, une liste d'offres, l'index de catégories.
Deux copies des six prédicats donneraient un index qui propose une catégorie que sa propre page ne
montre pas — **sans qu'aucun test des deux fonctions ne devienne rouge**. `search_catalog` doit être
republiée de toute façon (sa signature gagne `p_sin_tag`), donc l'extraction ne coûte presque rien.
⚠️ `create or replace` ne suffit pas quand la signature change : `drop` explicite, puis `create`,
puis **regrant** — un grant oublié rendrait la vitrine entière muette pour `anon`.

**Un plafond dur sur `?pagina`** (1 à 20). Sans lui, `?pagina=99999` demande 2,4 millions de lignes
à Postgres depuis une URL publique et anonyme. Ce n'est pas un réglage d'affichage.

**Le tri des catégories ne se fait pas en SQL.** L'ordre alphabétique porte sur le libellé résolu
dans la locale, avec son repli JSONB — que la base ne connaît pas. Un `order by label->>'es'`
classerait la version anglaise par ses libellés espagnols.

### Deux limites dites d'avance plutôt que découvertes

- **Le seed n'aura aucune image de catégorie** : `storage.objects` ne se remplit pas depuis un
  fichier SQL. Les tuiles rendront l'aplat gris en local comme en e2e ; la variante illustrée n'est
  couverte que par le test de composant et sa story.
- **Tant que la Tranche 3 (l'admin éditorial) n'est pas livrée**, une catégorie créée en préprod ou
  en production naît sans image ni texte. Seul le seed sait les remplir d'ici là.

**Validée par Jérôme le 2026-09-08**, en bloc, après lecture des sept points que la rédaction avait
tranchés seule. Trois tranches : les quatre listings (quatre 404 en moins), l'index de catégories,
puis l'admin éditorial. Rien n'est encore implémenté.

## 2026-09-08 (suite) — Spec 29 Tranche 1a : `search_catalog` republiée, et une dette de la veille refermée

Premier lot de code de la spec 29. SQL seul, à `xhigh` sur demande de Jérôme — « une migration, donc
l'effort maximal ». Le découpage qui a été retenu : **1a le SQL à `xhigh`, 1b le front à `high`**,
parce que la Tranche 1 n'est pas homogène et que payer l'effort maximal sur des composants dont tous
les contrats sont écrits, c'est exactement ce que la règle de cadrage de la veille condamne.

### Une décision de la spec renversée en la codant, et arbitrée avant d'écrire

Le §6b validé le matin faisait extraire les six prédicats de filtrage dans une fonction
`catalog_candidates` partagée. Deux faits, découverts en l'écrivant :

- **`security invoker` impose `grant execute … to anon`** sur la fonction extraite — sinon
  `search_catalog`, qui s'exécute avec les droits du visiteur, ne peut pas l'appeler. La fonction
  « interne » serait donc devenue une **RPC PostgREST publique**, née d'un détail d'implémentation.
  Aucune donnée nouvelle exposée, mais une surface d'API de plus sans contrepartie.
- Il aurait fallu **découper 300 lignes qui marchent**, couvertes par 16 assertions, pour un seul
  consommateur nouveau.

Jérôme a tranché après une explication reformulée — sa première réaction avait été « pas compris » :
`search_catalog_tags` **appellera** `search_catalog` au lieu de la découper. L'objectif reste
atteint à l'identique — **les six filtres n'existent qu'une fois** — et c'est cela seul qui compte :
deux copies divergeraient un jour, l'index proposerait « Kayak » et la page Kayak dirait « aucun
résultat », sans qu'aucun test des deux fonctions ne devienne rouge.

### La règle que je me suis fixée avant de toucher au SQL

**Les 16 assertions pgTAP existantes doivent rester vertes SANS être modifiées.** Si l'une avait dû
être ajustée, c'est qu'un prédicat aurait bougé. Résultat : 16/16 vertes, `git status` vide sur le
fichier de test. Les 5 assertions nouvelles s'ajoutent, elles ne remplacent rien (seul `plan(16)` →
`plan(21)` change, et c'est une nécessité mécanique de pgTAP).

Deux précautions prises avant d'écrire, et toutes deux ont servi :

- **La définition VIVANTE en base a été comparée caractère par caractère au fichier de migration**
  avant réécriture (règle 7 de `.claude/rules/supabase.md` : ne jamais retaper une RPC longue à la
  main). Identiques — donc le corps a pu être repris du fichier, qui porte les commentaires, plutôt
  que de `pg_get_functiondef`, qui les perd au-dessus du `create`.
- **Vérifié qu'aucun appelant ne passe ses paramètres par position** (`buscar.ts`, `sugerencias.ts`
  et les 16 assertions nomment tous les leurs). C'est ce qui autorise à insérer `p_sin_tag` à sa
  place logique, après `p_tag_slug`, au lieu de l'ajouter en queue de signature.

⚠️ Et `create or replace` ne suffisait pas : la signature change. Sans `drop` explicite, Postgres
aurait créé une **surcharge** — deux fonctions homonymes, PostgREST en choisissant une selon les
paramètres reçus, sans erreur et sans trace. Vérifié après coup : `nb de fonctions homonymes = 1`,
et les grants `anon`/`authenticated` bien reposés (le `drop` les emporte).

### Ce qui est prouvé, et comment

**Par mutation, pas par déduction** — la leçon de la revue de la veille :

| Mutation appliquée en base | Ce qui rougit |
|---|---|
| retirer la branche « tag inconnu » | `not ok 19` — **et elle seule** |
| neutraliser `p_sin_tag` | `not ok 20` — **et elle seule** |
| retirer la branche « tag inconnu » | l'e2e `home.spec.ts:233`, sur l'assertion « l'écran n'est jamais l'état vide » |

La troisième ligne est la plus parlante : le test e2e échoue exactement sur le **symptôme décrit**
par la revue — la page vidée qui ne se déverrouille plus. Les mutations ont vécu dans le
scratchpad, jamais dans le dépôt, et `git status` a été vérifié après restauration (la leçon du
`ZZrefute.test.tsx` oublié : la vérification de l'arbre reste du côté de l'orchestrateur).

**Vérifié** : typecheck 0 · lint 0 · **pgTAP 851/851** (846 + 5) · e2e `home.spec.ts` **7/7** ·
types régénérés — une seule ligne de diff, `p_sin_tag?: boolean`, **optionnelle**, donc aucun
appelant existant ne change.

### Ce que ça referme

La dette `?tag=<slug inconnu>` de la revue du 2026-09-08 est **close**, exactement comme la spec 28
§10quinquies l'avait prescrite (le prédicat, l'assertion pgTAP, `&tag=zzz-inexistant` dans l'e2e).
Sa ligne quitte le backlog. Reste ouvert, et déplacé en dette technique : **le seed n'a toujours
aucun tag** — c'est la Tranche 2.

## 2026-09-08 (suite) — Spec 29 Tranche 1b : les quatre listings existent, et deux d'entre eux étaient déjà écrits

Front, à `high` — la moitié annoncée du découpage 1a/1b. Tous les contrats venaient du §7 de la
spec : rien à décider, seulement à écrire. Quatre routes qui rendaient un 404 la veille répondent.

### Deux découvertes qui ont RETIRÉ du travail

La reconnaissance avant d'écrire a fait gagner deux fichiers sur la liste du §0 :

- **`lib/seo/jsonld/breadcrumb.ts` existait déjà**, avec son test — et il est **déjà posé sur les
  deux fiches**. Le §0 l'annonçait « à créer ». ⚠️ Le constat qui en découle vaut mieux que le
  gain : **les fiches déclarent aux moteurs un fil d'Ariane qu'aucun visiteur ne voit**. Le JSON-LD
  est là depuis un lot antérieur, le composant visible n'existait nulle part. La règle SEO 6 veut
  qu'un JSON-LD décrive ce que la page affiche — écart porté au backlog, à refermer avec la spec 30.
- **`BuscadorInicio` poussait déjà vers `/`.** La décision 10 (« une recherche depuis un listing
  repart à l'accueil ») était donc gratuite : le fichier n'a pas été touché, seul `atajosTipo={[]}`
  lui est passé. Le §0 prévoyait de lui ajouter une « destination explicite ».

### Quatre choses que seul le rendu réel pouvait dire

- **HeroUI `Breadcrumbs` rend un `<ol>` NU, sans `<nav>`.** Un `<ol>` portant un `aria-label` n'est
  pas un landmark ARIA : le fil aurait disparu de la liste des repères de la page, précisément pour
  le public à qui il sert le plus. L'enveloppe `<nav>` est à nous ; le style reste entièrement
  celui du design system. Constaté en imprimant le HTML rendu, pas en le supposant — et la sonde a
  été retirée aussitôt (`git status` vérifié).
- **`isPending`, jamais `isDisabled`** sur « Cargar más ». L'atome `Button` le dit déjà, mais la
  raison est plus forte ici qu'ailleurs : un bouton désactivé **perd le focus**, donc le visiteur au
  clavier serait renvoyé en haut du document à chaque page chargée — sur le seul chemin qui lui soit
  garanti, puisque l'observateur d'intersection ne se déclenche jamais pour lui.
- **`react-hooks/refs` a refusé d'écrire une ref pendant le rendu**, et il avait raison : ça casse
  la pureté du rendu, que React 19 peut jeter et rejouer. La synchronisation passe par un effet.
- **`delete criterios.tipo` plutôt que `tipo: undefined`** : `hayCriterios` compte les CLÉS
  présentes. Une clé posée à `undefined` compte pour un filtre, et la page aurait affiché « ta
  recherche ne donne rien » là où il faut « cette section est encore vide » — sur une route où
  personne n'a rien cherché. Le genre de bug qui ne casse aucun test et ment au visiteur.

Et un rappel d'outillage : **`npx next typegen`** est nécessaire après avoir créé une route.
`PageProps<"/[locale]/camps">` n'existe pas tant que les types de routes ne sont pas régénérés, et
l'erreur (`does not satisfy the constraint 'AppRoutes'`) ne dit pas quoi lancer.

### Le plafond, reposé deux fois — et ce n'est pas une redondance

`?pagina` est plafonné à 20 dans la page **et** dans le Route Handler. Le pont est appelable
directement, sans passer par la page : sans le second plafond, `?pagina=99999` contournait la
protection en une requête. Un plafond ne vaut qu'à **chaque porte d'entrée**, pas à la première.

### Ce que ce lot ne prouve pas, et pourquoi c'est dit

**Le défilement n'est pas couvert en e2e, et il ne peut pas l'être** : une page sert 24 offres, le
seed en compte dix, `hayMas` est donc toujours faux et le bouton n'est jamais rendu. Fabriquer 25
offres dans un spec pour voir un bouton coûterait plus cher que ce que ça prouve.
`ListadoInfinito.test.tsx` couvre l'ajout de page, le décompte, le `replaceState` (jamais
`pushState`) et l'échec du pont avec le vrai composant ; l'e2e couvre le **pont**, en HTTP, contre
la vraie base — y compris qu'une réponse d'échec **n'a pas de clé `tarjetas`**, sans quoi une panne
se lirait « il n'y a plus rien ».

**Trois des quatre listings sont vides** avec le seed : aucun camp, evento ni transporte n'y est
vendable. Ce n'est pas un défaut du lot — c'est le cas « route structurelle sans offre » du §9 (200
et un état vide, jamais un 404), et c'est ce qui a permis de tester les deux états sans rien
fabriquer. Conséquence à connaître : seule la section alojamientos porte un « Ver más » sur
l'accueil aujourd'hui, les sections vides n'étant pas rendues.

**Vérifié** : typecheck 0 · lint 0 · **943 tests Vitest** (923 + 20) · les six contrôles verts ·
`next build` vert, les cinq routes dans la table · **e2e vitrine 30/31** — le seul échec est
`reserve-lodging-pms-availability`, antérieur au chantier et inchangé depuis quatre lots (arbitrage
produit en attente, pas une régression).

**Reste de la spec 29** : la Tranche 2 (`catalog_tags` éditoriale, `search_catalog_tags`, l'index de
catégories et ses pages) et la Tranche 3 (l'admin qui remplit image et texte).

## 2026-09-08 (suite) — Spec 29 Tranche 2 : les tags deviennent des catégories, et l'index existe

Le cinquième « Ver más » de l'accueil cesse d'être un 404. Fait à `high`, d'un bloc — contrairement
à la Tranche 1, dont la migration republiait 300 lignes validées : ici **tout est additif**, deux
colonnes nullables et une fonction neuve. Le pire cas d'une erreur était un écran neuf qui affiche
mal, pas une vitrine cassée. Ce raisonnement, dit avant de commencer, s'est vérifié.

### Ce que le SQL a demandé, et ce qu'il n'a pas demandé

`search_catalog_tags` **appelle** `search_catalog` au lieu de recopier ses filtres (l'arbitrage du
matin). Elle tient en une quinzaine de lignes, et son seul vrai piège est un `having count(*) > 0`
sans `group by` : sans lui, l'agrégat rendrait TOUJOURS une ligne, et l'index afficherait « Otras
actividades » menant à une page vide — exactement ce que le cahier §2a interdit pour les tags.
Vérifié par mutation : le retirer fait rougir l'assertion 8, et elle seule.

**Et une règle qui n'était vérifiée par rien l'est maintenant** : la contrainte `slug <> 'otras'`
a son `throws_ok`, posé EN TANT QUE POSTGRES — en `anon`, une policy RLS refuserait l'écriture avant
que la contrainte n'ait son mot à dire, et le test aurait prouvé la policy, pas la contrainte.

### Trois choses apprises en codant

- **`products.category` n'est pas un tag**, et la confusion coûte une contrainte violée : c'est
  l'ancienne liste FERMÉE de six familles (`musica, arte, bienestar, nautica, adrenalina,
  gastronomia`) que `catalog_tags` remplace précisément. Les deux coexistent tant que la colonne
  n'est pas retirée. Mon seed écrivait `'naturaleza'` ; le CHECK l'a refusé.
- **Le nom d'une catégorie est du contenu partenaire**, donc une page servie en repli doit rester
  `noindex` (règle SEO 2). `CategoriaConOferta` a gagné `localesNativas`, calculé avec
  `hasNativeContent` — **le même prédicat que le sitemap et les fiches**, jamais une seconde
  version : deux copies feraient lister au sitemap des URL que les métadonnées déclarent `noindex`.
- **La page d'une catégorie fait deux lectures, et c'est le bon compte.** Une liste vide ne
  distingue pas « catégorie inconnue » de « catégorie vidée » — les deux doivent rendre 404 — d'une
  RECHERCHE sans résultat dans une catégorie vivante, qui doit rendre 200. Et la lecture qui décide
  de l'existence se fait SANS les critères : sinon un lien partagé deviendrait mort selon les dates
  qu'il porte.

### Le seed n'illustre pas, il exerce

Quatre catégories, dont deux qui ne doivent **jamais** apparaître : `gastronomia` n'a aucune offre,
`cultura` n'en a qu'une **non vendable**. Les deux doivent être absentes de l'index ET rendre 404
sur leur page. C'est le cas limite le plus facile à casser sans s'en apercevoir — il suffirait
d'oublier le prédicat `p.sellable` — et il est maintenant tenu par l'e2e comme par pgTAP. Une
cinquième activité reste délibérément non taguée : c'est elle qui rend la tuile « Otras
actividades » atteignable.

⚠️ Toujours **aucune image** : `storage.objects` ne se remplit pas depuis un fichier SQL. Les tuiles
rendent l'aplat gris en local comme en e2e.

### Une fragilité révélée, pas créée

`home.spec.ts` (« carte groupée ») a échoué une fois pendant ce lot. Ce n'était pas une régression :
**`reserve-lodging-pms-availability` laisse un produit `lodging` derrière lui à chaque exécution**,
et l'ordre de section étant `created_at desc`, huit résidus saturent le plafond de 8 et poussent la
carte du seed hors de l'accueil. Il y en avait 8 en base — mes propres exécutions du jour en avaient
ajouté deux. Purgés à la main, les 22 tests repassent. Porté au backlog : le compteur repartira.

### Un constat d'accessibilité sur le socle

HeroUI rend le dernier élément d'un fil d'Ariane en `<span role="link" aria-disabled="true"
aria-current="page">`. Le `aria-current` est correct et posé tout seul — c'est l'essentiel — mais un
lecteur d'écran annonce « lien désactivé », que le motif WAI-ARIA déconseille. **Documenté et porté
au backlog, jamais contourné par un composant parallèle** : le socle est unique (CLAUDE.md §2.2).
Conséquence pratique immédiate : un test compte `a[href]`, jamais `getByRole("link")` — l'e2e est
tombé dessus.

### Aussi livré, en avance sur le §2

Le message du slug réservé côté admin (`NewTagForm` **et** `RenameTagButton`, qui recalcule le
slug). La spec le plaçait en Tranche 3 ; la contrainte et son message vont ensemble, sinon un admin
qui nomme une catégorie « Otras » lit « No se pudo crear la etiqueta » et réessaie à l'identique.

**Vérifié** : typecheck 0 · lint 0 · **960 tests Vitest** (943 + 17) · **pgTAP 861/861** (851 + 10) ·
six contrôles verts · `next build` vert (`/[locale]/actividades` et `/[locale]/actividades/[tag]`
dans la table) · **e2e vitrine 37/38**, le seul échec restant étant `reserve-lodging-pms-availability`,
antérieur au chantier et inchangé depuis cinq lots.

**Reste de la spec 29** : la Tranche 3 — l'écran admin qui remplit l'image et le texte d'une
catégorie (`/api/upload/tag`, le bloc éditorial sur `/admin/tags/[id]`). Tant qu'elle n'est pas
livrée, une catégorie créée en préprod ou en production naît sans image ni texte.

## 2026-09-08 (fin) — Spec 29 Tranche 3 : une catégorie devient éditable sans SQL, et sans filet e2e

Dernière tranche de la spec 29. `/admin/tags/[id]` porte désormais un bloc « Contenido de la
categoría » — le texte bilingue et l'image de couverture que la vitrine affiche depuis ce matin.
Avant lui, seul le seed pouvait les remplir : une catégorie créée en préprod ou en production
naissait vide **et le restait**. C'est la différence entre une fonctionnalité montrable et une
fonctionnalité livrée.

### Le seul geste qui touchait de l'existant partagé

`/api/upload/[entity]` est le chemin d'upload canonique de **tout** le module images — photos de
produits et d'établissements, propositions socio, import PMS. La règle que je me suis fixée avant
d'y toucher : **une entrée de liste blanche en plus, et rien d'autre**. Signature intacte, ordre des
vérifications intact (la session est lue avant le corps de la requête), deux chemins existants
intacts.

⚠️ Un seul changement de forme, et il vaut la peine : le ternaire qui choisit le dossier est devenu
une **table**. À trois valeurs, un `a ? b : c ? d : e` est exactement l'endroit où une quatrième
entité partirait dans le mauvais dossier sans que rien ne le signale — le fichier atterrirait dans
`products/` et la page l'afficherait quand même.

### Ce que j'ai réutilisé plutôt qu'écrit

`MediaGallery` avec `maxPhotos={1}`, et `LocalizedTextField` pour le texte bilingue. Les deux
existaient. Le README l'exige (« un composant qui duplique quelque chose de `packages/ui` est une
rupture ») et le gain est concret : le recadrage, les messages d'erreur et le geste sont ceux que
les admins connaissent déjà des fiches produit.

En revanche, **le nettoyage du texte est sorti du composant** vers `lib/tags/`. Ce n'est pas de la
cosmétique de saisie : `hasNativeContent` — le prédicat partagé avec le sitemap — déclare une locale
« native » dès que la chaîne est non blanche. Écrire `{ es: "…", en: "  " }` déclarerait la page
`/en/` traduite, et Google l'indexerait avec un texte vide. Fonction pure, donc testée comme telle.

### Livré sans filet, et vérifié autrement

⚠️ **Les e2e admin sont tous cassés en local** (facteur MFA du seed refusé par GoTrue depuis le
2026-09-07). J'ai annoncé les trois options à Jérôme avant de commencer — vérifier à la main,
réparer le seed d'abord, ou livrer sans preuve — et retenu la première.

L'UI admin exigeant `aal2`, la vérification est passée par HTTP, avec une vraie session obtenue par
mot de passe (le Route Handler, lui, ne demande que `getUser()`) :

| Exercé | Résultat |
|---|---|
| `POST /api/upload/tag` sans session | 401 `not_authenticated` — l'auth reste lue avant le corps |
| `POST /api/upload/zzz` | 400 `invalid_entity` |
| les TROIS entités avec session admin | 200, chacune dans **son** dossier |
| l'objet dans le bucket | présent, converti en WebP par sharp |
| `image_path` écrit, puis la vitrine | `/es/actividades` sert bien l'URL de l'image |

**Ce que personne n'a vu, et il faut le dire** : le rendu de l'écran admin lui-même. Son layout
exige `aal2` — ni navigateur ni Playwright n'y accèdent tant que le seed MFA n'est pas réparé. Sa
mise en page, son bouton et sa galerie sont couverts par le typecheck et par rien d'autre.

Nettoyage fait : les quatre objets téléversés pendant la vérification sont supprimés (par l'API
Storage — `storage.protect_delete()` refuse la suppression SQL directe, ce que j'ignorais), et
`image_path` est remis à `null`. La base locale est restée alignée sur le seed.

### Une dette mineure constatée, laissée telle quelle

Le `Button` d'`apps/admin` est le HeroUI brut : il n'a pas le couple `isPending`/`pendingLabel` que
l'atome d'`apps/web` ajoute — celui qui **garde le focus** pendant un envoi, là où `isDisabled` le
perd. J'ai suivi la convention locale (`isDisabled` + libellé conditionnel, comme les six autres
formulaires admin) plutôt que d'innover dans un coin. L'écart entre les deux apps est réel ; il ne
se corrige pas dans ce lot.

**Vérifié** : typecheck 0 · lint 0 · **964 tests Vitest** (960 + 4) · **pgTAP 861/861** · six
contrôles verts · `next build` vert sur les deux apps.

**La spec 29 est intégralement livrée** — ses trois tranches. Son frontmatter passe à
`statut: implemente`, et les six routes qui rendaient un 404 hier matin répondent. Deux de ses
décisions ont été renversées en codant et arbitrées avant d'être écrites (l'extraction SQL le matin,
la place du message de slug réservé l'après-midi) : c'est tracé en §10bis, §10ter et §10quater.

## 2026-09-08 (fin) — Le « seed MFA cassé » n'existait pas : c'était un deadlock, et la base était polluée

Tâche demandée : réparer le seed MFA, qui bloquait tous les e2e admin. Résultat : **il n'y avait
rien à réparer dans le seed**. Le diagnostic porté au backlog depuis le 2026-09-07 était faux, et
deux autres pannes se cachaient derrière.

### Le diagnostic du backlog était faux, et voici comment on l'a su

Le backlog disait : « `seed.sql` insère un secret TOTP en clair ; GoTrue v2.195.0 chiffre désormais
ces secrets, donc la vérification échoue ». Trois mesures l'ont démenti :

- une session isolée obtient l'**AAL2 sans problème** avec le secret en clair du seed ;
- deux vérifications avec le **même code** réussissent toutes les deux — il n'y a pas non plus
  d'anti-rejeu ;
- et les e2e admin **atteignaient les écrans** : ils échouaient sur des assertions, pas à la porte.

⚠️ La leçon vaut plus que le correctif : **un diagnostic écrit au backlog n'est pas un fait tant
qu'il n'a pas été reproduit.** Celui-ci a orienté une semaine de renoncement (« les e2e admin sont
cassés, on ne peut rien vérifier ») sur une cause inventée.

### La vraie panne : un deadlock que le client ne voit jamais

`Failed to update sessions. ERROR: deadlock detected (SQLSTATE 40P01)`. Tous les specs admin
partagent LE compte `admin@hifago.test` — donc l'unique facteur TOTP seedé. Deux workers qui montent
en AAL2 en même temps verrouillent les mêmes lignes dans un ordre différent, et Postgres en tue un.

⚠️ **Le client ne voit jamais le mot « deadlock »** : GoTrue renvoie « Unexpected failure, please
check server logs ». Le message réel n'est que dans `docker logs supabase_auth_*` — c'est là qu'il
fallait regarder, et c'est ce qui explique qu'on ait pu se tromper de cause si longtemps.

**Trois correctifs partiels ont échoué avant le bon**, et ils sont documentés dans le code pour
qu'on ne les retente pas : le seul réessai (les workers se re-deadlockent à l'identique) ; la purge
des 45 sessions accumulées (le deadlock survient même à zéro session) ; un verrou autour du seul
`verify` (pendant qu'un worker attendait, la montée en AAL2 de l'autre invalidait sa session — il
échouait alors sur « Auth session missing! », une erreur qui ressemble à un bug de helper et n'est
que la conséquence de la première).

Ce qui marche : un `pg_advisory_lock` autour de **toute la séquence connexion + AAL2**, et
uniquement pour les comptes à facteur TOTP partagé. ⚠️ Cette dernière condition n'est pas une
optimisation : la première version sérialisait TOUTES les connexions, y compris celles des comptes
clients de `apps/web` — la suite web est passée de 1 à 3 échecs. **Un verrou trop large coûte plus
qu'il ne protège.**

**Mesure** : plus aucun échec d'authentification, sur la suite admin entière. Les ~15 échecs
restants sont tous des échecs d'INTERFACE (éléments introuvables, timeouts de clic) — une autre
panne, à diagnostiquer séparément.

### La deuxième panne : 78 produits résiduels contre 7 de seed

En cherchant pourquoi `reserve.spec.ts` s'était mis à échouer, la vraie raison est apparue : les
specs e2e créent de vrais produits et ne les suppriment jamais. **78 résidus pour 7 lignes de
seed.**

Ce n'est pas du désordre, c'est une cause d'échec : les sections de l'accueil et des listings
plafonnent à 8 offres en `created_at desc`, donc les résidus — toujours plus récents — poussent les
offres SEEDÉES hors de l'écran. `home.spec.ts` et `reserve.spec.ts` échouaient sur des sélecteurs
pourtant justes. Et **les deux suites interfèrent** : lancer les e2e admin cassait ensuite les e2e
web, ce qui rend tout échec inattribuable.

Jérôme a tranché en une phrase : « les tests devraient delete à la fin des tests ». D'où
`packages/e2e-support/src/cleanup.ts` et un `globalTeardown` sur les deux apps.

**Comment il reconnaît ses cibles** : par l'HORODATAGE (`Date.now()` dans le slug), jamais par une
liste de préfixes — une liste serait périmée au prochain spec écrit, et personne ne le remarquerait.
Les sept slugs du seed sont fixes et n'en portent aucun. Vérifié après coup : le seed est intact.

⚠️ **L'ordre de suppression vient de `pg_constraint`, pas d'une intuition** — et deux essais ont
échoué avant le bon, chacun sur une FK que je n'avais pas listée : `ledger_entries.order_line_id`
pointe vers la LIGNE et pas la commande, et `partner_capabilities.establishment_id` bloque la
suppression d'un établissement. Neuf tables référencent `establishments`, dont huit en NO ACTION.

**Mesure** : la suite admin crée puis purge 22 produits, 14 établissements et 4 catégories ; la base
revient à ses 7 produits de seed. L'accumulation entre exécutions est finie.

⚠️ **Ce que ça ne résout pas, dit clairement** : l'interférence PENDANT une même exécution. Deux
tests parallèles se disputent toujours les 8 places d'une section — `cart-multi-establishment` passe
seul et échoue en suite. La correction de ce cas-là est de scoper les données par test, pas de
nettoyer à la fin. Porté au backlog.

### Et une régression de mon propre lot, trouvée au passage

`admin-tags-catalog.spec.ts` échouait parce que la Tranche 3 a ajouté `CategoriaEditorialBlock` sur
`/admin/tags/[id]` : la page tire désormais `MediaGallery` et `ImageCrop`, donc s'hydrate plus
lentement, et le clic arrivait sur un bouton encore sans gestionnaire. Le test avait DÉJÀ le bon
diagnostic écrit à côté — mais son `waitForLoadState` était placé après le clic qu'il devait
protéger. Déplacé avant ; le fichier passe, quatre exécutions de suite.

**Vérifié** : typecheck 0 · lint 0 · **964 tests Vitest** · e2e admin **62-66/80 selon les
exécutions, zéro échec d'authentification** (contre un blocage total annoncé) · e2e web 35/38.

## 2026-09-08 (fin) — Ce que les e2e admin cachaient vraiment : trois pannes empilées

En reprenant les ~15 échecs admin laissés ouverts, une troisième cause est apparue sous les deux
premières.

**`ServerFilters` est un `<form method="GET">` à soumission NATIVE** : chaque « Aplicar » recharge
entièrement la page et referme le panneau de filtres. Le clic suivant sur « filters-toggle »
atteignait donc un bouton déjà présent dans le HTML servi, mais dont React n'avait pas encore
attaché le gestionnaire.

⚠️ Ce qui rendait ce défaut illisible : **le premier clic passait toujours, le second jamais.** La
page était chargée depuis longtemps au premier, fraîchement rechargée au second. Et le test
échouait 30 secondes plus tard sur un champ de filtre resté INVISIBLE — donc en pointant le
mauvais coupable. Cinq specs échouaient ainsi.

D'où `abrirFiltros(page)`, qui attend l'hydratation avant d'ouvrir le panneau, appliqué aux huit
specs qui utilisent `ServerFilters`. Mesuré : `admin-products-list` passe de 3/4 à **4/4**.

### Et le vrai état, mesuré plutôt qu'espéré

La suite admin reste à **18 échecs**. Mais la mesure qui compte est ailleurs : relancée en
`--workers=1`, **16 des 18 échouent encore**. Ce ne sont donc ni des artefacts de parallélisme, ni
de la pollution de base — **ce sont de vrais défauts fonctionnels**, de natures variées : quatre
navigations qui n'aboutissent pas, cinq éléments absents, quatre timeouts, trois textes faux.

C'est un chantier de diagnostic à part entière, un test à la fois. Il est ouvert au backlog, avec
ce qui est désormais RÉGLÉ et ne doit pas être re-diagnostiqué : l'authentification (un deadlock,
pas le chiffrement des secrets), l'accumulation de résidus entre exécutions, et le panneau de
filtres cliqué avant hydratation.

⚠️ La leçon de la journée, en une phrase : **trois pannes distinctes se présentaient comme une
seule** (« les e2e admin sont cassés »), et la première explication écrite au backlog était fausse.
Chacune n'a cédé qu'après avoir été REPRODUITE — jamais après avoir été déduite.

## 2026-09-08 (fin) — Spec 30 : la reconnaissance a corrigé deux des trois questions avant qu'on les pose

Dernier écran hérité de la vitrine — les deux fiches, les plus gros fichiers du chantier. La
session s'ouvrait sur trois « décisions de modèle à poser à Jérôme, jamais à trancher seul ».
**Deux d'entre elles étaient mal posées**, et c'est une reconnaissance de sept lecteurs, lancée
avant la première question, qui l'a établi.

### Les trois corrections, et ce qu'elles ont évité

- **« La fiche vitrine n'a aucun support en base » était faux.** Le cahier §2e la tranche depuis
  le 2026-09-07 : `sellable = true` + `external_booking_url` (migration `20260814190000`), « ce
  n'est **jamais** `sellable = false` », « ce n'est pas réservé aux eventos ». Le front lit déjà la
  colonne — mais dans la seule branche `evento`. Il ne manquait pas un modèle, il manquait un
  débranchement. La seule question réellement ouverte était la contrainte de prix, et le cahier la
  désignait déjà nommément comme à soumettre à Jérôme.
- **`partners.phone` était le mauvais candidat, et c'était mesurable en une requête** : 0 ligne
  remplie sur 36 (`partner_accounts.phone` : 1 sur 41). La migration `20260819100000` dit
  pourquoi — `partners` est *l'organisation*, le partenaire saisit son téléphone ailleurs. Écrire
  la migration demandée aurait produit **un bouton de contact vide sur toutes les fiches** : un lot
  livré, mesuré vert, et sans le moindre effet. Le cahier §2f gardait d'ailleurs trois issues
  ouvertes, pas une décision acquise. Retenu : une colonne `contact_phone` sur `establishments` —
  le contact décrit le LIEU, et aucune policy publique n'est posée sur une table d'identité.
- **La citation qui justifiait un arbitrage n'était pas là où on la citait.** « Casa Kayam ·
  6 alojamientos » est attribué au « cahier §2b.4 » par la spec 28 §10bis **et** par le backlog ;
  `grep` ne trouve pas même le mot « alojamientos » dans le cahier. Elle existe — dans le journal
  du 2026-08-15 — et elle décrit un comportement **livré** : « une carte, qui mène à sa page et
  annonce son décompte ». L'arbitrage n'était donc pas « faut-il ajouter ça au cahier » mais
  « faut-il réparer ce que la refonte a perdu ». Jérôme a tranché : on restaure.

⚠️ **La leçon prolonge celle du matin** (« un diagnostic écrit au backlog n'est pas un fait tant
qu'il n'a pas été reproduit) : **une citation propagée de document en document n'est pas une
source.** Trois documents citaient le cahier ; aucun ne l'avait rouvert.

### Ce que la troisième question a coûté à trancher : rien du tout

`products.check_in_time` vs celui de l'établissement traîne au backlog depuis des semaines comme
« décision de modèle ». Mesuré : **zéro ligne renseignée des deux côtés**, la vitrine ne lit que
celui de l'établissement, et les deux migrations qui ont créé la situation disent déjà la réponse
— « les horaires sont une propriété du LIEU » (`20260827200000`), « les fusionner est une décision
de modèle, pas un nettoyage » (`20260827220000`). Il ne restait qu'à l'écrire. Le coût de trancher
était nul, et il ne le serait plus dès la première ligne saisie.

### Le lot B3 n'est pas ce que son nom laissait craindre

La consigne le présentait comme portant l'invariant anti-survente. Mesuré : **aucun des trois
formulaires n'appelle `create_order` ni la moindre RPC** — leur unique appel réseau est un GET de
disponibilité PMS. La barrière est ailleurs (`CheckoutForm.tsx:188`). Ce qu'ils portent est un
guidage d'affichage à échec fermé. Le risque de l'extraction n'est donc pas de créer une survente,
c'est d'**affaiblir le guidage** — d'où la règle posée avant d'écrire : les 32 tests DOM restent
verts **sans être modifiés**.

Et la reconnaissance a trouvé ce qu'aucune lecture rapide n'aurait donné : **huit duplications
nommées** entre les trois fichiers (dont une garde `NaN` présente dans un seul des trois, et le
même calcul de places restantes écrit à quatre endroits), plus une couche de domaine **déjà
extraite** sous un autre nom — `lib/products/reservationRange.ts`, 7 exports, 14 tests — que la
spec 27 classe pourtant « explicitement non touché » tout en annonçant `lib/reservas/`. Une
contradiction interne, arbitrée par Jérôme : `lib/reservas/` absorbe.

### Deux pièges écrits noir sur blanc parce qu'ils se referment en silence

- **`establishments` n'a AUCUN grant `SELECT` au niveau table pour `anon`** — dix-huit grants
  colonne par colonne. Une colonne ajoutée n'hérite de rien : sans `grant select (contact_phone)`,
  la vitrine répond « permission denied for column », une erreur qui tombe AVANT la RLS et
  ressemble à un bug de policy (piège §11.1).
- **Le `drop` de `search_catalog` réussira même s'il casse `search_catalog_tags`** : les
  dépendances de fonction à fonction ne vivent pas dans `pg_depend`, le corps PL/pgSQL étant du
  texte opaque. Vérifié que l'appelante nomme ses colonnes, donc une colonne de plus au `returns`
  ne la casse pas — mais c'est une propriété à **vérifier**, pas à espérer, et `drop … cascade`
  la supprimerait en silence.

### Ce que la rédaction a tranché seule, et un défaut trouvé en l'écrivant

Huit points, listés au §10. Le plus intéressant est né de la lecture du code, pas d'une question :
**la fiche produit confond deux axes**. `occurrenceLabel` (quand a lieu l'événement, propriété du
TYPE) n'est rendu que dans la branche `evento` du mode de réservation — comme si la date d'un
événement dépendait de la façon dont on le réserve. Et un evento **sans** `external_booking_url`
n'affiche alors aucun moyen de réserver : un cul-de-sac silencieux, qui existe depuis toujours et
que personne n'avait nommé. La spec sépare les deux axes et **préserve** le comportement (un
evento ne devient pas réservable en ligne — ce serait un changement produit que personne n'a
demandé), mais elle l'écrit et porte la garde manquante au backlog.

### Validé, et ce que ça ouvre

**Validée par Jérôme le 2026-09-08**, en bloc, après lecture des huit points tranchés seuls.
Quatre tranches : le lot B3 d'abord (rien d'autre ne peut commencer avant), le SQL et le seed
d'images ensuite, puis les deux fiches. Rien n'est implémenté.

Trois arbitrages quittent le backlog (les deux de la spec 28 §10bis — le décompte restauré, le
visuel de 64 px laissé dormant parce que ce lot rend des grilles —, et `check_in_time`), et le
point « source du contact d'un établissement » du cahier §2f est fermé. Trois points nouveaux s'y
ouvrent : le bucket qui grossit sans garde-fou (37 objets orphelins, jamais inscrit nulle part),
le nettoyage de `products.check_in_time` côté admin et socio, et la garde manquante sur les
eventos sans URL.

**Vérifié** : `docs:check` vert (43 documents), sommaire et index régénérés. Aucun code touché.

## 2026-09-08 (fin) — Spec 30 Tranche 1 : le lot B3, et une des huit duplications n'existait pas

`lib/reservas/` existe. Les trois formulaires de réservation en sont délestés, **aucune vue n'a été
touchée** — c'est tout l'objet de cette tranche, et c'est ce qui rend la réécriture des deux fiches
possible sans reperdre les six correctifs de fuseau d'août en les recopiant à la main.

### La règle posée AVANT d'écrire, et ce qu'elle a prouvé

**Aucun test existant n'est modifié.** Les 32 tests DOM des quatre fichiers Lodging et les 12 de
`ProductDetailView` devaient rester verts *sans être touchés* — c'est la règle qui avait fonctionné
sur la Tranche 1a de la spec 29 (16 assertions pgTAP intactes). Vérifiée mécaniquement : `git
status` ne montre aucun fichier de test en `M`, et le test déménagé affiche **0 insertion,
0 suppression** — un rename pur.

Ça n'a rien d'une formalité : si l'un de ces 32 tests avait rougi, l'extraction aurait changé un
comportement du calendrier, c'est-à-dire du **guidage à échec fermé**. C'est le seul vrai risque du
lot, et le seul dispositif capable de le détecter.

### Une des huit duplications annoncées n'existait pas

Le §7a de la spec, écrit le matin depuis une reconnaissance, affirmait que `buildInCartNightsMap`
« existe, est testée, et n'est utilisée que par UN des trois formulaires » — les deux autres
recopiant la même boucle à la main. **Faux, et la lecture du code suffit à le voir** : la fonction
fait `if (!includeLine(line) || !line.endDate) continue`. Elle IGNORE les lignes sans `endDate`, or
les lignes d'activité et de créneau n'en ont jamais.

Les deux boucles « dupliquées » n'étaient donc pas la fonction recodée : l'une agrège des lignes à
PLAGE nuit par nuit, les autres des lignes à DATE UNIQUE. Les fusionner aurait fait compter une
nuit d'hébergement comme une place d'activité — silencieusement, et sans qu'aucun test ne rougisse.

⚠️ **Et le fait qui a décidé du geste** : les 14 tests de `reservationRange.ts` ne couvrent
**qu'une seule de ses six fonctions**, `reachableRangeWindow`. `buildInCartNightsMap`,
`resolveTierPrice`, `nightsInRange`, `hasUnavailableNightInRange` et `estimateNightsTotal` n'ont
aucun test. Généraliser une fonction non testée dont dépend le calendrier d'hébergement aurait été
le geste le plus risqué du lot, pour refermer une duplication qui n'en était pas une. D'où une
fonction distincte, `agregarEnCarrito`, dont un test énonce précisément la différence.

### La table de motifs PMS, et le test qui l'empêche de périmer

Le backlog demandait une chose : que `pms_category_not_quoted` rejoigne `connector_inactive` dans
les motifs non retentables. En écrivant la table, on découvre que la Route Handler émet **dix**
motifs — dont deux *calculés* avant la réponse — et que le formulaire n'en connaissait que **deux
par leur nom**. Les huit autres héritaient de « réessayer » par défaut, dont `month_out_of_range`,
que personne n'avait réclamé et qu'il est absurde de proposer de rejouer.

⚠️ **Le défaut de la table est « non retentable », et c'est le point** : si un motif ajouté demain
héritait de « retentable », l'oubli serait invisible — le bouton s'afficherait, le visiteur
cliquerait en vain, personne ne le saurait. Fermé, l'oubli se voit.

Mais une table écrite à la main périme, et reproduire sous une autre forme le défaut qu'on vient de
corriger aurait été absurde. D'où un test qui **relit la source de la Route Handler** et échoue si
un motif y apparaît sans être classé. ⚠️ Il a trouvé un faux positif à sa première exécution —
`"rejected"` et `"unparseable"`, littéraux en position de COMPARAISON dans un ternaire, pris pour
des motifs. C'est aussi la preuve qu'il regarde vraiment la source, et pas une copie.

### Un contrôle CI criait à tort, depuis toujours

`check-timezone.sh` filtre les commentaires avec un `sed 's://.*::'` — qui ne retire **pas** les
blocs `/* … */`. Or `scripts/lib/sans-commentaires.pl` existe précisément pour ça depuis le
2026-09-07 (« le fichier sans lequel trois contrôles naissaient faux »), et les **quatre autres**
contrôles l'utilisent. Celui-là ne l'a jamais reçu.

Personne ne l'avait vu parce qu'aucun fichier du dépôt ne citait `new Date()` dans un bloc JSDoc —
les trois formulaires en citent cinq, mais tous en `//`. `lib/reservas/calendario.ts` est le
premier, et le contrôle a crié sur le commentaire qui explique précisément pourquoi il ne faut pas
écrire `new Date()`. C'est mot pour mot le cas que l'en-tête du filtre décrit : « une recherche
naïve remonte les fichiers les plus soigneux, et un contrôle qui crie à tort est un contrôle qu'on
désactive ».

Corrigé, puis **vérifié par mutation** plutôt qu'observé vert : un vrai `new Date()` injecté dans
ce même fichier est toujours attrapé, à la bonne ligne. La mutation a vécu le temps d'une commande,
et `git diff` a été vérifié vide après restauration.

### L'échec e2e restant, mesuré et non déduit

`reserve-lodging-pms-availability` échoue toujours — 37/38, exactement le score d'avant le lot.
Le journal et le backlog le disent antérieur ; **la leçon du matin étant qu'une ligne écrite n'est
pas un fait**, il a été mesuré : `git stash -u`, relance sur `HEAD` seul, même échec, **même ligne
66, même cause** (la date est désactivée, donc le clic n'aboutit pas, donc l'avertissement ne
s'affiche jamais — le test décrit un produit qui n'existe plus). Puis `git stash pop`.

### Deux choses dites plutôt que tues

- **Le lint m'a attrapé**, comme la veille : `useMemo(ultimoDiaReservable, [])` est refusé
  (« Expected the first argument to be an inline function expression ») — le compilateur React ne
  sait pas analyser une référence. La forme inline n'est donc pas un choix de style, et c'est
  maintenant écrit dans la doc de la fonction.
- **Un changement de comportement, minuscule et assumé** : `limitarCantidad` fait retomber
  `Infinity` sur 1, là où l'ancien code le plafonnait au maximum. Inatteignable par un
  `<input type="number">`, mais un nombre non fini vient toujours d'une saisie cassée. Testé,
  documenté, signalé ici plutôt que glissé.

**Vérifié** : typecheck 0 · lint 0 · **1015 tests Vitest** (964 + 51) · les six contrôles verts ·
`next build` vert · e2e vitrine **37/38**, l'unique échec mesuré antérieur.

**Reste de la spec 30** : la Tranche 2 (les trois migrations, le seed d'images, le seed enrichi),
puis les deux fiches. ⚠️ La migration du contact (§6b) est à relire en manual edit — Jérôme l'a
demandé en validant la spec.

## 2026-09-08 (nuit) — Spec 30 Tranche 2 : le SQL, le seed, et une migration qui a cassé un écran

Les trois migrations sont appliquées, le seed porte enfin des photos, et l'admin sait créer une
vitrine. Le lot a livré ce qu'il annonçait — et deux de ses trouvailles portent sur la *méthode*,
pas sur le code.

### La trouvaille qui vaut le plus : une assertion verte pour la mauvaise raison

L'assertion « le couchage NON vendable ne compte pas dans `n_alojamientos` » a été vérifiée par
mutation. Résultat : remplacer le décompte par un compte BRUT, sans aucun filtre `sellable`, **ne
la fait pas rougir**. La raison est que `search_catalog.test.sql` tourne en `anon`, et que la
policy `products_select_public` écarte déjà le non-vendable avant que le décompte ne compte quoi
que ce soit. En `postgres`, la même mutation rend bien 3 au lieu de 2.

L'assertion reste juste — elle vérifie ce qui compte pour l'écran, et cette propriété est tenue
DEUX fois (le prédicat *et* la RLS). Mais son libellé promettait un filet qui n'existe pas. Réécrit
pour dire ce qu'il prouve. ⚠️ **Sans mutation, ce test aurait été compté comme une garantie du
prédicat pendant des mois.**

### Ma propre migration a rendu atteignable un état que le front ne savait pas rendre

La contrainte `products_price_cop_required_unless_vitrine` autorise depuis ce matin un produit
non-evento sans `price_cop`. Une heure plus tard, mesurée en réel sur le seed, la fiche d'une offre
en vitrine affichait **« 0 COP »** et un bouton **« ajouter au panier »**. Le code n'avait jamais eu
à traiter ce cas : la contrainte l'interdisait.

Corrigé en avance sur la Tranche 3, minimalement — le prix retombe sur `price_label` puis sur rien
(jamais « 0 COP », qui prétendrait la gratuité), et `reservationMode` gagne `"vitrina"` pour tout
non-evento porteur d'une URL. Les 12 tests de `ProductDetailView` sont restés verts sans être
touchés. ⚠️ La leçon est générale : **une migration permissive change ce que le front peut
recevoir**, et l'écran qui l'affiche n'a pas été écrit pour ça.

### Deux erreurs de méthode, de mon fait, dites plutôt que tues

- ⚠️ **J'ai conclu « c'est le carrousel » sur UNE mesure, et c'était faux.** `attribution.spec.ts` a
  échoué une fois ; retirer les photos du produit l'a fait repasser, et j'en ai tiré une cause.
  Trois exécutions ultérieures **avec** les photos passent, et la suite rend 37/38 en `--workers=1`
  comme en parallèle, deux fois de suite. La vraie variable était *seul vs en suite* — la dette de
  concurrence déjà au backlog. C'est exactement l'erreur contre laquelle ce dépôt met en garde
  depuis le matin, commise sur moi-même, huit heures plus tard.
- ⚠️ **Une fonction-sonde laissée en base** (`search_catalog_mutant_probe`) a pollué
  `database.types.ts`. C'est le **diff** qui l'a montrée, pas moi. Deuxième occurrence de ce piège
  après `ZZrefute.test.tsx` : la vérification de l'arbre — et maintenant de la BASE — reste du côté
  de celui qui mute.

### Un contrôle qui se déclarait vert sur une base incomplète

`db-setup.sh` lançait `psql -f seed.sql` **sans `ON_ERROR_STOP=1`**. psql continue alors après une
instruction refusée et sort 0 : une valeur d'`unit` inexistante a fait échouer un insert, et le
script a affiché « base locale prête » sur une base à laquelle il manquait une ligne. Seule une
requête à la main l'a révélé. Corrigé — un script qui ne signale pas son échec est pire qu'un
script absent.

### Ce que le SQL a demandé de plus que la spec ne prévoyait

- **Le contact a eu besoin d'une RPC**, pas seulement d'une colonne : `update_establishment`
  remplace tous les champs et tourne dans les trois chemins de modération de propositions — le
  contact y serait écrasé à chaque approbation, sans erreur et sans trace. Même raison que pour les
  horaires, même forme (`update_establishment_contact`).
- ⚠️ **`anon` a le grant `UPDATE` sur `contact_phone`, sans qu'on le lui ait donné** : une colonne
  ajoutée hérite des grants au niveau TABLE, et `establishments` en porte pour `anon`. Seul
  `SELECT` manquait. Le seul rempart en écriture est donc la policy `establishments_write_admin`,
  et une assertion pgTAP le PROUVE désormais au lieu de l'espérer (CLAUDE.md §3.5).

### Le seed exerce quatre cas, dont celui qu'on perd toujours

`seed-media.mjs` pose 6 lignes média depuis 5 visuels **synthétiques** — générés, jamais
téléchargés (§7.3, et aucune licence à arbitrer). Idempotent, vérifié par trois exécutions
successives. Il exerce 3 photos (carrousel complet), 2 photos, 1 photo (ni flèches ni points) et
**0 photo** — ce dernier est le plus facile à perdre en ajoutant « juste une photo partout », et
c'est le seul qui couvre l'aplat gris.

⚠️ Le partage des deux établissements n'est pas esthétique : `establishment-page.spec.ts` écrit les
horaires de `…-0004` puis les remet à `null`. Y poser des valeurs de seed les ferait effacer par la
première exécution e2e venue. Casa Kayam devient donc le riche, `…-0004` reste le pauvre — et
exerce l'absence de contact.

### Un fait d'outillage, constaté deux fois dans la même séance

⚠️ **La suite pgTAP est inexploitable après une exécution e2e.** Six fichiers comptent `audit_log`
en absolu (`want: 1`, `have: 274` le matin, `have: 2` le soir) et tout e2e admin y écrit.
`npm run db:setup` la referme à chaque fois. Ce n'est pas une régression — c'est la dette déjà
inscrite —, mais sa conséquence méritait d'être nommée : **un chiffre pgTAP mesuré après des e2e ne
veut rien dire.** Portée au backlog.

**Vérifié** : typecheck 0 · lint 0 · **1015 tests Vitest** · **pgTAP 876/876** (868 + 8, après
remise à plat) · six contrôles verts · `next build` vert sur les deux apps · e2e vitrine **37/38**,
l'échec antérieur mesuré par remisage · `admin-evento-vitrine` échoue à la ligne 22, **avant tout
code touché**, vérifié sur `HEAD` seul et déjà nommé au backlog.

**Reste de la spec 30** : la Tranche 3 (la fiche produit) et la Tranche 4 (la fiche établissement).

## 2026-09-08 (nuit) — Spec 30 Tranche 3 : la fiche produit, et un défaut du socle qu'un test a révélé

La fiche produit est réécrite sur le socle. `check-data-layer.sh` a **une exemption de moins** —
quatre, puis trois — et la page passe de 325 à 140 lignes. Le fil d'Ariane que cette page déclarait
aux moteurs depuis des semaines s'affiche enfin, et il porte exactement les mêmes entrées.

### Vérifié en réel, pas seulement en test

Le fil d'une chambre : `Inicio › Alojamientos › Casa Kayam Guatapé › Cama en dormitorio compartido`
— quatre niveaux, parce que c'est le chemin qu'on fait prendre au visiteur (décision de Jérôme).
Celui d'une activité en porte trois. Et le JSON-LD porte **les mêmes entrées, dans le même ordre**,
parce qu'ils viennent désormais d'une seule liste (`migasParaJsonLd`) et non de deux tableaux
écrits côte à côte.

### Trois choses trouvées en écrivant, dont deux que seuls les tests ont vues

- ⚠️ **`data-testid` sur un composant maison passe le typecheck et ne fait rien.** J'ai écrit
  `<Title as="h1" data-testid="product-name">` ; l'atome attend `testId`. TypeScript accepte
  silencieusement les props hyphenées sur un composant, et le rendu réel que j'avais sous les yeux
  ne portait aucun testid — je ne l'ai pas vu. Ce sont **quatre e2e** qui l'ont attrapé.
- ⚠️ **La réécriture avait perdu le suffixe d'unité** (« por persona ») : le type `FichaProducto`
  ne portait pas `unit`. Trouvé en adaptant les tests de la vue — c'est exactement ce qu'ils
  couvrent, et c'est la raison de les lire AVANT de réécrire plutôt qu'après.
- **Un test comparait le prix à une chaîne inventée.** L'ancien passait `priceDisplay="$ 120.000"`
  en dur : il n'exerçait donc pas le formatage. Le montant traverse maintenant l'atome `Price`,
  donc le vrai `formatCop`. ⚠️ Et l'assertion porte désormais sur l'ABSENCE de suffixe, jamais sur
  la chaîne exacte : `formatCop` insère une espace INSÉCABLE, invisible dans un message d'échec —
  « expected '120.000 COP' to be '120.000 COP' ».

### Le défaut du socle, et pourquoi je ne l'ai pas corrigé

En supprimant le lien « ← Volver al catálogo » (le fil d'Ariane le remplace, et en dit plus), un
test est tombé. Pas un flake : **`Migas` rend des `<a href>` NATIFS**, parce que
`Breadcrumbs.Item` de HeroUI étend le `Link` de react-aria et non celui de `@/i18n/navigation`.
C'est écrit dans le commentaire du composant depuis sa création — et c'est une violation de
l'invariant 9 de la spec 27 que personne n'avait relevée.

Invisible sur un listing. Sérieux sur une fiche : **cliquer le fil provoque une navigation
complète, qui vide le panier**, tenu en mémoire par `CartContext`. Le commentaire de
`cart-multi-establishment.spec.ts` le disait déjà, une porte à côté : « un `page.goto()` direct
réinitialiserait le panier ».

Les deux corrections de fond dépassent ce lot, et je les ai mesurées avant de renoncer : brancher
un `RouterProvider` react-aria — **absent de la version installée**, vérifié —, ou rendre le panier
persistant, ce que le cahier §2b.6 décide depuis le 2026-09-07 et que le backlog porte déjà. Le
lien de retour est donc conservé, avec la raison écrite juste à côté pour que personne ne le
supprime en le prenant pour un doublon du fil.

### Un changement que ce lot fait sans le dire ailleurs

⚠️ La fiche passe du client à session au client **anonyme** (invariant 3 de la spec 27).
Conséquence réelle : un admin connecté ne peut plus prévisualiser une fiche non publiée —
`products_select_public` lui ouvrait la porte via `OR is_admin(…)`. C'est voulu : une page publique
montre ce que le public voit, et une prévisualisation est un écran d'admin, pas un effet de bord
d'authentification.

**Vérifié** : typecheck 0 · lint 0 · **1019 tests Vitest** (1015 + 4) · six contrôles verts, dont
`check-data-layer` vérifié PAR MUTATION — réintroduire un `.from()` dans la route le fait rougir,
donc l'exemption retirée protège vraiment · `next build` vert · **e2e vitrine 37/38**, le seul
échec étant l'antérieur connu.

**Reste de la spec 30** : la Tranche 4 — la fiche établissement, ses chambres en grille avec leur
prix, son bouton de contact, et la dernière exemption CI de la vitrine.

## 2026-09-08 (nuit) — Spec 30 Tranche 4 : la fiche établissement, et la liste d'exemptions tombe à deux

Le dernier écran hérité de la vitrine est réécrit. **`check-data-layer.sh` n'exempte plus que DEUX
fichiers**, tous deux hors vitrine — le compte client et le tunnel de paiement. La liste que son
en-tête dit devoir « RÉTRÉCIR à chaque lot » est passée de cinq à deux en trois jours, et la
prochaine réduction demandera d'ouvrir un autre chantier plutôt que d'en finir un.

### Le prix, enfin affiché — et deux informations retirées

⚠️ La carte d'une chambre porte désormais **son prix**. L'écran d'origine SÉLECTIONNAIT `price_cop`
en base et ne l'affichait nulle part, alors que l'entretien du 2026-09-07 le demande explicitement
(« photo · nom · capacité · prix »). C'est un écart au décidé qui traînait, refermé.

En contrepartie, deux informations disparaissent de la carte, et il faut le dire plutôt que le
laisser découvrir : la nature du couchage (« Habitación privada ») et le nombre d'unités
(« 3 en total »), que la `ProductRow` maison affichait. La forme décidée en compte quatre, pas six.
La nature du couchage se lit dans le NOM de la chambre, le parc total n'aide pas à choisir et reste
sur la fiche. `establishment-page.spec.ts` a été adapté, avec la raison écrite dedans — pas
seulement le test corrigé.

### Les cartes passent en grille, et ça ferme un arbitrage sans le rouvrir

Les produits d'un établissement étaient rendus par une `ProductRow` maison en `Card layout="row"` —
donc un visuel de **64 px** pour une chambre, dont la photo est justement ce qui décide. Ils sont
désormais des `TarjetaOferta` en grille, comme partout ailleurs.

⚠️ Conséquence voulue : **ce lot ne réveille pas** le point ouvert de la spec 28 §10bis sur cette
variante. Elle reste affichée sur l'accueil, et nulle part ailleurs.

### Un écran qui n'avait jamais eu de test

`EstablishmentDetailView` n'avait aucune couverture unitaire — la reconnaissance du matin l'avait
relevé. La vue en a maintenant 14, dont celle qui compte le plus : **aucune ligne d'horaires
n'apparaît quand le lieu n'en a pas**, c'est-à-dire que `products.check_in_time` ne remonte plus
nulle part. La règle tranchée ce matin est désormais tenue par un test, pas seulement par une
phrase.

### Un état écrit et inexerçable, dit plutôt que caché

L'état « établissement sans aucun produit » est **inatteignable** : `establishments_select_public`
exige au moins un produit vendable rattaché, donc la page rend 404 avant d'y arriver. Le bloc est
écrit quand même (une policy peut changer), mais aucun test ne peut l'exercer — et prétendre le
contraire serait faux.

**Vérifié** : typecheck 0 · lint 0 · **1033 tests Vitest** (1019 + 14) · **pgTAP 876/876** · six
contrôles verts, `check-data-layer` vérifié PAR MUTATION sur les deux fiches · `next build` vert
sur les deux apps · **e2e vitrine 37/38**, le seul échec étant l'antérieur connu.

**La spec 30 est intégralement livrée** — ses quatre tranches. Son frontmatter passe à
`statut: implemente`. Deux lignes quittent le backlog : le JSON-LD `BreadcrumbList` sans fil
d'Ariane (les deux fiches l'affichent maintenant, construit depuis la MÊME liste), et
`pms_category_not_quoted` (la table de motifs de `lib/reservas/` le referme, avec
`month_out_of_range` en prime).

## 2026-09-08 (nuit, fin) — `/simplify` sur la journée entière : neuf duplications refermées, et un contrôle rouge que personne n'avait vu

Revue qualité des quinze commits du jour (`ce7d21c..HEAD`, 157 fichiers, 14 884 insertions), sur
les quatre angles de `/simplify` — réutilisation, simplification, efficacité, altitude. Pas de
chasse aux bugs : c'est `/code-review` qui fait ça.

### Deux corrections de cadrage, avant même de commencer

La consigne annonçait « 12 commits, ~102 fichiers, ~9600 insertions **+ tout le non committé —
la spec 30 et son implémentation, le gros morceau** ». Mesuré : `git status` **vide**, aucun stash.
Les trois commits de 21:24 portent la spec 30 et son code. Le périmètre réel était donc **15
commits et 14 884 insertions**, entièrement committés — plus large que ce qu'on croyait relire, et
sans la fenêtre « avant commit » qu'on pensait avoir.

Et le plancher de tests annoncé (964) était celui de la **fin de la spec 29**. La spec 30 l'avait
monté à 1033 — vérifié par exécution avant de toucher à quoi que ce soit, et c'est ce chiffre-là
qui a servi de plancher.

### Le dispositif a cédé en cours de route, et il faut le dire

Treize chercheurs puis un réfuteur par trouvaille, sur le modèle qui a marché ce matin. **Cinq
chercheurs sur treize ont abouti ; les trente autres agents sont morts sur la limite de session**
— dont la TOTALITÉ des réfuteurs. Le workflow a rendu « 0 confirmée, 0 réfutée » parce que les
trouvailles sans verdict sont filtrées : le résultat avait l'air d'une zone propre, il n'était que
le symptôme d'une panne. Les 22 trouvailles produites ont été relues dans `journal.jsonl`.

⚠️ **Conséquence assumée : aucune trouvaille de ce lot n'a été réfutée par un agent adversarial.**
Chacune a été vérifiée à la main, en la reproduisant — ce qui a d'ailleurs suffi à en tuer une
(voir plus bas). Mais le taux de faux positifs de 55-60 % mesuré ce matin n'a pas été payé par un
mécanisme ici : il a été payé par une relecture, qui est moins reproductible. Les huit zones dont
le chercheur est mort (les deux fiches, le code mort, `apps/admin`, l'efficacité, l'altitude, les
e2e, le SQL, l'accueil) ont été reprises à la main, mais moins systématiquement que les cinq autres.

### La trouvaille qui vaut le plus : le prix décidé à trois endroits, dans DEUX ordres opposés

`resolverPrecio` (fiche produit) et le ternaire d'`establecimiento.ts` (cartes de chambre) testent
`price_label` PUIS `price_cop`. `precioDe` de `buscar.ts` — toute carte de l'accueil et des
listings — testait **`precio_cop` d'abord**. Un produit portant les deux s'affichait donc
« 120.000 COP » sur l'accueil et « Consultar » sur sa propre fiche.

Trois faits qui font de cette divergence un cas d'école :

- **Elle existait déjà**, elle n'était pas un risque futur. Latente seulement parce qu'aucune ligne
  du seed ne porte les deux — et la contrainte `products_price_cop_required_unless_vitrine`
  n'interdit nullement de les porter : elle exige un `price_cop` sauf vitrine/evento, jamais
  l'absence de `price_label`.
- **Le commentaire d'`establecimiento.ts` disait « Même règle que partout »** — et c'était faux au
  moment où il a été écrit.
- **Aucun test ne l'aurait vue.** `buscar.test.ts` a bien un cas « distingue les quatre formes de
  prix » : il exerce montant seul, libellé seul, rien, et `desde`. Jamais les deux ensemble.

`resolverPrecio` vit maintenant dans `tipos.ts` — la feuille du graphe d'imports, donc atteignable
par les trois sans cycle — et l'assertion manquante existe. ⚠️ **Vérifiée par mutation** : inverser
l'ordre fait rougir ce test, et lui seul (1 échec sur 626).

### Huit autres duplications, toutes du même jour

- **`migasParaJsonLd` avait été extrait pour supprimer une copie… qui était toujours là.** Son
  propre commentaire dit « le mapping *était* recopié dans `ListadoTipo.tsx` ». Il l'était encore,
  et l'index de catégories en avait fait une quatrième — sous la forme la plus dangereuse :
  **deux listes parallèles**, une pour le JSON-LD et une pour le fil visible, sous un commentaire
  affirmant « ils sortent de la même liste ». Ajouter un niveau à l'une seulement passait tout.
- **Un second écrivain d'URL** dans `ListadoTipo.tsx` : six `if` sérialisant les critères pour le
  pont, alors que `criterios.ts` s'annonce comme le seul endroit qui connaît le nom des paramètres
  — et que le pont, lui, les relit bien par `leerCriterios`. La copie avait **déjà perdu une règle**,
  la garde `personas >= 1`.
- **`hasUnavailableNightInRange` recopiait `plazasRestantes`** (`capacity - booked - panier`), dans
  le dossier créé le matin même pour que cette règle n'existe qu'une fois. C'est exactement le mode
  d'échec annoncé : une extraction faite fichier par fichier fige les ressemblances.
- **Le prédicat « toute la journée est complète » était écrit deux fois** dans `SlotReservationForm`,
  plus une troisième formulation du même « complet » vingt lignes plus bas. Une date pouvait devenir
  barrée-mais-cliquable sans qu'aucun test ne bouge.
- **`Math.max(reste, 1)` en dur dans les trois formulaires**, alors que `cantidad.ts` porte cette
  règle avec le commentaire qui l'explique. → `topeCantidad`.
- **`generateMetadata` de l'index recopiait `metadataListado("activity", …)` ligne pour ligne** —
  17 lignes, et la copie avait perdu la note qui explique que le canonical ignore `?pagina=`.
- **Le bloc `labels` du panneau de recherche, 24 lignes, écrit TROIS fois** (deux copies
  strictement identiques au `md5`). ⚠️ Et le typecheck ne protégeait pas : `valueLabel` est
  OPTIONNEL dans `SearchPanelLabels`, donc un libellé facultatif ajouté sur un seul écran compile.
  → `labelsBuscador(locale)`.
- **Six clés i18n mortes** (`fromPrice`, `dormKind`, `privateKind`, `roomsQtyLabel`, `bedsQtyLabel`,
  `establishmentTitle`) — exactement ce que la Tranche 4 a retiré des cartes de chambre. Vérifié
  qu'aucune n'est atteinte par une clé construite dynamiquement. `fromPrice` était de surcroît un
  doublon de `HomePage.precioDesde`, qui est la clé vivante.

### Le contrôle rouge que la journée n'avait pas vu

⚠️ **`npm run check:instructions` échouait**, et il échouait AVANT que je touche à quoi que ce
soit : `docs/backlog.md` faisait 67 lignes pour un plafond de 60. Chaque lot du 2026-09-08 a
annoncé « six contrôles verts » — c'était exact, mais ce **septième** n'est pas dans les six, et
il a débordé pendant la journée sans que personne le nomme. Le hook `PostToolUse` refusait d'ailleurs
toute édition tant qu'il était rouge, ce qui est le bon comportement.

Refermé comme le fichier le prescrit lui-même : un GROUPE entier part. Les deux blocs de dette
(technique, QA/UI mineure) vivent maintenant dans `docs/dette-technique.md` — **rien n'a été élagué**,
tout est déplacé au complet, et le backlog est à 58 lignes.

### Trois commentaires devenus faux, dont un qui portait une décision SEO

- ⚠️ **`lib/seo/jsonld/site.ts`** justifiait l'absence de `potentialAction`/`SearchAction` par :
  « la recherche du catalogue est un filtre en mémoire côté client (`CatalogBrowser.tsx`), sans URL
  de résultats adressable ». **Les deux prémisses sont mortes hier** : `CatalogBrowser` est supprimé,
  et les critères vivent dans l'URL depuis la spec 28. Le nœud reste sans `SearchAction` par
  non-décision, pas par choix — c'est écrit, et porté au backlog.
- **`check-data-layer.sh` affirmait que la couche porte `import "server-only"`.** Elle ne le porte
  pas : le paquet n'est ni installé ni déclaré nulle part, `buscar.ts` le dit franchement depuis le
  2026-09-07, et **trois specs le décrivent comme acquis**. Un garde-fou qui annonce une protection
  inexistante est pire qu'un garde-fou absent — son en-tête dit maintenant ce qu'il couvre vraiment,
  et surtout ce qu'il ne verra jamais : un composant CLIENT important `lib/catalog/`.
- `Image.tsx` parlait au présent de « ses deux appelants actuels », tous deux supprimés hier.

### Ce que la revue a trouvé et que je n'ai PAS corrigé

⚠️ **La spec 30 n'est pas intégralement livrée, contrairement à ce que dit l'entrée de la
Tranche 4.** Une migration de 313 lignes (`search_catalog_n_alojamientos`) calcule et publie
`n_alojamientos` ; `buscar.ts` le mappe avec un commentaire soigné ; le type `TarjetaOferta` le
porte ; quatorze fixtures le posent à `null` — et **`TarjetaOferta.tsx` ne l'affiche nulle part**.
La spec le nomme pourtant dans ses fichiers touchés (§0, l. 280) et son tableau §7c dit
« gagne `nAlojamientos` ». Ce n'est pas du code mort à retirer : c'est la moitié visible d'une
fonctionnalité, à finir. Non fait ici parce qu'afficher un décompte demande des libellés pluriels
es/en à écrire — un changement de comportement, pas un nettoyage.

### La trouvaille que la vérification a tuée

Un chercheur a signalé que `ListadoInfinito` et `IndiceCategorias` décident eux-mêmes du LCP
(`prioridad={index === 0}` en dur) là où `SeccionOfertas` reçoit la décision en prop. **Réfutée à
la lecture** : l'accueil rend PLUSIEURS sections dont une seule doit porter le LCP — d'où la prop ;
les deux autres organisms sont toujours seuls sur leur page, donc « la première carte » EST le LCP
sans ambiguïté. Leur ajouter la prop serait de l'anticipation, que le README interdit.

### Vérifié

typecheck 0 · lint 0 · **1037 tests Vitest** (142 admin + 629 web + 266 domain, contre 1033 au
départ — **+4, jamais moins**) · les six contrôles de la vitrine verts **et `check:instructions`
repassé au vert** · `next build` vert sur les deux apps · `docs:check` vert (44 documents).

Deux vérifications par MUTATION plutôt que par observation : inverser l'ordre de `resolverPrecio`
fait rougir exactement le test ajouté ; réintroduire un `.from()` dans une route de listing fait
toujours rougir `check-data-layer.sh` après réécriture de son en-tête. Les deux mutations ont vécu
le temps d'une commande, et `git status` a été vérifié vide après restauration.

⚠️ **Non vérifié** : aucun e2e n'a été lancé (aucun fichier e2e n'est touché par ce lot, et la
suite demande une base locale démarrée). Le décompte pgTAP n'a pas bougé non plus — aucune
migration n'a été écrite, comme il se doit.

### Ce que ce lot ne prouve pas

Les huit zones dont le chercheur est mort n'ont pas reçu la même profondeur que les cinq autres.
Le code mort a été balayé à la main sur ses quatre axes (exports orphelins, clés i18n, `data-testid`,
résidus de renommage) ; l'efficacité, l'altitude, le SQL, les e2e et `apps/admin` ne l'ont été que
par échantillon. **Ce lot n'est donc pas une revue exhaustive de la journée** — dire le contraire
reproduirait exactement l'erreur que le journal du matin dénonce.

## 2026-09-09 — `/simplify`, seconde vague : les trois zones que la panne avait laissées, et 38 % de réfutation

Reprise ciblée des zones dont le chercheur était mort la veille sur la limite de session. **Trois
zones sur les huit, pas huit** — le tri est assumé : le SQL est append-only (une trouvaille n'y
produit qu'une proposition de migration, jamais un correctif) ; les e2e sont des garde-fous que la
règle interdit d'alléger ; l'efficacité et l'altitude transverses avaient déjà donné leurs cas
concrets. Restaient les trois à vraie densité : les deux fiches (le gros morceau du jour, et dans la
MÊME app — la règle de duplication inter-apps ne les protège pas), `apps/admin`, et l'accueil.

**16 agents sur 16, 13 trouvailles, 8 confirmées, 5 réfutées, aucune perdue.** 38 % de réfutation
contre 55-60 % aux deux audits précédents : les prompts portaient cette fois les quatre règles du
dépôt en dur, et les chercheurs ont eu la liste de ce qui était DÉJÀ corrigé le soir même.

⚠️ **Un défaut du dispositif corrigé, et c'est lui qui avait masqué la panne de la veille** : le
post-traitement JETAIT les trouvailles dont le réfuteur était mort (`verdict` nul), si bien qu'un
workflow ayant produit 22 trouvailles rendait « 0 confirmée, 0 réfutée ». Une panne y ressemblait
trait pour trait à une zone propre. Elles remontent désormais à part, marquées non réfutées.

### Ce que la vague a trouvé, et ce qui a été corrigé

- ⚠️ **`FichaProducto.tsx` se contredisait sur 64 lignes.** L'en-tête annonçait « le lien retour au
  catalogue a DISPARU : le fil d'Ariane le remplace » ; le corps le rend, sous douze lignes
  intitulées « CE LIEN N'EST PAS UN DOUBLON DU FIL D'ARIANE ». **Les deux blocs sont nés dans le
  même commit** (`4a4118f`) : l'en-tête a été écrit avant la mesure qui a fait revenir le lien, et
  jamais repris. Dans un dépôt où le commentaire est le livrable, c'est le pire cas — le lecteur du
  point d'entrée croit le lien parti. ⚠️ Le réfuteur a corrigé le CORRECTIF, et il avait raison :
  réécrire l'en-tête avec la raison complète aurait créé une seconde copie du raisonnement, libre de
  diverger à son tour. L'en-tête est donc un RENVOI ; la raison reste écrite une seule fois.
- **`hayCriterios` : « l'accueil s'en sert pour choisir son état vide »** — l'accueil est le seul
  écran qui ne l'appelle pas (ce sont les listings et l'index). Il n'a qu'un état vide, puisqu'on
  n'y arrive jamais sans chercher.
- **La réserve des suggestions codait « cinq sections » en dur** sous un commentaire qui énonçait
  pourtant la règle (« cinq sections, donc cinq fois la réserve ») et citait
  `ORDEN_SECCIONES.length`, que `buscar.ts` applique. Un sixième type d'offre aurait élargi la
  recherche de l'accueil et laissé les suggestions trop courtes — en silence, et seulement quand la
  dernière section aurait été celle qu'on cherchait. Dérivé.
- **Le même maillon de fil d'Ariane, deux façons** : la fiche produit passe par `segmentoDeTipo`,
  la fiche établissement écrivait `/alojamientos` en dur. `segmentos.ts` se déclare « SEULE source
  de vérité » — un segment renommé là-bas n'aurait été suivi que par une des deux fiches.
- **`export { SEGMENTO_POR_TIPO }` n'avait aucun consommateur** dans tout le dépôt. La constante
  reste, interne au module.

### Deux trouvailles vraies laissées de côté, et pourquoi

- ⚠️ **La décision « vitrine » n'est pas posée sur la création directe admin.** `product-form.tsx`
  gère `enVitrina` à la MISE À JOUR (l. 363, `price_label` pour tout type porteur d'une URL) mais
  pas à la CRÉATION (l. 447-451, où `price_label` n'est posé que si `isEvento`). Créer un transport
  en vitrine depuis l'admin perd donc son libellé de prix ; le modifier ensuite le rend. C'est
  **fonctionnel, pas du nettoyage** — même famille que le décompte de couchages. Porté au backlog.
- Le suffixe d'unité de prix (« por persona ») est décidé sur le MODE DE RÉSERVATION alors que
  c'est une propriété de la FORME du prix. Réel mais **inatteignable aujourd'hui** (aucune ligne ne
  produit l'état), et le corriger sans test qui l'exerce déplacerait une règle d'affichage sans
  filet. Signalé, non fait.

### Ce que les réfuteurs ont tué, et qui méritait de l'être

Cinq trouvailles, dont trois instructives : le bloc d'amorçage jsdom de react-aria recopié dans huit
fichiers de test — **la raison est écrite dans trois d'entre eux et refuse nommément ce correctif** ;
un test des suggestions accusé de ne pas pouvoir rougir — le réfuteur a **exécuté** la suite et
montré l'inverse ; et `limpiarDescripcion` accusé de réimplémenter `buildLocalizedPayload`, dont le
prédicat est équivalent mais l'usage non.

### ⚠️ Un piège d'outillage, trouvé en réparant la CI

**`docs:check` passe au ROUGE juste après un commit qui touche un document**, alors qu'il était vert
juste avant. `scripts/docs_index.js` date chaque document par `git log -1 -- <fichier>` : committer
change cette date, donc périme le manifeste que le commit vient d'inclure. La séquence correcte est
donc `commit` → `npm run docs:index` → `git commit --amend`, jamais l'inverse — et c'est
contre-intuitif, puisqu'on régénère naturellement AVANT de committer.

C'est le pendant du piège déjà documenté en tête du job `lint` (`fetch-depth: 0`, 17 runs rouges
d'affilée en septembre) : les deux viennent de ce que l'index dépend de l'historique git, pas
seulement du contenu des fichiers.

### Vérifié

typecheck 0 · lint 0 · **1040 tests Vitest** · les six contrôles de la vitrine verts, plus
`check:instructions` · `next build` vert sur les deux apps.

⚠️ **Cette mesure ne décrit PAS ce seul lot.** Une autre session a travaillé en parallèle dans le
même arbre (le décompte de couchages, commit `4be2b05`) : sur les 1040, trois tests sont les siens.
Elle a committé proprement ses cinq fichiers seuls — le risque du `git add -A` qui emporte le lot du
voisin ne s'est pas réalisé, mais il était réel tant que les deux lots cohabitaient non committés.

### Ce que la CI a dit pendant ce lot, et qui confirme la trouvaille du matin

`hifago-ci / lint` est **rouge sur `main`**, et la cause est exactement le septième contrôle trouvé
la veille : le job lance `npm run check:instructions` (l. 80 de `hifago-ci.yml`), et `docs/backlog.md`
fait 67 lignes sur le HEAD poussé. Il était donc rouge AVANT ce chantier de simplification — chaque
lot du 2026-09-08 a annoncé « six contrôles verts » sans que celui-là, qui n'est pas dans les six,
soit regardé. Le correctif était écrit et non committé ; c'est ce commit-ci qui le livre.

## 2026-09-09 — `/next` : le décompte de couchages refermé au backlog, et la marge de 60 lignes

Point de situation demandé après la spec 30. Rien construit : une vérification et une ligne retirée.

**Refermé** — « la spec 30 n'est PAS intégralement livrée : le décompte de couchages n'est affiché
nulle part ». Vérifié dans le code avant de retirer la ligne, jamais sur la foi du message de
commit : `TarjetaOferta.tsx` l. 146 rend `conteoAlojamientos`, et la clé existe dans les deux
locales avec son pluriel ICU (`{count, plural, one {# alojamiento} other {# alojamientos}}` /
`{# stay} / {# stays}`). Commit `4be2b05`, écrit par la session parallèle.

⚠️ **Le backlog passe de 60 à 59 lignes, et ce n'est pas cosmétique** : `check:instructions` refuse
au-delà de 60, et c'est ce contrôle qui tenait `hifago-ci / lint` rouge sur `main` (67 lignes au
HEAD poussé, cf. l'entrée précédente). La marge est donc d'UNE ligne. Le prochain point ouvert
ajouté la fait repasser rouge — le signal que la règle attend depuis le début : un groupe entier
doit partir en spec plutôt que de s'accumuler ici.

**Prochaine action donnée à Jérôme** : passer `cosmogab` en Admin sur `casakayam/hifago-2.0` pour
débloquer la GitHub App Vercel. Raison du choix : les cinq écrans publics sont finis et n'ont
jamais été vus ailleurs que sur `localhost` ; le prochain lot (tunnel) se construit par-dessus.

**Cadré, non appliqué** — la création admin directe d'un produit vitrine perd `external_booking_url`
ET `price_label` : `product-form.tsx` l. 436 construit son `.insert()` en ligne, avec ces deux clés
enfermées dans la branche `isEvento`, alors que le parcours socio passe par
`buildProductCreationPayload` qui les pose pour tous les types. Deux chemins de création divergents.
⚠️ Le correctif n'est PAS de recopier les deux clés dans l'insert — c'est de le faire passer par le
même constructeur. Recopier fige la divergence au lieu de la fermer, et c'est elle le défaut.

## 2026-09-09 — Corpus d'instructions dégraissé, et la suite e2e mise en pause

**Point de départ, une question de Jérôme** : « 43 % de quota consommés en moins d'une heure en
`high` sur Opus, sans conversation en parallèle — comment est-ce possible ? » La réponse n'est pas
la durée : à chaque appel d'outil, tout le contexte accumulé est renvoyé. Le coût par tour croît
avec la session, et le plancher de contexte est payé à CHAQUE tour.

**Le plancher mesuré** (ce qui part à chaque appel, avant la moindre ligne de travail) :

| Source | Avant | Après |
|---|---|---|
| `CLAUDE.md` | 15 377 o (199 l.) | **13 400 o (178 l.)** |
| `.claude/rules/orchestration.md` (`paths: **/*`, donc toujours chargée) | 5 648 o (89 l.) | **3 190 o (49 l.)** |
| 56 skills gstack globaux (`~/.claude/skills/`, hors dépôt) | ~8 100 o ≈ 2 000 tokens | inchangé — décision Jérôme |
| `AGENTS.md` du dépôt legacy parent | 1 904 o | inchangé — autre dépôt |

Gain : **−4 435 o par tour**, soit ≈ 1 100 tokens à chaque appel d'outil.

**Deux gestes, et pourquoi ceux-là.** `orchestration.md` est chargée sur `**/*` : c'est du CLAUDE.md
qui ne dit pas son nom. 40 de ses 89 lignes étaient du récit rétrospectif — il descend ici (ci-
dessous), la prescription reste. `CLAUDE.md` §11 était un index de 31 lignes dont chaque entrée ne
faisait que renvoyer vers une règle `.claude/rules/*` **déjà chargée automatiquement par chemin** :
il payait à chaque tour pour désigner ce qui arrive tout seul.

**Le piège du déport, et sa réponse.** Sortir §11 casse ~90 citations `CLAUDE.md §11.N` (code,
migrations, `docs/specs/`, journal) — et le journal est append-only, donc les réécrire par `sed`
n'était pas une option. Choix retenu : `CLAUDE.md` §11 reste le nom canonique et déclare son
déport ; les points vivent dans `docs/pieges-empiriques.md`, numérotation figée. `check-instructions.mjs`
sait désormais suivre cette délégation — une section titrée « index déporté » résout ses sous-points
contre le premier `.md` cité dans son corps, et signale si le fichier manque ou ne porte aucun point
numéroté. **Vérifié par trois mutations** (retirer le piège 16 → les 37 citations rougissent ;
supprimer le fichier → rouge ; dénuméroter ses lignes → rouge ; restauration → vert) : la
délégation ne désarme pas le contrôle, ce qui était le seul vrai risque du geste.

**Ce que j'ai volontairement NON fait**, et pourquoi : (a) §7.4 (pièges Vercel) — le déplacer vers
une règle par chemin raterait sa cible, c'est un piège de COMMANDE (`vercel deploy`), pas de
fichier : aucune ouverture de fichier ne le déclencherait au moment utile ; le compacter sur place
gagnait 3 lignes contre un risque réel de perdre un incident payé cher. (b) Les 56 skills gstack —
hors dépôt, ils affectent tous les autres projets de Jérôme ; c'est sa décision, pas la mienne.
(c) L'`AGENTS.md` du parent — autre dépôt git.

### Les tests : la lenteur n'était pas le problème

Chronométré ce jour, stack locale : pgTAP **6 s** (52 fichiers, 876 assertions) · Vitest **40 s**
(95 fichiers, 898 tests) · e2e web **88 s** (14 specs, 38 tests) · e2e admin **3,2 min** (47 specs,
82 tests). La suite complète tient en ~6 min : elle n'est pas lente. Ce qui coûtait, c'est
**19 rouges sur 120** (17/82 admin, 2/38 web) qu'aucun job de CI ne surveille — `hifago-ci.yml`
n'a jamais eu de job e2e. Chaque feature repayait donc le diagnostic d'un mur de rouge dont rien
ne disait ce qui venait d'elle.

⚠️ Nuance à ne pas perdre : les 17 admin **n'étaient pas une découverte** — `docs/backlog.md` les
portait déjà, mesurées le 2026-09-08 (16 des 18 échouaient aussi en `--workers=1`, donc ni
parallélisme ni pollution). En revanche `attribution.spec.ts` (web) passe SEUL et échoue en suite :
celui-là est bien de la pollution. Et `reserve-lodging-pms-availability.spec.ts` est rouge même
isolé.

**Décision de Jérôme : la suite e2e est mise en pause, et les rouges PMS ne sont PAS réparées.**
Elle ne tourne plus dans `/hifago-test` par défaut (le skill le dit à trois endroits) ; la CI n'en
avait déjà pas ; `npm run test:e2e` reste lançable à la main. **Aucun fichier de spec n'a été
`.skip`é ni supprimé** — le geste est réversible, et le point de réactivation est en tête du
backlog. ⚠️ Quand elles reviendront : `npm run db:setup` AVANT toute mesure pgTAP, puisque tout e2e
admin écrit dans `audit_log` que six fichiers pgTAP comptent en absolu.

### Le récit retiré d'`orchestration.md` (mesuré les 2026-09-07/08)

**170 agents** lancés en sept workflows sur une session. Les deux gros audits ont tué **60 % de
leurs propres trouvailles** à la vérification (15 confirmées sur 40, puis 14 sur 24) : le mécanisme
adversarial fonctionne, mais une bonne part du budget sert à fabriquer du bruit puis à le détruire.
La construction de l'accueil à **7 agents** (3,2 Mo de transcript) écrivait du code dont *tous* les
contrats de props étaient déjà figés — les agents exécutaient, ils ne décidaient rien — et **les
deux vrais défauts du lot ont été trouvés après coup, pas par eux** (le pluriel `valueLabel` qui ne
pouvait pas venir de la page ; la resynchronisation du panneau sur l'URL qu'exigeait la spec). À
l'inverse, la **reconnaissance à 5 agents** (1,4 Mo, le plus petit budget de la journée) a trouvé
trois choses qui ont changé le plan avant la première ligne de code : aucune migration nécessaire,
deux modules déjà écrits, un sixième fichier e2e concerné dans l'autre app.

Sur la Tranche 1a de la spec 29 — une migration republiant une fonction de 300 lignes, faite à
`xhigh` —, **l'effort n'a rien trouvé**. Les défauts ont été attrapés par des méthodes : la règle
posée AVANT d'écrire (« les 16 assertions existantes restent vertes sans être modifiées »), les
mutations exécutées contre la vraie base, l'impression du rendu réel au lieu de sa supposition
(HeroUI rendait un `<ol>` sans `<nav>`), et le lint qui a refusé tout seul une écriture de ref
pendant le rendu. Le rendement marginal de l'effort est faible sur du code dont les contrats sont
fixés ; celui de la vérification est élevé à tous les niveaux.

Enfin, sur les cinq niveaux d'effort, deux sont utilisés en pratique, pour une raison mécanique :
**changer d'effort demande une action de Jérôme** (`/effort`), donc personne ne bascule six fois
dans une session — on choisit un niveau en début de bloc et on y reste.

## 2026-09-09 — Entretien « identité anonyme » ouvert, et la création admin d'une vitrine qui échouait en silence

### L'entretien : deux points tranchés, dont une révision du 2026-09-07

Ouverture de l'entretien de la spec « identité anonyme », celle qui bloque le panier en base, puis
le tunnel, puis les deux derniers écrans hérités. Deux questions posées, deux réponses.

**① Le geste déclencheur : le premier ajout au panier, et RIEN d'autre.** C'est une **révision** de
la décision du 2026-09-07, qui disait « ajout au panier **ou** arrivée par `?ref=` ». Le fait
nouveau qui l'a rouverte : `?ref=` n'est pas un geste, c'est un chargement de page — et le trigger
`on_auth_user_created` (`20260813163438_identity_account_provisioning.sql`) insère une ligne
`partner_accounts` pour TOUT nouvel `auth.users`. Chaque scan de QR curieux aurait donc créé un
compte partenaire fantôme. Le coût assumé de la révision, dit à Jérôme avant son choix : entre le
scan et le premier ajout, l'attribution reste portée par le cookie de session, donc toujours perdue
si l'onglet se ferme d'ici là — soit exactement le comportement d'aujourd'hui, ni pire ni meilleur.

**② L'attribution vit sur le panier en base, pas sur l'identité.** Le code référent est recopié du
cookie `hifago_ref` vers le panier au moment où celui-ci naît en base. C'est littéralement « la
réservation en cours » du cahier §3c (validé le 2026-08-13), qui interdit une préférence durable
pour un invité — `partner_accounts.saved_attribution_code` reste réservé aux comptes enregistrés.
Panier commandé ou vidé → l'attribution repart de zéro. Le cookie ne sert plus qu'entre le scan et
le premier ajout. Écartée en connaissance de cause : une colonne dédiée sur l'identité anonyme,
plus simple à lire mais qui aurait attribué des commandes des mois après un scan.

⚠️ Le §3c répondait déjà à deux questions qu'il aurait été tentant de reposer : **le dernier code
présenté prime**, et une commande passée n'est jamais réattribuée rétroactivement. Non rouvertes.

Restent à trancher avant d'écrire la spec, dans cet ordre : le périmètre `authenticated` (79
fichiers de migration, 67 policies, `is_anonymous` nulle part) ; le sort de la ligne
`partner_accounts` d'un anonyme ; la conversion d'identité quand l'invité pose son email ; le
nettoyage des anonymes abandonnés.

### Le correctif : créer une vitrine non-evento depuis l'admin était impossible

Défaut trouvé en cadrant le correctif annoncé par `/next`, et plus grave que « une duplication à
refermer ». Il y a en réalité **deux défauts distincts** sur le même écran, et le premier jet de ce
lot les avait confondus — c'est la vérification adversariale lancée après coup (30 agents, 18 écarts
confirmés dont 16 sans conséquence) qui a séparé les deux. À écrire une fois, correctement :

**Défaut 1 — l'URL externe perdue en silence.** Un admin qui créait directement une vitrine sur un
NON-evento (activité, alojamiento, transport, camp) avec un prix chiffré saisissait bien son URL,
mais l'insert écrit à la main n'emportait `external_booking_url`/`price_label` que dans son bloc
`isEvento` : le produit se créait **sans son URL, sans la moindre erreur**. Destructeur et muet.
C'est celui que ce lot referme.

**Défaut 2 — la vitrine sans prix, toujours impossible.** Si le prix est laissé VIDE (le cas que la
migration `20260908200000_products_vitrine_sin_precio.sql` était justement censée ouvrir) :
`enVitrina` vrai ⇒ `needsOwnPrice` faux, la validation JS laisse passer ; le `<form>` porte
`noValidate` (l. 534), donc rien ne bloque non plus côté navigateur ; mais `fields.priceCop` vaut
`""` et **`Number("") = 0`**, refusé par `products_price_cop_positive` (`check (price_cop > 0)`,
20260813233000). L'écran n'affiche qu'un « No se pudo crear la actividad. » qui ne nomme aucun
champ. **Ce défaut-là n'est PAS corrigé par ce lot** : il vit dans `buildProductCreationPayload`
lui-même, donc le corriger changerait aussi la proposition socio, et il frappe une troisième fois
dans l'`update` d'édition (passer un produit existant en vitrine en effaçant son prix échoue
pareil). Trois chemins, un seul geste — hors périmètre du correctif demandé, ouvert au backlog.

Le chemin socio (proposition) et l'édition, eux, marchaient — les deux passent par un payload
construit ailleurs. C'est ce qui rendait le défaut invisible : seule la création admin-directe
réécrivait ses colonnes à la main. La migration `20260908200000_products_vitrine_sin_precio.sql`
avait pourtant averti en toutes lettres qu'il resterait un geste à faire côté écran ; le geste fait
la veille a déverrouillé le champ dans `product-type-fields.tsx` sans que l'insert suive.

**Correctif** : les 59 lignes d'insert manuel de `product-form.tsx` appellent désormais
`buildProductCreationPayload`, moins les trois clés qui ne sont pas des colonnes de `products`
(`photos`/`tag_ids`/`slot_rules`, rattachées après l'insert par ce chemin-ci). Les quatre clés
d'identité (`partner_id`, `establishment_id`, `type`, `slug`) restent posées explicitement, APRÈS
le spread, donc jamais écrasables. Bilan : −59/+23. Le commentaire de tête de
`productCreationPayload.ts`, qui se décrivait comme « miroir exact » de l'insert d'en face, disait
désormais faux : il est corrigé dans le même geste — une seule définition, plus de miroir à tenir.

Équivalence vérifiée colonne par colonne sur les cinq types avant d'appliquer : toutes les colonnes
qui passent de « écrite explicitement à `null` » à « absente du payload » (`duration_days` partout
sauf camp, `price_cop` pour evento) sont `?` et nullable dans `TablesInsert<"products">`, et leurs
migrations ne posent aucun `default` — omettre équivaut donc bien à écrire `null`.

### Vérifié

`typecheck` et `lint` verts sur `apps/admin` (le lint a d'ailleurs refusé deux formes
intermédiaires : ni `ignoreRestSiblings` ni `varsIgnorePattern` ne sont actifs dans cette config,
d'où les trois `delete` explicites plutôt qu'une déstructuration `...rest`). 142 tests Vitest
passent, 15 fichiers.

**Équivalence auditée à 30 agents**, un par type de produit plus un sur les effets de bord, chaque
écart soumis à un réfuteur : 24 écarts examinés, 18 confirmés, dont **16 sans aucune conséquence**
(colonnes passant de `null` explicite à absentes — défaut NULL vérifié en base pour chacune) et 2
qui pointent le défaut 2 ci-dessus. Aucune régression introduite. Les réfuteurs ont par ailleurs
corrigé trois affirmations fausses de leurs propres finders (un mécanisme de validation attribué au
mauvais composant, un « aucun test ne couvre `external_booking_url` » démenti par
`admin-evento-vitrine.spec.ts:39`, et une portée annoncée trop étroite) — le rapport brut n'est pas
la vérité, la réfutation l'est.

⚠️ Fragilité laissée en place, sciemment : la liste des trois `delete` est manuelle et le cast
`as TablesInsert<"products">` désactive l'excess property check de TypeScript. Si le builder gagne
un jour une quatrième clé hors-colonne, elle partira dans l'insert et PostgREST répondra une erreur
que rien ne préviendra à la compilation.

### Non fait, assumé

**Aucun test ne couvre ce correctif.** Ni `buildProductCreationPayload` ni la création admin n'ont
de test unitaire ; la couverture réelle est `apps/admin/e2e/admin-product-create.spec.ts`, et la
suite e2e est en pause depuis la décision de Jérôme du même jour — elle n'a pas été lancée. Au sens
de `CLAUDE.md` §11.20, ce correctif est pour l'instant un souhait : rien ne l'empêche de régresser.
La seule spec qui touche une vitrine, `admin-evento-vitrine.spec.ts`, ne teste QUE l'evento — le cas
non-evento, celui du défaut 1, n'a jamais eu de test.

**Le défaut 2 n'est pas corrigé**, sur décision de périmètre et non par oubli : le correctif
(`price_cop: null` quand aucun prix n'est saisi, dans les branches `hasPriceQtyFields` et `isCamp`
du builder, plus le même geste dans l'`update`) changerait le comportement de la proposition socio,
que Jérôme n'a pas demandé de toucher. Signalé, jamais corrigé en silence (avant-la-spec §7).
Aucune ligne de code écrite pour l'identité anonyme — l'entretien continue.

### Suite du même jour — le défaut 2 refermé côté admin, et le mur qui reste côté socio

Jérôme : « on est sur l'implémentation ». Le défaut 2 ci-dessus est donc traité dans la foulée, et
le raisonnement de périmètre qui l'avait mis en attente était mauvais : ce n'était pas « étendre au
socio un correctif admin », c'était **le même bug aux trois endroits**, dont un déjà cassé.

**La cause exacte, plus fine que « prix vide »** : le chemin d'édition avait DÉJÀ une garde,
`Number.isFinite(price) ? price : null`, et son commentaire annonçait « null plutôt que NaN quand le
prix est vide ». Elle n'a jamais rien gardé : **`Number("")` vaut 0, pas NaN**. La garde laissait
donc passer 0, refusé par `products_price_cop_positive`. Correctif : tester `> 0`, pas `isFinite` —
dans `buildProductCreationPayload` (branches `hasPriceQtyFields` ET `isCamp`, via un unique
`priceCopOuNull`) et dans l'`update` d'édition. Seul un prix strictement positif est un prix ; le
reste vaut `null`, et c'est `products_price_cop_required_unless_vitrine` qui tranche ensuite.

**Vérifié contre la base locale, pas déduit** (deux inserts en transaction annulée) :
`price_cop = null` + `external_booking_url` → `INSERT 0 1` ; `price_cop = 0` + même URL →
`ERROR: violates check constraint "products_price_cop_positive"`. Le cas s'ouvre, l'ancien reste
refusé. Lint, typecheck et 142 tests Vitest verts.

**⚠️ Ce qui NE suffit pas, et c'est une découverte du lot** : le chemin SOCIO reste fermé, et aucun
correctif TypeScript ne peut l'ouvrir. La whitelist de `submit_product_creation_proposal`
(20260827120000, l. 152-164) ne laisse passer `external_booking_url`/`price_label` que pour
`p_type = 'evento'` — pour tout autre type, les deux clés sont **silencieusement jetées**, comme le
commentaire de la fonction le dit lui-même de toute clé absente. Un socio qui propose une vitrine
non-evento voit donc son URL disparaître entre son formulaire et la proposition enregistrée ; à la
modération, l'insert n'a ni prix ni URL et viole `products_price_cop_required_unless_vitrine`. Le
correctif est une **migration** (élargir la whitelist aux non-eventos, dans les deux RPC de
proposition comme l'avaient exigé `unit_count` et `lodging_kind` avant elle) — pas fait, ouvert au
backlog, pas engagé sans arbitrage puisque c'est du SQL et un changement de contrat socio.

Bilan honnête du lot : la vitrine non-evento est créable et modifiable **par un admin**, pas encore
proposable **par un socio**.

### La migration : la vitrine socio ouverte, et un piège de conception évité de justesse

Go de Jérôme sur la migration. Elle s'est révélée plus large que « élargir une whitelist » : **trois
fonctions** se tenaient, et corriger une seule des trois n'aurait rien rendu fonctionnel.

1. `submit_product_creation_proposal` ne whitelistait `external_booking_url`/`price_label` que pour
   `evento` — hors evento, les clés étaient jetées **sans erreur**, c'est le rôle documenté de cette
   whitelist. L'URL du socio disparaissait entre son formulaire et la proposition.
2. `submit_product_proposal` (modification) ne les whitelistait pour **aucun** type, evento compris.
3. `moderate_product_proposal` ne les écrivait dans son UPDATE nulle part : même une proposition qui
   les aurait portées les aurait perdues à l'approbation.

Migration `20260909180000_proposal_vitrine_non_evento.sql`, 423 lignes — les trois corps sont repris
de `pg_get_functiondef` **sur la base locale**, donc de ce qui tourne réellement, pas recopiés d'une
migration antérieure ; seules les lignes commentées « 2026-09-09 » diffèrent. Aucune table, aucune
policy, aucun grant touché ; aucune opération critique au sens du §4.1, donc pas de test de
concurrence. Deux pièges rencontrés, tous deux vérifiés et non devinés :

**Piège 1 — `pg_get_functiondef` ne termine pas par `;`.** Les trois définitions se collaient et la
migration échouait sur `syntax error at or near "CREATE"`, statement 0. Trivial une fois vu,
invisible avant.

**Piège 2, le vrai — l'UPDATE de `moderate_product_proposal` écrit ses colonnes
INCONDITIONNELLEMENT.** Écrire `external_booking_url = nullif(v_final_payload ->> '…', '')` comme
les lignes voisines aurait **effacé l'URL d'un produit** à l'approbation de toute proposition
`pending` créée avant la migration — leur payload ne porte pas la clé, et `->>` rend NULL aussi bien
pour « clé absente » que pour « clé présente à null ». D'où la forme `case when v_final_payload ?
'…' then … else <colonne> end`, qui distingue les deux. Les autres colonnes ont ce défaut ; il n'est
pas reproduit ici.

### Vérifié — en réel, pas déduit

Trois exécutions contre la base locale, chacune en transaction annulée, avec une **vraie session
socio fabriquée** (`set local role authenticated` + `request.jwt.claims`) :
- `submit_product_creation_proposal` sur une `activity` en vitrine → la proposition retient
  `https://wa.me/…` et `Consultar por WhatsApp`. Avant la migration : les deux clés jetées.
- approbation d'une proposition **sans** la clé → le nom est modifié et **l'URL préservée**.
- insert direct `price_cop = null` + URL → accepté ; `price_cop = 0` + URL → refusé par
  `products_price_cop_positive`. C'est la démonstration du correctif TypeScript du même jour.

**pgTAP : 878 tests, 52 fichiers, PASS.** Le lot a d'abord fait tomber `product_proposals.test.sql`
test 8 — la whitelist y est comparée **au champ près**, donc les deux clés nouvelles la faisaient
échouer : c'est le test qui devait suivre la migration, il a été mis à jour. Puis **deux assertions
ont été ajoutées** (plan 18 → 20) pour couvrir précisément le piège 2, clé absente vs clé présente à
null — sans elles, la décision de conception la plus délicate du lot n'aurait été vérifiée par rien
(§11.20). ⚠️ Les 9 autres rouges du premier passage n'étaient PAS de ce lot : c'est la dette connue
des comptages `audit_log` en absolu, refermée par `npm run db:setup`, comme le backlog le prescrit.

Côté TypeScript, `productEditPayload.ts` a été complété du même geste (sans lui la whitelist élargie
n'aurait rien reçu) : les deux clés de vitrine, et le même `Number("") = 0` que partout ailleurs —
c'était donc un **quatrième** chemin porteur du défaut, découvert en câblant la migration.

### Non fait, assumé

- **L'écriture inconditionnelle des autres colonnes de l'UPDATE reste un défaut**, et le test l'a
  prouvé au passage : un payload de proposition incomplet efface les colonnes absentes, jusqu'à
  violer une contrainte (`products_price_cop_required_unless_vitrine` s'est déclenchée en écrivant
  l'assertion). Pré-existant, hors périmètre, ouvert au backlog.
- L'écran admin d'**édition d'un evento** ne porte toujours pas `external_booking_url` (bloc
  `isEvento ? {} : …`) : un admin ne peut pas corriger l'URL d'un evento. La whitelist SQL l'accepte
  désormais pour tous les types, donc il ne reste que le geste d'écran. Signalé, non fait.
- Rien n'est committé.

## 2026-09-09 (suite) — Entretien identité anonyme : quatre points tranchés, et une faille de sécurité ACTUELLE trouvée en chemin

### Les décisions

③ **Refus par défaut, liste blanche explicite.** Une RPC n'accepte un visiteur anonyme que si elle
le dit nommément ; toutes les autres refusent. C'est l'échec fermé du §4.4 appliqué à l'identité :
une RPC écrite dans six mois sera fermée aux visiteurs tant que personne ne l'ouvre. L'alternative
(liste noire) a été écartée en connaissant son coût : chaque RPC future ouverte par défaut, donc
exactement le genre de règle que le §11.20 appelle un souhait.

④ **Le trigger `on_auth_user_created` reste inchangé** : un visiteur a sa ligne `partner_accounts`
comme tout compte. Cohérent avec l'existant — la table porte DÉJÀ 3 comptes non-partenaires sur 7
en local, son nom ment avant même les visiteurs. L'option « ne pas créer la ligne » a été écartée
sur un piège vérifié : `on_auth_user_created` est un trigger sur INSERT, or lier un email à un
utilisateur anonyme est un UPDATE — il aurait fallu un second trigger, sinon un invité converti
n'aurait jamais eu de ligne et aurait perdu ses accès en silence.

### Le fait qui a reformulé la question ③ avant de la poser

Le backlog et le journal du 2026-09-07 décrivaient le périmètre comme « 79 fichiers de migration,
67 policies ». **C'est faux, et de deux façons.** Mesuré ce jour :

1. **Aucune policy ne porte de clause `to authenticated`** — elles s'appliquent à tous les rôles et
   filtrent par `auth.uid()`. Il n'y a pas 67 policies à relire.
2. **Les 81 fonctions du schéma `public` exécutables par `authenticated` le sont TOUTES via
   `PUBLIC`** — donc aussi par le rôle `anon`, un visiteur sans aucune session. Les 106
   `grant execute … to authenticated` sont redondants : ils ne filtrent rien. **La sécurité repose
   entièrement sur les gardes internes de chaque fonction.**

Classement des 81 par garde : 40 exigent un rôle admin, 10 une capacité partenaire, **6 ne
vérifient que `auth.uid()` non nul**, 25 aucune identité (helpers). Ce sont ces 6 qui basculent le
jour où `auth.uid()` cesse de signifier « personne ayant vérifié son email ».

### ⚠️ `create_payment_intent` — une faille de sécurité ACTUELLE, sans rapport avec l'identité anonyme

Trouvée par un audit à 73 agents, puis **reproduite à la main contre la base locale**, parce qu'un
rapport d'agent n'est pas une preuve. La garde de la fonction est :

    if v_account_id is not null and v_order_account_id is not null
       and v_order_account_id is distinct from v_account_id then return 'order_not_found';

Elle n'est active que si **les deux** sont non nuls. Un appelant **sans aucune session** (rôle
`anon`, aucun JWT) la traverse. Résultat mesuré, sur la commande d'un autre compte :

    {"ok": true, "amount_cop": 45000, "payment_id": "7429fc84-…",
     "payer_email": "cliente.ledger.seed@test.local"}

Trois conséquences, toutes constatées : **fuite de PII** (l'email du titulaire est renvoyé à
l'appelant), **écriture** (une ligne `payments` 'pending' et `orders.payment_status` basculé sur la
commande d'autrui), et **déni de service** — l'idempotence `payment_already_pending` empêche ensuite
le vrai titulaire de créer son propre intent. Il faut connaître l'UUID de la commande, mais ces
UUID circulent (liens du tunnel, emails). ⚠️ Le seed ne permettait pas de le reproduire tel quel
(aucune commande n'a de ligne `reserved` avec acompte > 0) : l'état a été fabriqué en transaction
annulée. Le premier essai avait donné `nothing_to_pay`, ce qui aurait pu faire conclure à tort que
le rapport était faux.

hifago n'étant pas en production (le legacy l'est), il n'y a pas d'urgence opérationnelle — mais la
préprod est exposée, et le correctif n'est pas engagé sans arbitrage : il interagit avec la décision
① (aujourd'hui une commande invité a `account_id` null, demain elle portera l'uid anonyme, ce qui
change la forme exacte de la garde).

### ⚠️ `consume_partner_invitation` — une faille FUTURE, que la décision ③ doit couvrir

Reproduite à la main sous une vraie session anonyme (`is_anonymous: true`, aucun email) détenant un
lien d'invitation : retour `{"ok": true, "roles": ["referrer"]}`, capacité `referrer` **active**, et
`role_agreements` signés — un consentement contractuel posé par une identité jetable jamais
vérifiée. Sa garde entière est `if auth.uid() is null then return 'not_authenticated'`. Non
exploitable aujourd'hui (les sessions anonymes n'existent pas encore) ; elle le devient le jour de
la bascule. C'est le premier nom de la liste blanche à écrire.

### Non vérifié par moi, laissé tel quel dans le rapport

L'audit signale aussi des oracles de lecture (`has_capability`, `is_admin`, `has_admin_capability`,
`partner_id_for_account` prennent un uuid en PARAMÈTRE, jamais `auth.uid()`, et sont SECURITY
DEFINER — donc un appelant sans session teste l'identité d'autrui, 1 bit par uuid connu),
`establishment_slug_from_name` (oracle d'énumération de slugs masqués par la RLS), et deux chemins
plus exotiques (`enqueue_pms_cancellations`, `notify_admin_new_reconciliation_exception` détournée
via un trigger sur une table temporaire). **Je ne les ai pas reproduits** : ils sont rapportés comme
pistes, pas comme faits.

### `create_payment_intent` — la faille refermée le même jour

Go de Jérôme. **Le correctif consiste à RETIRER une condition**, pas à en ajouter :

    -  if v_account_id is not null and v_order_account_id is not null
    -     and v_order_account_id is distinct from v_account_id then
    +  if v_order_account_id is not null and v_order_account_id is distinct from v_account_id then

`v_account_id is not null and` **désarmait la garde exactement quand l'appelant n'avait pas de
session**. `is distinct from` traite déjà NULL correctement : appelant sans session sur une commande
qui a un propriétaire → distinct → refusé.

**Ce qui reste ouvert, délibérément** : une commande INVITÉ (`account_id` null) reste payable par le
porteur de son `order_id`. C'est le parcours invité d'aujourd'hui — vérifié avant de toucher à quoi
que ce soit : `CheckoutForm.tsx` appelle cette RPC **depuis le navigateur** avec le client Supabase
(qui porte la session quand le client est connecté), et `/api/payments/create` n'emballe que l'appel
Mercado Pago sans jamais appeler la RPC. Un correctif plus strict (exiger `auth.uid()`) aurait donc
cassé le paiement de tous les invités. L'identité anonyme refermera ce dernier cas d'elle-même :
toute commande portera alors un `account_id`.

**Pourquoi le test ne l'avait pas vu** : `payments.test.sql` cas 5 couvrait « un AUTRE compte
authentifié tente de payer la commande du buyer » — et passait, la garde fonctionnant quand les deux
uuid sont non nuls. Le cas « appelant SANS session » n'était testé nulle part. C'est le §11.20 en
situation : la règle était vérifiée pour une moitié de son domaine.

**Vérifié** : l'exploit rejoué contre la fonction corrigée renvoie `order_not_found`, zéro ligne
`payments`, aucun email divulgué. **pgTAP 879 tests, 52 fichiers, PASS** — dont le cas 1 (invité,
rôle anon, commande sans compte) toujours vert, qui est la preuve de non-régression du parcours
invité. Cas 5bis ajouté (plan 42 → 43).

⚠️ Une assertion de trop avait d'abord été écrite (« aucune ligne payments créée ») : elle échouait
parce que la commande de test porte DÉJÀ le paiement légitime du cas 4. Retirée plutôt que
rafistolée — le `order_not_found` prouve la garde à lui seul, la fonction retournant avant tout
insert. Une assertion fausse qui passe est pire qu'une assertion absente.

### La liste blanche posée, et `consume_partner_invitation` refermée

Premier geste de code de la décision ③. Le socle manquait entièrement : `is_anonymous` n'existait
nulle part dans le projet.

**`public.is_anonymous_session()`** — `stable`, `security definer`, `set search_path = ''`,
`auth.uid()` enveloppé en `(select auth.uid())`. ⚠️ Elle lit **`auth.users.is_anonymous`, pas le
claim JWT**, et c'est le point de conception du lot. `auth.jwt() ->> 'is_anonymous'` fonctionne
(vérifié) et le jeton est signé donc non falsifiable — mais un jeton émis AVANT la conversion d'un
invité porte encore `is_anonymous = true` jusqu'à son rafraîchissement : la garde refuserait alors
quelqu'un qui vient précisément de cesser d'être anonyme. La table est la source de vérité, et la
lecture est une recherche par clé primaire. Une assertion pgTAP fait mentir le claim exprès
(JWT `is_anonymous:true`, table `false`) pour vérifier que la fonction suit bien la table.

Le repli est `false` : « aucune session » n'est PAS « session anonyme ». La garde `auth.uid() is
null` de chaque RPC traite déjà ce cas, avant celle-ci, avec son propre message.

**Application à `consume_partner_invitation`** : refus `anonymous_not_allowed`, placé APRÈS
`not_authenticated` et AVANT toute lecture de l'invitation — sinon le refus dirait à un anonyme si
un jeton est valide, donc un oracle. Vérifié en réel : session anonyme →
`{"ok": false, "reason": "anonymous_not_allowed"}` ; compte à email vérifié →
`{"ok": true, "roles": ["referrer"]}`, donc aucune régression du parcours d'onboarding.

`JoinForm.tsx` porte le message correspondant. Il avait déjà un repli générique, donc rien n'aurait
cassé — mais « Une erreur est survenue » n'aurait pas dit au visiteur de créer un compte d'abord.

**Vérifié** : pgTAP 883 tests, 52 fichiers, PASS (4 assertions ajoutées, plan 11 → 15). Lint,
typecheck, 142 Vitest verts. Types régénérés (`is_anonymous_session` y apparaît).

### Ce que ce lot ne fait PAS, et qu'il ne faut pas croire fait

La décision ③ dit « refus par défaut ». **Une seule RPC sur 81 porte la garde aujourd'hui.** Le
socle existe et le premier nom de la liste est traité, mais la règle n'est pas encore *appliquée* :
rien n'empêche mécaniquement une RPC d'oublier la garde, et les 5 autres fonctions à garde
`auth.uid()` seule n'ont pas été revues (`set_my_payout_account` notamment — l'audit y voyait une
escalade en deux temps dont le premier temps vient d'être fermé, ce qui la neutralise sans la
corriger). Au sens du §11.20, « refus par défaut » reste un souhait tant qu'un contrôle mécanique
ne le vérifie pas. À instruire : liste explicite des RPC autorisées + test qui échoue quand une
nouvelle RPC n'y figure pas.

## 2026-09-10 — Entretien « identité anonyme » terminé : sept points tranchés

Rappel des quatre premiers (2026-09-09) : ① la session naît au PREMIER AJOUT AU PANIER seul ;
② le code référent vit sur le PANIER en base ; ③ REFUS PAR DÉFAUT, liste blanche explicite ;
④ le trigger `on_auth_user_created` reste inchangé. Les trois derniers, ce jour :

**⑤ L'identité anonyme EST le compte — « juste sans mot de passe ».** Formulation de Jérôme, et
elle est plus simple que les trois options que je lui proposais. Conséquences qu'il énonce
lui-même : *toute* commande est reliée à un utilisateur (donc `orders.account_id` devient NOT NULL,
alors qu'elle est nullable aujourd'hui — 1 commande sur 5 en local n'a pas de compte), et un même
email peut porter plusieurs commandes, y compris de plusieurs identités.

⚠️ **Ce que ça implique et qu'il faut écrire noir sur blanc : PAS de `updateUser({email})`.** Si
l'email était lié à `auth.users`, Supabase imposerait son unicité — donc « plusieurs commandes sur
le même mail » deviendrait faux dès le deuxième navigateur. L'email reste `orders.holder_email`,
une donnée de commande, jamais un identifiant de connexion. Ce choix supprime d'un coup les deux
problèmes que la doc Supabase nomme elle-même : l'échec de `updateUser` quand l'email appartient à
un compte existant (qui aurait demandé un chemin de repli en plein tunnel de paiement, plus une
réassignation des entités de l'anonyme vers le compte trouvé), et le *pre-account takeover* — lier
une identité sur un email non vérifié.

**Gain collatéral non anticipé** : `orders.account_id` NOT NULL referme le dernier trou de
`create_payment_intent`, celui laissé ouvert délibérément le 2026-09-09 (une commande invité reste
aujourd'hui payable par le porteur de son order_id, faute de propriétaire à comparer).

**⑥ Aucune confirmation d'email supplémentaire.** L'email #7 `client_order_confirmed` part déjà sur
`orders.holder_email` au paiement : une adresse fausse se voit tout de suite, le client n'a pas son
voucher. Comme l'email n'est pas un identifiant (⑤), une confirmation en plus ne protégerait rien
et ajouterait une friction dans le tunnel. Un contrôle de forme à la saisie suffit.

**⑦ Purge des identités anonymes sans commande après 30 jours.**

⚠️ Nuance à ne pas perdre : Jérôme a dit « **si pas de commande reliée** purge après 30j » — il n'a
pas mentionné le panier, que ma question incluait pourtant dans le critère. Pris au mot, un panier
de plus de 30 jours ne protège donc PAS son identité et part avec elle. C'est cohérent (un panier
de 30 jours est mort) mais ce n'est pas ce que la question posait : à confirmer en écrivant la
spec, pas à supposer.

### Le fait qui a reformulé la question ⑤ avant qu'elle soit posée

La doc Supabase a été consultée AVANT de proposer une conception, plutôt qu'après : `updateUser`
échoue si l'email appartient à un compte existant (conflit à gérer à la main, avec réassignation
des données de l'anonyme), et Supabase refuse de lier une identité sur un email non vérifié en
nommant le risque — *pre-account takeover*. Sans cette lecture, l'option « conversion automatique »
aurait été proposée comme praticable.

### Reste à faire

La spec n'est pas écrite. Les sept décisions ne vivent que dans ce journal et dans `docs/backlog.md`.

### La spec 31 écrite — et une huitième décision que la reconnaissance a arrachée

Reconnaissance à 6 agents AVANT d'écrire une ligne, selon la leçon mesurée la veille (« la
reconnaissance à 5 agents, le plus petit budget de la journée, a trouvé trois choses qui ont changé
le plan avant la première ligne de code » ; à l'inverse, 7 agents lancés à écrire du code déjà
spécifié n'avaient rien décidé). **147 faits vérifiés, 6 surprises, 4 qui ont changé le plan** :

1. **`create_manual_order_line` écrit `account_id = null` EN DUR** alors que son appelant est
   toujours authentifié — la « reserva manual desde la agenda » de la spec 20, quand un client
   appelle ou se présente sur place. La décision ⑤ la cassait, et **aucune des sept décisions ne
   la couvrait**. Huitième question posée à Jérôme, qui a d'abord demandé ce qu'était une
   réservation au comptoir — le terme venait de moi, pas de son produit. Sa réponse (« une résa
   doit avoir au moins une personne donc tu link avec ça ? ») visait la solution sémantiquement
   juste : une identité par client. Vérification faite : `insert into auth.users` réussit **en
   local** depuis une fonction `security definer` mais est refusé sur Supabase Cloud (restriction
   plateforme, `seed.sql` l. 29-33) — une migration qui compterait dessus aurait passé tous les
   tests et cassé au déploiement. ⑧ : compte technique unique, la personne restant dans
   `holder_name`/`holder_phone`.

2. **L'attribution se serait déplacée sur l'identité toute seule.** `create_order` porte déjà les
   deux blocs `saved_attribution_code` (lecture → `source='account'`, puis écriture), gardés par le
   seul `v_account_id is not null`. Inoffensifs aujourd'hui faute d'`auth.uid()` chez un invité ;
   **actifs le jour de ①, sans qu'une ligne de SQL n'ait bougé** — livrant exactement le contraire
   de ②. C'est la trouvaille la plus précieuse du lot : une régression invisible dans un diff.
   D'où un invariant *négatif* dans la spec, avec test pgTAP exigé.

3. **Le job de purge pouvait mourir en silence.** Les 20 FK vers `partner_accounts` sont toutes
   NO ACTION, zéro CASCADE — bon filet, mais un `delete` ensembliste **avorte en entier** dès
   qu'une seule identité est bloquée (une ligne `audit_log`, immuable par décision, ou un
   `comm_campaign_targets` laissé par une campagne « Todos »). Vérifié : deux identités, une seule
   bloquée, zéro purgée.

4. **`signInAnonymously()` n'est pas idempotent** : `POST /signup` inconditionnel puis remplacement
   de la session. Appelé naïvement dans `addLine`, il déconnecterait un client connecté au premier
   clic.

Et le prérequis n°0, sans lequel toute la spec est inerte : `enable_anonymous_sign_ins = false`
dans `supabase/config.toml` (l. 229).

### Ménage imposé par un contrôle, et un point refermé qui traînait

Le hook `post-edit-check-instructions` a refusé l'écriture : `docs/backlog.md` était à 67 lignes
pour un plafond de 60. Sa propre règle prescrit qu'un groupe entier parte plutôt qu'un élagage au
hasard — huit points de dette produit/admin sont donc partis dans `docs/dette-technique.md`, où un
premier groupe était déjà allé le 2026-09-08. Rien n'a été supprimé.

⚠️ En faisant ce ménage, un point s'est révélé **refermé le jour même sans avoir été retiré** :
« le chemin des propositions SOCIO ne porte ni `external_booking_url` ni `price_label` ». La
migration `20260909180000` et `productEditPayload.ts` l'ont réglé pour tous les types SAUF evento.
Le point a été fusionné avec celui qui portait déjà le cas evento, plutôt que laissé à double.
C'est exactement ce que la règle du backlog demande — retirer dès qu'un point est fermé, vérifié
par le code — et ça n'avait pas été fait dans la foulée du commit.

Backlog : 67 → 52 lignes. `docs/specs/README.md` complété, index régénéré : 46 documents.

### Non fait

La spec est en **brouillon** : elle n'est pas validée par Jérôme, et aucune ligne de code n'a été
écrite. Ses deux points ouverts sont nommés en §10 — le critère exact de la purge (« si pas de
commande » a-t-il voulu dire « ni panier » ?) et le nom du compte technique.

### Les deux points ouverts de la spec 31, tranchés le jour même

**⑨ Seule une commande épargne une identité de la purge — un panier ne la protège pas.** La
question avait été reposée explicitement parce que la première réponse de Jérôme (« si pas de
commande reliée ») ne reprenait pas le « ni panier » que ma question incluait : c'était bien un
choix, pas une omission. Le coût est maintenant écrit dans la spec comme invariant 10 plutôt que
laissé implicite — le panier portant l'attribution, un visiteur venu par le QR d'un partenaire qui
revient après 30 jours retrouve un panier vide **et le référent perd sa commission**, sans que ni
l'un ni l'autre ne l'apprenne. Si le support voit un jour remonter ce cas, c'est cette ligne qu'il
faudra rouvrir : elle est nommée pour ça.

**⑩ Le compte technique des réservations manuelles réutilise `reserva-manual@hifago.local`.** Le
choix n'est pas cosmétique : cette adresse est **déjà** écrite par `create_manual_order_line` dans
`orders.holder_email`, et elle figure déjà dans `NON_REAL_EMAIL_SENTINELS`
(`apps/admin/lib/whatsapp.ts` l. 27-30) aux côtés de `sin-email-migrado@hifago.local`, si bien
qu'`isRealClientEmail` la filtre et qu'aucun écran ne la présente comme un contact réel. Choisir
une valeur neuve aurait exigé une troisième sentinelle — et l'oublier aurait affiché un faux email
à un prestataire, exactement le défaut que ce filtre existe pour empêcher.

La spec 31 porte donc **10 invariants** et ne garde qu'un point ouvert, sans effet sur ses quatre
tranches : faut-il qu'un invité puisse renseigner son nom une fois pour toutes
(`update_my_account_profile`) plutôt qu'à chaque commande ?

## 2026-09-10 (suite) — Tranche 1 de la spec 31 implémentée : session, garde create_order, invariant 3

Go de Jérôme, effort `high` seul (contrats fixés par les 8 décisions de la spec). Trois livrables,
et deux détours obligatoires trouvés en creusant — l'un a mérité de reposer la question à Jérôme,
l'autre non.

### `create_order` : la garde réintroduite, en connaissance de son historique

Avant de toucher quoi que ce soit, le test existant disait lui-même *« not_authenticated n'existe
plus »* — signal qu'il fallait comprendre POURQUOI avant d'agir. Trouvé :
`20260814090000_create_order_guest_checkout.sql`, correctif du 2026-08-13 pour que la réservation
invité reste possible SANS COMPTE (cahier §1/§2, décision de Jérôme : « forcer un compte avant
achat fait perdre des ventes »).

**La remettre ne renverse pas cette décision.** Elle porte sur le mot de passe, jamais exigé. Ce
qui change : `CartContext` crée désormais une identité anonyme au premier ajout au panier, donc
`auth.uid()` n'est plus jamais nul quand un vrai visiteur atteint `create_order` — la garde ne
bloque que ce qui n'a plus aucun chemin légitime (un appel direct hors panier). Vérifié : seul
`CheckoutForm.tsx` appelle cette RPC, et il lit `lines` de `useCart()` avec un garde `if
(lines.length === 0) return` — impossible d'atteindre le checkout sans être passé par
`addLine` d'abord.

Même migration : le bloc d'attribution (`saved_attribution_code`, lecture ET écriture) exclut
désormais explicitement `is_anonymous_session()` — invariant 3, la régression qui se serait
activée d'elle-même le jour où ① entre en vigueur. Squelette anti-survente intouché (aucun `for
update`, aucun décompte modifié) : pas de nouveau test de concurrence requis.

Vérifié en réel, en transaction annulée : appel sans identité → `not_authenticated` (avant même
`empty_cart`) ; session anonyme + code référent présenté → la commande résout l'attribution
normalement, MAIS `saved_attribution_code` reste vide sur l'identité anonyme.

### Le détour qui a mérité de reposer la question : le test de la RPC anti-survente

La garde casse ~15 scénarios « invité » de `create_order.test.sql`, qui prouvaient explicitement
`orders.account_id null` sous rôle `anon` sans aucune identité — exactement le chemin qu'aucune
interface réelle ne peut plus emprunter, mais que le test simulait délibérément. Ce n'est pas une
régression (mesuré : le seul appelant réel passe par le panier), mais c'est un chantier — pas un
ajustement d'une ligne — sur le test de la RPC la plus critique du projet. Reposé à Jérôme avec le
chiffrage exact plutôt que décidé seul (avant-la-spec §4) : « oui, tout de suite ».

Fait : helper `test_login_anonymous()` (même patron que `test_login`, claim `is_anonymous:true`),
fixture `auth.users` anonyme, les 9 blocs `test_logout(); set local role anon` convertis. Une
assertion neuve (« 15b bis ») prouve l'invariant 3 sur le cas où l'ancien code aurait le plus
sûrement persisté — un compte présentant un code frais valide. Et le commentaire du cas 16, qui
justifiait l'isolation par « anon n'a pas de ligne partner_accounts », est devenu FAUX par
construction (décision ④ : toute identité anonyme en a une) — corrigé pour dire la vraie raison
(invariant 3, plus une identité distincte du compte 021 déjà utilisé). Trouvé le même motif dans
`order_lines_holder_contact_operator.test.sql`, un second fichier que la reconnaissance n'avait pas
mentionné — corrigé à l'identique.

**94 assertions dans create_order.test.sql (93→94), 884 tests pgTAP au total, PASS.**

### `CartContext` : les invariants 1 et 2, et un troisième trouvé en les codant

`addLine` devient async : vérifie qu'aucune session n'existe (`getSession()`) avant d'appeler
`signInAnonymously()` — jamais l'inverse, parce que cette fonction **n'est pas idempotente** (POST
`/signup` inconditionnel, remplace la session locale). Sans cette garde, un client RÉELLEMENT
CONNECTÉ aurait été déconnecté à son premier clic « Ajouter au panier ». Échec fermé : si la
session ne peut pas être établie, la ligne n'entre pas dans le panier — jamais de panier sans
identité derrière, qui produirait une commande orpheline que `create_order` refuse désormais.

Conséquence directe, pas anticipée dans le fichier de la spec : les 3 formulaires de réservation
(`ReservationForm`, `SlotReservationForm`, `LodgingReservationForm`) appelaient `addLine` en
fire-and-forget optimiste. `await` + `toast.danger` sur échec dans les trois, une clé i18n
`addToCartError` ajoutée aux deux locales (`ProductPage` namespace, déjà utilisé par les trois).
`PanierPre.tsx` (fixture Storybook, jamais exécutée par la suite de test) laissé fire-and-forget —
fonctionnellement correct, juste non attendu.

Le commentaire de tête de `CartContext.tsx`, que la spec elle-même avait identifié comme déjà faux
(citant le §3e comme justification durable alors qu'il a été réécrit et validé le 2026-09-07), est
corrigé dans le même geste.

### Ce qu'un test a révélé, et qui n'était dans aucun plan

`SiteHeader.test.tsx` préremplissait le panier via le même `addLine` — 4 tests sur 14 sont tombés
(`Cannot read properties of null`, badge affichant « vacío » au lieu du compte attendu), plus un
rejet non attrapé : `@supabase/ssr` lève faute d'URL/clé en environnement de test. `getSession`
mocké (session déjà active, comme `CartContext.test.tsx`), `rendu()` devient async et flushe la
micro-tâche via `act` avant de rendre le container — 16 sites d'appel convertis. **14/14 verts.**

### Vérifié — l'état final

pgTAP 884/884 · apps/web 70 fichiers/635 tests (dont 3 neufs pour les invariants 1/2, et les 14 de
SiteHeader réparés) · apps/admin 142/142 (témoin, non touché) · lint et typecheck propres sur les
deux apps · types Supabase régénérés (aucune colonne changée, diff vide, attendu).

### Non fait, assumé

Tranches 2 (compte technique, backfill, `account_id` NOT NULL), 3 (liste blanche étendue aux 80
autres RPC) et 4 (purge) restent à faire. `security_definer_exposure.test.sql` accepte toujours
`auth.uid(` comme garde suffisante — n'a pas grondé sur l'ajout de la garde `create_order`
puisqu'elle EST une garde `auth.uid()`, mais ne vérifierait pas non plus son ABSENCE sur une
future RPC. Rien n'est committé à l'écriture de cette entrée.

## 2026-09-10 (suite) — Tranche 2 de la spec 31 : orders.account_id NOT NULL, et un chantier de tests bien plus large que prévu

Go de Jérôme, effort `high` seul. Le lot livré est celui annoncé (compte technique, backfill,
NOT NULL ×2, seed) — mais la mesure faite AVANT d'écrire la migration a évité une erreur de
conception, et la vérification APRÈS a découvert un chantier de tests que rien n'avait anticipé :
8 fichiers pgTAP cassés par la contrainte, dont un qui a révélé qu'une fonction entière
(`client_key_for_order`) perd deux de ses trois branches en usage réel.

### La question posée avant d'écrire une ligne : deux natures de `account_id null`

Mesuré en local avant toute migration : une seule commande à `account_id null`, et ce n'est PAS
une réservation comptoir — c'est un fixture `seed.sql` d'invité web antérieur à la Tranche 1
(`cliente.directo.seed@test.local`). Le compte technique de l'invariant 8 n'a de sens que pour le
comptoir. Question posée à Jérôme plutôt que devinée (avant-la-spec §1) : le même compte technique
absorbe-t-il aussi ces orphelines historiques, ou un second compte dédié ? Réponse : le même —
coût mesuré nul (aucune donnée réelle n'existe, `CLAUDE.md` §7.3).

### Le compte technique — pourquoi son email n'est pas neuf

`e0000000-0000-4000-8000-000000000001`, `reserva-manual@hifago.local` : délibérément la MÊME
adresse déjà écrite par `create_manual_order_line` et déjà listée dans
`NON_REAL_EMAIL_SENTINELS` (`apps/admin/lib/whatsapp.ts`) — une valeur neuve aurait exigé une
troisième sentinelle. Créé via l'API Admin (`seed_auth_users.mjs`, jamais un insert SQL direct,
refusé sur Supabase Cloud depuis le 2026-08-21) — et le fixture "commande directe" du seed pointe
désormais vers un compte SÉPARÉ (`d0000000-...-000003`), jamais le compte technique : un invité web
historique n'est pas une réservation comptoir, même sans identité anonyme "vivante" (la créer
exigerait `signInAnonymously()`, hors de portée d'un script Admin API).

### Le backfill, prouvé deux fois

Reset complet (`db:setup`) : 0 ligne `null` sur 5 commandes, contrainte posée, compte technique
créé — le chemin normal marche. Mais un `db reset` ne peut PAS exercer le backfill lui-même (0
ligne `null` au moment où la migration s'applique, puisque le seed n'a pas encore tourné). Preuve
séparée, en transaction annulée : `alter ... drop not null`, insertion d'une ligne orpheline, rejeu
exact des deux `UPDATE` de la migration, vérification que la ligne porte bien le compte technique,
`alter ... set not null` repasse sans erreur. Les deux chemins sont vérifiés, pas un seul.

### Le vrai chantier : 8 fichiers pgTAP cassés, et un de plus dans la suite complète

`db:setup` frais + `test:db` : 8 fichiers en échec (`clients_stage_timezone`,
`list_client_orders_rpc`, `list_clients_rpc`, `notification_admin_events`,
`notification_payment_events`, `order_lines_holder_contact_operator`, `payments`,
`pms_cancellation_queue`) — tous des fixtures directes `insert into orders`/`order_lines` qui
omettaient `account_id`, hérité nullable jusqu'ici. Réparés un par un, avec un souci systématique :
donner une identité DISTINCTE quand le test dépend de la distinction entre clients (les listes
admin), et le compte technique ou une identité partagée quand elle ne compte pas (notifications,
file PMS).

**La découverte qui dépasse le périmètre annoncé** : `list_clients`/`list_client_orders`
appellent tous deux `client_key_for_order(account_id, holder_email, holder_phone, order_id)` —
`coalesce(...)`. `account_id` n'étant plus jamais null, ses branches email/téléphone/order_id
deviennent **structurellement inatteignables** par les deux seuls appelants réels (vérifié :
`select proname from pg_proc where prosrc ilike '%client_key_for_order%'` n'en donne que deux).
Pas une régression fonctionnelle — l'écran admin construit toujours son URL depuis le client_key
que `list_clients` a lui-même renvoyé, jamais en tapant un email à la main — mais un fait qui
mérite d'être écrit plutôt que découvert en confusion dans six mois. `client_key_for_order`
elle-même n'est PAS modifiée (hors périmètre de cette tranche) ; sa couverture email/téléphone,
qui ne pouvait plus être construite via une vraie ligne `orders`, est déplacée dans
`list_client_orders_rpc.test.sql` en appelant la fonction PURE directement, sans passer par la
table — 3 assertions neuves, la seule façon de garder cette logique testée plutôt que orpheline.

**Un test a aussi confirmé, en le cassant, que les Tranches 1 et 2 se referment vraiment
ensemble** : `payments.test.sql` cas 1a-1d appelait `create_payment_intent` sous rôle `anon` SANS
AUCUNE session, en supposant que ça équivalait à « être le propriétaire » (vrai dans l'ancien monde
`account_id null`). Une fois l'Order de test dotée d'une vraie identité, cet appel est devenu un
tiers — refusé par la garde de la migration `20260909190000` (Tranche 1 du matin). C'est
exactement l'invariant 6 qui se ferme : la fuite laissée volontairement ouverte ce jour-là («
l'identité anonyme refermera ce dernier cas d'elle-même ») s'est refermée EXACTEMENT comme prévu,
et c'est un test qui vient de le prouver plutôt qu'une affirmation. Cas 1a/1b et cas 2 corrigés
pour appeler avec la propre session de l'invité (`test_login` sur son identité), comme le ferait
réellement `CheckoutForm.tsx`.

### Deux commentaires de code devenus faux, trouvés en relisant les seuls appelants TS

`apps/web/.../cuenta/reservas/page.tsx` affirmait qu'un invité (`account_id null`) n'accède jamais
à cet écran — c'est maintenant l'inverse et c'est voulu (la commande d'un invité lui appartient
enfin). `CheckoutForm.tsx` affirmait que `create_order` accepte `account_id null` — plus vrai
depuis ce matin, corrigé pour dire pourquoi le comportement observable ne change pourtant pas
(CartContext a déjà posé l'identité avant que ce formulaire ne s'exécute).

### Vérifié — l'état final

pgTAP **887/887**, 52 fichiers. `apps/web` 70 fichiers/635 tests, `apps/admin` 15/142 (témoin, non
touché). Lint et typecheck propres sur les deux apps, avant ET après les deux corrections de
commentaires. Types Supabase régénérés : diff réel cette fois, `account_id` perd son `| null` sur
`orders`/`order_lines` (Row/Insert/Update) — aucun code TS n'écrit directement dans ces deux
tables (confirmé par un typecheck propre sans aucun ajustement de call site), seules les deux RPC
le font.

### Non fait, assumé

Tranches 3 (liste blanche étendue — une seule RPC sur ~80 la porte) et 4 (purge à 30 jours)
restent à faire. Le fait `client_key_for_order` (deux branches mortes en pratique) est consigné,
pas corrigé — hors périmètre, decision de Jérôme à solliciter séparément si un jour ça compte.

## 2026-09-10 (suite) — Tranche 3 de la spec 31 : la liste blanche devient un contrôle mécanique

Go de Jérôme, effort `high` seul. Remesuré AVANT d'écrire quoi que ce soit : l'audit du 2026-09-09
datait d'avant les Tranches 1/2, et un premier balayage retrouvait les mêmes 4 candidats du matin
— jusqu'à ce qu'une requête plus large (sans exclure `partner_id_for_account`) en révèle **4 de
plus** : `submit_establishment_creation_proposal`, `withdraw_establishment_proposal`,
`withdraw_product_proposal` avaient toutes trois un garde réel (`partner_id_for_account(...) is
null → reject`), simplement invisible au regex de la veille qui excluait ce nom du bucket « sans
garde » sans jamais vérifier qu'il l'excluait aussi de la liste des CANDIDATS trouvés. Les trois
sont effectivement fermées (une identité anonyme n'a structurellement jamais de `partner_id`,
décision ④) — aucune n'a été touchée.

### Les deux vraies RPC ouvertes, et pourquoi l'une des deux ne l'était qu'en apparence

`update_my_account_profile` — RIEN ne l'empêchait : une session anonyme pouvait écrire son
`full_name`/`phone` sur sa propre ligne `partner_accounts`, exactement ce que l'audit du
2026-09-09 avait signalé (« RÉUSSIT pour un anonyme »). L'invariant 5 de la spec l'exclut déjà
explicitement de la liste blanche (point ouvert §10 : « faut-il qu'un invité renseigne son nom une
fois pour toutes ? non tranché ») — donc refermée par défaut, sans nouvelle question à poser :
l'entretien avait déjà tranché « non, pour l'instant ».

`set_my_payout_account` — en réalité déjà fermée en pratique : `select partner_id into v_partner_id
from partner_accounts where id = auth.uid(); if v_partner_id is null then raise` est exactement le
même filet que `partner_id_for_account`, juste recopié en ligne au lieu d'appeler le helper nommé.
Refactorée pour appeler `partner_id_for_account(auth.uid())` — comportement IDENTIQUE, mais
désormais reconnaissable par le contrôle mécanique sans lui ajouter un cas particulier. Vérifié
que les deux tables (`apps/admin/partner/(app)/account/ProfileBlock.tsx`,
`PayoutAccountBlock.tsx`) vivent sous des sessions Supabase indépendantes de `apps/web`
(`CLAUDE.md` §2.1) : aucune session anonyme du site vitrine ne pouvait structurellement les
atteindre — défense en profondeur, pas une fuite réellement exploitable aujourd'hui.

### Le vrai livrable de cette tranche : un test qui détecte une RPC neuve, pas juste ces deux-là

`security_definer_exposure.test.sql` gagne un cas 2 : parmi les fonctions qui passent le cas 1 via
`auth.uid(` SEUL (ni `is_admin`/`has_capability`, qui excluent déjà structurellement un anonyme),
exige `is_anonymous_session(` OU `partner_id_for_account(` OU une présence nommée dans la liste
blanche (invariant 5 : `create_order`, `create_payment_intent`, `cancel_order` — les RPC 100 %
publiques comme `search_catalog` n'y figurent pas, elles ne contiennent aucun `auth.uid(` donc ne
sont jamais captées par ce filtre).

**Prouvé, pas seulement écrit vert** : une RPC fictive créée en transaction annulée
(`auth.uid() is null` pour toute garde, non whitelistée) est bien détectée par la requête du cas 2
avant d'être ajoutée au test lui-même — le contrôle attrape une vraie violation, ce n'est pas un
test qui passe par construction.

### Vérifié

pgTAP **892/892**, 53 fichiers (nouveau : `liste_blanche_tranche3.test.sql`, 4 assertions —
refus anonyme + non-régression sur un vrai operator pour les deux RPC, aucun test dédié n'existait
avant ce lot pour ni l'une ni l'autre). Lint, typecheck, 142 Vitest `apps/admin` verts. Types
Supabase régénérés, diff vide (attendu : aucune signature de fonction n'a changé).

### Non fait, assumé

Tranche 4 (purge à 30 jours) reste à faire. Le contrôle mécanique ne couvre que les fonctions dont
le SEUL garde est `auth.uid()` — une RPC future sans AUCUN garde d'identité reste attrapée par le
cas 1 (inchangé), pas par le nouveau cas 2 ; les deux se complètent, aucun des deux ne remplace
l'autre.

## 2026-09-10 (suite) — Tranche 4 de la spec 31 : la purge, et un trou de sécurité re-découvert avant de le recommettre

Go de Jérôme, effort `high` seul. La spec elle-même avait laissé une vraie inconnue d'architecture
ouverte (§0, prérequis) : rien ne garantit qu'un `DELETE FROM auth.users` a le même droit qu'un
`INSERT` sur Supabase Cloud (l'INSERT est refusé, constaté le 2026-08-21). Posé à Jérôme avant
d'écrire une ligne, avec les trois formes possibles (SQL pur risqué, file + Edge Function sûre mais
plus lourde, les deux couches séparées) — choix : SQL pur, risque assumé, à vérifier en priorité
dès qu'un projet préprod existe.

### La logique de purge, et le piège qu'elle devait éviter par construction

`purge_expired_anonymous_identities()` : `is_anonymous = true`, `created_at < now() - 30 jours`,
`not exists` de commande. La spec avait déjà nommé le piège central (§0 cas limites, écrit le
2026-09-10 matin) : un `delete` set-based avorte ENTIER dès qu'une seule identité est bloquée par
une FK `NO ACTION` (`audit_log` immuable, `comm_campaign_targets`...) — 20 FK vers
`partner_accounts`, mesurées la veille. Plutôt que d'énumérer ces 20 tables par leur nom (une liste
qui se périme dès qu'une nouvelle FK apparaît), le job supprime IDENTITÉ PAR IDENTITÉ et rattrape
`foreign_key_violation` — la ligne bloquée est comptée, nommée dans le retour, jamais retentée en
silence, et surtout jamais capable d'arrêter les autres purges du même lot.

**Vérifié en réel, 5 cas dans une seule transaction annulée** : purgeable → disparaît ; avec une
commande → jamais touchée (invariant 9, quel que soit l'âge/statut de la commande) ; trop jeune →
jamais touchée ; bloquée par `audit_log` → skip nommé, ET les autres candidates du même lot purgées
quand même (LA preuve que le piège est vraiment évité, pas seulement documenté) ; compte réel →
jamais candidat. `partner_accounts_id_fkey ... on delete cascade` vérifié avant d'écrire une seule
ligne : supprimer `auth.users` suffit, pas besoin de toucher `partner_accounts` séparément.

### Le trou que j'allais recommettre en copiant un patron sans le vérifier

Migration écrite sur le modèle exact de `expire_stale_payment_orders` (le job pg_cron le plus
proche) — et son commentaire affirmait, recopié sans vérifier : « aucun grant à
anon/authenticated/service_role ». **Faux pour la nouvelle fonction**, vérifié après coup :
`has_function_privilege('anon', 'purge_expired_anonymous_identities()', 'EXECUTE')` rendait `true`.
PostgreSQL accorde EXECUTE à PUBLIC par défaut sur toute nouvelle fonction — l'inverse exact de la
posture des TABLES (fermées par défaut dans ce projet). `expire_stale_payment_orders` elle-même
n'était protégée que par un `revoke` ajouté APRÈS coup, dans une migration corrective du 2026-08-27
(`20260828002053_revoke_internal_rpc_from_data_api_roles.sql` : « n'importe qui pouvait déclencher
les jobs à volonté », 6 fonctions touchées ce jour-là) — pas par sa migration de création
elle-même. En copiant le PATRON de création sans copier la correction séparée qui l'a suivie, ce
lot allait reproduire exactement le même trou, dans le même projet, un mois après sa découverte.

`revoke all on function public.purge_expired_anonymous_identities() from public, anon,
authenticated;` ajouté, `service_role` conservé (même précédent). Vérifié après correction :
`false`/`false`/`true`. **Consigné dans `.claude/rules/supabase.md`** (chargée sur tout fichier
`.sql`) comme piège distinct des grants de table : « grants par défaut, sens INVERSE selon table ou
fonction » — pour qu'un futur job pg_cron ne le redécouvre pas une troisième fois.

### Vérifié — l'état final

pgTAP **899/899**, 54 fichiers (nouveau : `purge_expired_anonymous_identities.test.sql`, 7
assertions couvrant les 5 cas). Lint et typecheck propres sur les deux apps (aucune n'appelle cette
fonction — cron-only, aucun grant applicatif). Types Supabase régénérés : 1 ligne ajoutée
(signature de la fonction), rien d'autre.

### Non fait, assumé

**Le risque Cloud du DELETE reste non vérifié** — c'est le point le plus important laissé ouvert
par ce lot, écrit en tête de la migration elle-même : à tester en priorité dès qu'un projet préprod
existe, avant de faire confiance à ce job en production. Les 4 tranches de la spec 31 sont
maintenant toutes livrées ; la spec elle-même garde un point ouvert (§10 : `update_my_account_profile`
pour un futur écran `/cuenta`, sans effet sur ce qui est livré).

## 2026-09-10 (suite) — `/next` : backlog resynchronisé, exploit des oracles de lecture reproduit

Point « liste blanche des sessions anonymes n'est appliquée qu'à UNE RPC sur 81 » retiré du
backlog : entièrement refermé par la Tranche 3 ci-dessus (contrôle mécanique posé, les deux vraies
RPC ouvertes corrigées, les trois autres candidates confirmées déjà gardées) — la ligne datait
d'avant la tranche, jamais mise à jour.

En cherchant la prochaine action de code (le panier en base reste gaté par une conception à
concevoir, pas un « 2 minutes »), repris le point voisin du backlog resté « à instruire » depuis
l'audit du 2026-09-09 : les 5 fonctions-oracle d'identité (`is_admin`, `has_capability`,
`has_admin_capability`, `partner_id_for_account`, `establishment_slug_from_name`), SECURITY
DEFINER, prenant un uuid en paramètre plutôt que `auth.uid()`. Vérifié en local, jamais fait avant
: `has_function_privilege('anon', 'public.is_admin(uuid)', 'EXECUTE')` → `true` pour les 5 (`anon`
ET `authenticated`). Exploit reproduit en transaction annulée (jamais engagée) : sous `set local
role anon`, `select is_admin('a0000000-0000-4000-8000-000000000005')` (un vrai admin du seed)
répond `true`, sans aucune session — exactement le 1-bit-par-uuid décrit par l'audit, maintenant
prouvé plutôt que supposé.

Correctif non appliqué : ce point reste listé « Arbitrage Jérôme requis » (CLAUDE.md §10) — revoke
EXECUTE de `anon`/`authenticated` casserait potentiellement des policies RLS qui appellent ces
helpers sous ces rôles ; l'usage réel dans les policies n'a pas été audité. Piste proposée au
backlog : fermer `anon` d'abord (aucune identité anonyme n'a de raison de tester `is_admin`),
`authenticated` en attente de cet audit.

## 2026-09-10 (suite) — Spec 32 « panier en base » écrite (`docs/specs/32-panier-en-base.md`)

Jérôme a redirigé vers la suite logique du backlog plutôt que l'arbitrage des oracles de lecture
ci-dessus : la spec 31 étant entièrement livrée, « panier en base » (décidé le 2026-09-07, gaté
derrière l'identité anonyme) devenait la prochaine action. Reconnaissance d'abord
(`CartContext.tsx`, schéma `order_lines`, `create_order` vivant) avant d'interroger Jérôme, suivant
`docs/specs/avant-la-spec.md`.

**Cinq décisions tranchées en entretien** (2 tours de questions) : `cart_items` séparée
d'`order_lines` (jamais un statut dessus, pour ne pas mélanger lignes non engagées et lignes ayant
traversé le verrou anti-survente) ; pas de dénormalisation, toujours `product_id` + jointure à la
lecture ; prix toujours lu en direct depuis `products`, jamais un snapshot ; plafond global sur
toute la commande ; `create_order` lira désormais ses propres lignes et son attribution côté
serveur via `auth.uid()`, au lieu de les recevoir en paramètres d'un client qui pourrait les
falsifier.

### Deux trouvailles en creusant, avant d'écrire une ligne de spec

En relisant le cahier §3e (réécrit le 2026-09-07, jour de la décision « panier en base ») : il
répondait déjà à la moitié de ce qui allait être demandé à Jérôme — panier jamais un stock réservé,
disponibilité revérifiée à la reprise (jamais un retrait automatique), prix jamais figé puisque
`create_order` calcule tout côté serveur. Il fixait aussi déjà, dans le **même paragraphe**, un
plafond global avec des valeurs précises (12 lignes de nuits/36 unités/40 prestations/20 par
ligne) — alors que `docs/01-cahier-des-charges-client.md` §2f, plus bas dans le **même fichier**,
listait encore « plafond global ou par établissement » comme une question ouverte. Contradiction
interne jamais résolue depuis son écriture ; §2f corrigé (barré, renvoyé à §3e).

Deuxième trouvaille, plus sérieuse : les valeurs 12/36/40/20 que §3e dit « codées en dur dans
`create_order`, `v_lodging_lines > 4 or v_lodging_units > 12`, `v_prestation_lines > 20` » citent
en réalité le code **d'avant** le 2026-09-07 (4/12/20, l'ère un-seul-établissement) — vérifié dans
la version vivante de `create_order`
(`supabase/migrations/20260910140000_simplification_spec31.sql:158,161`). La décision du
2026-09-07 (autoriser une commande multi-établissements avec des plafonds relevés) n'a donc jamais
été implémentée. Signalé, pas corrigé silencieusement (spec 32 §10, `create_order` étant de toute
façon réécrit pour lire ses propres lignes — occasion naturelle de le faire dans la même
migration, à confirmer par Jérôme).

Troisième fait retrouvé en relisant spec 31 elle-même plutôt que de redemander : son propre §0
avait déjà engagé le nom de la table (`carts`, pas seulement `cart_items`) et son rôle — porter
`attribution_code`, jamais l'identité elle-même (invariant 3, la trouvaille la plus précieuse de
cette spec-là). Deux tables, pas une : `carts` (une ligne par identité, l'attribution) et
`cart_items` (les lignes, sans prix ni statut).

### Écrit, pas encore codé

`docs/specs/32-panier-en-base.md` : contrat compact (modèle `carts`/`cart_items`, RLS directe —
non capacitaire, `CLAUDE.md` §3.2 —, contrat `create_order` avant/après), invariants, cas limites,
3 points ouverts en §10 (valeur des plafonds ci-dessus, interaction avec
`partner_accounts.saved_attribution_code` qui a une durée de vie différente de `carts`, mécanisme
de revérification de dispo à la reprise — pas inventé ici). `docs/specs/README.md` et
`docs/ai-index.json` régénérés (`npm run docs:index`) ; tenté de corriger au passage le statut
« Brouillon » de la spec 31 dans le README (elle est livrée) — rejeté par le script, qui vérifie
que README et frontmatter s'accordent : reverti, le frontmatter reste `brouillon`, à trancher par
Jérôme (peut-être volontaire tant que la vérification Cloud du job de purge n'est pas faite).
`docs/backlog.md` mis à jour (ligne « panier en base » devient « spec écrite », plafonds retirés de
la liste des points groupés en §2f).

### Périmètre étendu le même jour : l'écran `/carrito`

Jérôme a demandé si des composants restaient à créer pour le panier. Réponse trouvée dans le code
déjà écrit, pas inventée : `/carrito` est une route **déjà prévue** par la spec 27
(`docs/specs/27-architecture-vitrine-et-routage.md:75`, « à créer »), jamais construite — et
`SiteHeader.tsx` porte depuis le 2026-09-02 une constante `ROUTE_PANIER` prête à basculer le jour
où elle naît. En attendant, `CheckoutForm.tsx` fait les deux métiers (liste du panier + paiement)
sur un seul écran, pis-aller documenté comme tel. `apps/web/components/README.md` cite même déjà
`CartSummary` comme exemple d'organism — jamais écrit non plus.

Comme l'invariant 6 de cette spec (ligne indisponible affichée, retirable, jamais retirée seule)
n'a aujourd'hui aucune UI, et que `/carrito` en est l'endroit naturel, proposé à Jérôme d'intégrer
la construction de cet écran au périmètre de la spec 32 plutôt que de le déférer encore. Accepté.
Spec mise à jour : `/carrito` (Server Component, jointure + revérification de dispo),
`CartSummary` (mode éditable sur `/carrito`, lecture seule sur `/pago`), `CheckoutForm.tsx`
simplifié à son seul formulaire de paiement, `SiteHeader` bascule sa constante. Toujours zéro ligne
de code — spec seule, mise à jour avant tout début d'implémentation.

## 2026-09-10 (suite) — Spec 32, Tranches 1+2 implémentées : schéma et `create_order` réécrit

Go de Jérôme pour l'implémentation. Effort `max` pour la partie RPC anti-survente (CLAUDE.md §4),
`high` pour le reste — seul, comme annoncé (`.claude/rules/orchestration.md`).

### Tranche 1 — `carts`/`cart_items` (`20260910150000_carts_et_cart_items.sql`)

RLS directe (CLAUDE.md §3.2), `account_id` référence `partner_accounts(id)` — même convention
qu'`orders`/`order_lines`, jamais `auth.users(id)` directement. Vérifié en réel, transaction
annulée : deux comptes réels du seed, compte B voit 0 ligne du panier de A (`select` filtré par la
policy) et se fait refuser l'insertion d'une ligne au nom de A (`new row violates row-level
security policy`) — pas juste « la policy existe », le refus est reproduit.

### Tranche 2 — `create_order` lit son propre panier (`20260910160000_create_order_lit_panier_et_attribution.sql`)

Corps extrait par `pg_get_functiondef` depuis la version vivante (supabase.md règle 7), modifié
par occurrences comptées, jamais retapé à la main : `p_lines` (paramètre) devient `v_lines`
(variable), peuplée par une requête sur `cart_items` pour `auth.uid()` ; `p_attribution_code`/
`p_attribution_source` (retirés de la signature) remplacés par une lecture de
`carts.attribution_code`/`attribution_source`, en PRIORITÉ sur le repli existant
`partner_accounts.saved_attribution_code` (additif, rien retiré) ; `cart_items`/`carts` vidées
juste après la création de la commande, jamais avant un retour `'ok', false`. Signature changée
(perd deux paramètres) → `drop function` explicite, `grant execute … to authenticated, anon`
reposé (même geste que `20260817190000_orders_holder_email_required.sql:305`, dernier précédent à
avoir changé cette signature — un `grant` sur une fonction ne survit pas à un changement de
signature, piège déjà consigné pour les fonctions, sens inverse des tables).

**Le même geste, en passant** : les plafonds validés le 2026-09-07 (12 lignes de nuits/36
unités/40 prestations) n'avaient jamais été codés — `create_order` portait toujours 4/12/20,
trouvé en préparant la spec (§10). Corrigés dans cette même migration puisque la fonction était de
toute façon réécrite.

**Vérifié en réel, transaction annulée** : panier vide → `empty_cart` ; aucune session →
`not_authenticated` ; panier réel avec `carts.attribution_code = 'SEED-REFACTIVE'` → commande
créée, `referrer_partner_id` résolu, `commission_case = 'external_referrer'`, `cart_items`/`carts`
vidées après coup. Types Supabase régénérés.

### La trouvaille qui a failli passer inaperçue : le test de concurrence ne représentait plus rien

`/hifago-test concurrence` obligatoire après toute RPC anti-survente touchée (skill
`hifago-migration`). En le préparant : `create_order.concurrency.mjs` fait tourner 20 acheteurs
concurrents sous le **même** `account_id` — sans conséquence quand les lignes arrivaient en
paramètre, mais avec un panier en base par compte, les 20 auraient écrit/vidé la MÊME ligne
`cart_items` en même temps. Le scénario ne testait plus l'invariant qu'il prétendait prouver.

Réécrit : N acheteurs **distincts** (`auth.users` + panier `cart_items` propre à chacun avant
l'appel, segment d'id dédié `8100` pour le nettoyage), plus fidèle à la réalité qu'une seule
identité répétant l'appel. **Prouvé, pas juste réécrit** : les deux scénarios tiennent leurs 5/5
runs consécutifs — scénario 1 (20 concurrents, capacité 1) : succès exactement 1, 19 `'full'` ;
scénario 2 (anti-deadlock, paires `[A,B]`/`[B,A]`) : 20/20 succès, aucun deadlock. L'ordre des
lignes d'un panier, autrefois la position dans le tableau JSON, est maintenant l'ordre d'insertion
dans `cart_items` (`create_order` trie par `created_at`) — chaque insertion étant son propre
aller-retour réseau, l'ordre voulu est préservé sans effort supplémentaire.

### Non fait, restant sur cette tranche avant de considérer create_order terminé

4 autres scripts de concurrence (`create_order_camp`, `create_order_date_range`,
`create_order_default_capacity`, `create_order_slot`) partagent le même défaut (un seul
`account_id` partagé) et la même correction à appliquer. 7 fichiers pgTAP appellent l'ancienne
signature, dont `create_order.test.sql` (94 assertions) — pas seulement un renommage de paramètre,
chaque cas doit désormais poser son panier en `cart_items` avant d'appeler la RPC. Écran `/carrito`,
`CartSummary`, `CartContext.tsx`, `CheckoutForm.tsx` (Tranches 3-4 du plan) non commencés.

## 2026-09-10 (suite) — Les 6 scripts de concurrence restants réécrits, suite complète verte

Go de Jérôme. Même correction que `create_order.concurrency.mjs` appliquée à
`create_order_camp`/`create_order_date_range`/`create_order_default_capacity`/`create_order_slot`
: N acheteurs distincts (segment d'id `8100` dans le préfixe propre à chaque fichier), panier posé
en `cart_items` avant l'appel, `create_order` appelé sans lignes. `create_order_date_range` avait
en plus deux groupes indépendants par scénario (imbrication qty variables, plages adjacentes) —
indices d'acheteurs simplement répartis en plages disjointes du même segment.

`set_product_availability.concurrency.mjs` était différent : il avait **déjà** N identités
acheteuses distinctes (nécessaire à son propre scénario, admin vs acheteurs) — pas de
restructuration de fond, juste le panier `cart_items` posé pour chaque identité existante avant
l'appel, et la nouvelle signature à 2 paramètres.

Les deux fichiers `create_manual_order_line_*` qui mentionnaient `create_order` en commentaire
(comparaison de risque) n'appellent pas la fonction elle-même — non touchés, vérifié par grep
avant de les ignorer plutôt que supposé.

**Vérifié en réel** : chaque fichier exécuté individuellement (5/5 runs propres partout), puis
`npm run test:concurrency` (la suite complète, 8 fichiers) exécutée de bout en bout sans échec.
`/hifago-test concurrence` — obligatoire après toute RPC anti-survente touchée — est maintenant
honoré pour de bon, pas simplement déclaré.

### Reste avant de considérer la spec 32 terminée

7 fichiers pgTAP appellent l'ancienne signature de `create_order`, dont `create_order.test.sql`
(94 assertions) — le plus gros morceau restant, chaque cas devant poser son panier en `cart_items`
avant d'appeler la RPC plutôt que de lui passer des lignes. Écran `/carrito`, `CartSummary`,
`CartContext.tsx`, `CheckoutForm.tsx` (Tranches 3-4) non commencés.

## 2026-09-10 (suite) — `create_order.test.sql` adapté, 93/93 vert

Go de Jérôme. Plutôt que de retaper chaque cas (94 assertions, ~30 sites d'appel), deux helpers
pgTAP ajoutés à côté de `test_login` — `test_set_cart(jsonb)` (vide puis repeuple `cart_items`
pour `auth.uid()` depuis le MÊME tableau jsonb qu'avant, juste relocalisé) et
`test_set_attribution(code, source)` (pose `carts.attribution_code`) — non `SECURITY DEFINER`
exprès : passent par la RLS directe comme un vrai client, preuve en passant que ce chemin marche.
Chaque site d'appel : `select test_set_cart(<même littéral jsonb qu'avant>);` puis
`create_order(holder_name, email, phone, consent)` sans les lignes. Contrairement aux scripts de
concurrence, aucune restructuration de fond n'était nécessaire ici (les cas s'enchaînent
séquentiellement, jamais en parallèle) — `test_set_cart` vide `cart_items` avant chaque repose,
donc un cas précédent resté en échec (panier jamais vidé, invariant du §0) ne contamine jamais le
suivant.

**Trouvaille en route, signalée plutôt que contournée** : le cas 3 (« produit inexistant →
`product_not_found` ») ne peut plus être testé — `cart_items.product_id` porte une FK vers
`products`, donc un id inexistant échoue à l'`INSERT` dans `cart_items` avant même d'atteindre
`create_order`. Le garde-fou reste dans le code (défense en profondeur, inoffensif) mais n'est
plus atteignable par ce chemin, et aucun autre ne le semble non plus (supprimer un produit encore
référencé par un panier échouerait sur la même FK). Cas retiré (`plan` 94→93), pas de contournement
de la contrainte pour le garder vert artificiellement. Deux plafonds re-dimensionnés au passage
pour continuer à déclencher les MÊMES garde-fous sous les nouvelles valeurs (12/36/40, cf. lot
précédent) : cas 5 (5→13 lignes lodging) et cas 14c (idem, sous une identité invité).

**Vérifié** : le fichier seul (93/93), puis `npm run test:db` (la suite pgTAP complète, 54
fichiers) — `create_order.test.sql` n'apparaît plus dans les échecs. Les échecs restants : les 6
autres fichiers qui appellent encore l'ancienne signature (attendu, cf. liste ci-dessous), et deux
échecs `audit_log` (`product_availability_rpc.test.sql`, `set_product_availability_socio.test.sql`)
confirmés préexistants et sans rapport — aucune référence à `create_order`/`cart_items` dans 4 des
5 fichiers en échec restants, et `product_availability_rpc.test.sql` rejoué seul confirme 13/14 (le
seul échec restant est la pollution `audit_log` déjà documentée dans `.claude/rules/supabase.md`,
pas une régression).

### Reste avant de considérer la spec 32 terminée

6 fichiers pgTAP appellent encore l'ancienne signature : `create_order_pms_backed.test.sql`,
`date_range_booking.test.sql`, `notification_payment_events.test.sql`,
`order_lines_holder_contact_operator.test.sql`, `order_lines_referrer_rls.test.sql`,
`release_order_after_pms_refusal.test.sql`. Écran `/carrito`, `CartSummary`, `CartContext.tsx`,
`CheckoutForm.tsx` (Tranches 3-4) non commencés.
