{
  "name": "hifago-monorepo",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n web,admin -c blue,magenta \"npm run dev --workspace=apps/web\" \"npm run dev --workspace=apps/admin\"",
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:admin": "npm run dev --workspace=apps/admin",
    "storybook": "npm run storybook --workspace=apps/web",
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "test:db": "supabase test db",
    "test:e2e": "npm run test:e2e --workspaces --if-present",
    "test:concurrency": "for f in tests/concurrency/*.concurrency.mjs; do node \"$f\" || exit 1; done",
    "test:pms-integration": "for f in tests/pms-integration/*.integration.mjs; do node \"$f\" || exit 1; done",
    "test:notification-integration": "for f in tests/notification-integration/*.integration.mjs; do node \"$f\" || exit 1; done",
    "test:notification-real": "node tests/notification-real/send_8_real_emails.mjs",
    "db:setup": "bash scripts/db-setup.sh",
    "docs:index": "node scripts/docs_index.js --build",
    "docs:check": "node scripts/docs_index.js --check"
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
